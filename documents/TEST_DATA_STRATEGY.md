# Test Data Strategy

Created: 2026-06-18

## Purpose

This document records how the project will seed OpenEMR with test data for legacy validation, Workbench reporting, and future side-by-side modernization parity tests.

## Current State

The local legacy baseline is working and has a project-owned synthetic gold dataset applied.

Original local database profile before any seed:

- `patient_data`: 0 rows
- `form_encounter`: 0 rows
- `openemr_postcalendar_events`: 0 rows
- `lists`: 0 rows
- `pnotes`: 0 rows
- `users`: 4 rows

Current verified gold seed profile as of 2026-06-18:

- Patients: 1,000
- Providers and staff: 20
- Facilities: 3
- Insurance records: 1,400
- Appointments: 2,800
- Regular appointment recurrence metadata anchors: 80 preventive-care appointments, including one seeded recurrence exception-date anchor
- Encounters: 2,100
- Vitals: 2,100
- Clinical notes: 2,100
- Problems: 1,500
- Allergies: 900
- Medication list entries: 2,200
- Prescriptions: 2,200
- Lab/procedure orders: 1,000
- Lab reports: 700
- Lab results: 2,400
- Messages: 1,200
- Portal mailbox messages: 400
- Billing line items: 3,000
- Patient history rows: 1,000
- Portal-enabled patients: 200
- Provisioned portal accounts: 200

The generated canonical dataset, summary, and legacy MariaDB seed SQL live under `modernization-workbench/seed-data/openemr-shared-synthetic-v1/generated/`.

Slice 194 extends every canonical patient with deterministic mother and guardian contact fields. The legacy seed maps those values into OpenEMR `patient_data.mothersname`, `patient_data.guardiansname`, `patient_data.guardianrelationship`, `patient_data.guardianphone`, and `patient_data.guardianemail`; the modernized PostgreSQL seed maps the same values into nullable `patients.mother_name`, `guardian_name`, `guardian_relationship`, `guardian_phone`, and `guardian_email`. Slice 195 extends the same permanent canonical patient contract with guardian sex, address, city, state, postal code, country, and work phone, mapped into legacy `patient_data.guardiansex`, `guardianaddress`, `guardiancity`, `guardianstate`, `guardianpostalcode`, `guardiancountry`, and `guardianworkphone`, plus modernized `patients.guardian_sex`, `guardian_address`, `guardian_city`, `guardian_state`, `guardian_postal_code`, `guardian_country`, and `guardian_work_phone`. Slice 196 adds permanent deterministic patient social-detail fields: race, ethnicity, interpreter preference, family size, monthly income, homeless status, and financial review date. These map into legacy `patient_data.race`, `ethnicity`, `interpreter`, `family_size`, `monthly_income`, `homeless`, and `financial_review`, plus modernized `patients.race`, `ethnicity`, `interpreter`, `family_size`, `monthly_income`, `homeless`, and `financial_review_date`. Slice 197 adds permanent deterministic employer fields: employer name, street, city, state, postal code, and country. These map into legacy `employer_data.name`, `street`, `city`, `state`, `postal_code`, and `country`, plus modernized `patient_employers.name`, `street`, `city`, `state`, `postal_code`, and `country`. Slice 198 does not add permanent seed rows; it reuses the existing deterministic provider/user catalog and the existing `providerID` / `provider_id` values for restore-backed primary-provider assignment tests. Slice 201 adds permanent deterministic related-person/contact rows for every canonical patient: legacy seeds OpenEMR `person`, `contact`, `person_patient_link`, `contact_relation`, and `contact_telecom`, while the modernized PostgreSQL seed maps the same data into `patient_related_contacts`. Contact IDs follow `3200000 + (pid - 100000)`, so the `MOD-PAT-0010` parity anchor uses contact `3200010` / `Casey Brooks` with relationship `parent`. Slice 202 adds one permanent deterministic history/lifestyle row for every canonical patient: legacy seeds OpenEMR `history_data`, while the modernized PostgreSQL seed maps the same data into `patient_histories`. The `MOD-PAT-0010` read-only anchor includes former tobacco use, exercise, preventive screening, family history, appendectomy date, and additional history text for database/API/UI parity. Slice 203 enriches the existing 1,400 deterministic insurance rows with subscriber relationship, identity, DOB, sex, address, phone, and employer fields. The legacy seed maps those values into OpenEMR `insurance_data.subscriber_*` columns, while the modernized PostgreSQL seed maps the same values into `insurance_records.subscriber_*` columns without changing the insurance row count. Slice 204 provisions deterministic portal account facts for the existing 200 portal-enabled patients. The legacy seed maps CMS login values into `patient_data.cmsportal_login` and account rows into `patient_access_onsite`; the modernized PostgreSQL seed maps the same values into `patients.cms_portal_login` and `patient_portal_accounts`. Slice 205 adds no permanent seed rows; it reuses the `MOD-PAT-0004` provisioned account and temporarily toggles `portal_pwd_status` / `portal_onetime` or `password_status` / `one_time_token` during reset parity, then restores the original patient-managed/no-reset state. Slice 206 also adds no permanent seed rows; it reuses the same account anchor and temporarily toggles legacy `patient_data.allow_patient_portal` or modernized `patients.portal_enabled` during access revoke/grant parity, then restores the original enabled state. Slice 208 adds no permanent seed rows; it reuses `MOD-PAT-0004` and creates only transient modernized `patient_portal_sessions` rows during sign-in/logout checks. Slice 209 adds no permanent seed rows; it reuses `MOD-PAT-0004`, the two existing portal-related message rows, and the existing 2026-07-28 future appointment to prove portal home parity. Slice 210 adds deterministic portal-mailbox projections of the existing portal-related message facts into legacy `onsite_mail` and modernized `portal_mailbox_messages`: 400 rows for the 200 provisioned portal accounts, with IDs `9300001` through `9300400`. The canonical 1,200 staff-side message count remains unchanged while portal inbox/home parity has a real mailbox source on both targets. Slice 211 adds no permanent seed rows; it creates paired temporary sent/recipient mailbox rows above the seeded ID range during compose parity and deletes them by title/portal user during cleanup. Slice 212 also adds no permanent seed rows; it reuses the seeded `MOD-PAT-0004` inbox message and creates paired temporary reply rows above the seeded range with `reply_mail_chain` tied to the original mailbox thread, then deletes them by title/body/portal user during cleanup. Slice 213 adds no permanent seed rows; it reuses the seeded `MOD-PAT-0004` inbox message and a cleanup-backed temporary reply to verify chronological thread projection, then deletes transient reply rows by title/body/portal user. Slice 214 adds no permanent seed rows; it creates paired temporary sent/recipient mailbox rows above the seeded ID range, soft-deletes the signed-in patient's visible copy with legacy `message_status = Delete` semantics, verifies active-folder hiding and archive visibility, then hard-deletes the temporary rows by title/portal user during cleanup. Slice 215 adds no permanent seed rows; it creates one temporary inbound mailbox row above the seeded ID range, marks the signed-in patient's copy from `New` to `Read`, verifies active-folder retention plus home new-count decrement, and hard-deletes the temporary row by title/portal user during cleanup.

Slice 216 adds no permanent seed rows; it creates two temporary inbound mailbox rows above the seeded ID range, archives the selected patient-visible rows together with OpenEMR `Delete` soft-archive semantics, verifies active-folder removal plus archive visibility, and hard-deletes the temporary rows by title/portal user during cleanup. Slice 217 adds no permanent seed rows; it creates one temporary inbound mailbox row and one temporary sent mailbox row above the seeded ID range, verifies both appear in the signed-in patient's active All-folder view, archives the inbound row to prove All-folder exclusion, and hard-deletes the temporary rows by title/portal user during cleanup. Slice 218 adds no permanent seed rows; it reuses the existing `MOD-PAT-0004` active patient-owned document anchors `8000007` and `8000008` to verify portal document category listing and selected ZIP download behavior. Slice 219 adds no permanent seed rows; it reuses the existing `MOD-PAT-0004` appointment anchors to verify signed-in patient future and past portal appointment lists. Slice 220 adds no permanent seed rows; it creates a temporary pending `^` portal appointment request and matching provider reminder note for `MOD-PAT-0004`, verifies both rows, then hard-deletes them during cleanup. Slice 221 adds no permanent seed rows; it reuses the same `MOD-PAT-0004` portal account, patient primary provider `105`, provider-derived facility `11`, and existing OpenEMR appointment category rows to verify request-form option/default parity. Slice 222 adds no permanent seed rows; it reuses the same `MOD-PAT-0004` portal account plus existing problem, allergy, medication-list, and active-prescription anchors to verify signed-in clinical-summary parity. Slice 223 adds no permanent seed rows; it reuses the same `MOD-PAT-0004` portal account plus existing lab order `5000004`, report `6000004`, and result anchors `7000013` through `7000016` to verify signed-in lab-results parity.

Slice 58 patient payment capture readiness does not add permanent gold records. It reuses the `MOD-PAT-0005` revenue-cycle anchor and creates temporary patient-responsibility AR rows during parity tests, then voids and hard-deletes them so the seeded payment session/activity counts remain stable.

Slice 72 encounter billing linkage mutation readiness also does not add permanent gold records. It reuses `MOD-PAT-0001` encounter `1000013`, creates a temporary `CPT4 99499` billing row during smoke/parity tests, verifies encounter-linked billing and diagnosis visibility, marks the row billed/inactive, and hard-deletes it so the seeded billing count returns to baseline.

Slice 73 encounter diagnosis coding mutation readiness also does not add permanent gold records. It reuses `MOD-PAT-0001` encounter `1000013`, creates a temporary `ICD10 R73.03` fee-sheet diagnosis row during smoke/parity tests, verifies encounter-linked diagnosis-coding visibility, marks the row billed/inactive, and hard-deletes it so the seeded billing count returns to baseline.

Slice 74 encounter fee-sheet entry readiness also does not add permanent gold records. It reuses `MOD-PAT-0001` encounter `1000013`, creates temporary `CPT4 99499` and `ICD10 R73.03` fee-sheet rows during parity tests, verifies encounter-workspace billing and diagnosis visibility, marks both rows billed/inactive, and hard-deletes them so the seeded billing count returns to baseline.

Slice 75 encounter procedure-order entry readiness also does not add permanent gold records. It reuses `MOD-PAT-0001` encounter `1000013`, creates a temporary pending `80053` laboratory order during smoke/parity tests, verifies encounter-workspace procedure-order visibility, and hard-deletes the order so the seeded procedure-order count returns to baseline.

Slice 76 encounter procedure-result entry readiness also does not add permanent gold records. It reuses `MOD-PAT-0001` encounter `1000013`, creates a temporary pending `80053` laboratory order plus reviewed final report/result during smoke/parity tests, verifies encounter-workspace report/result visibility, and hard-deletes the order so the seeded procedure-order/report/result counts return to baseline.

Slice 77 encounter sign-off readiness also does not add permanent gold records. It creates a temporary encounter for `MOD-PAT-0002`, records a temporary `admin` attestation in legacy `esign_signatures` or modernized `encounter_signatures`, verifies signature facts and modernized Sign-Off rendering, deletes the signature, and deletes the encounter so seeded encounter and signature counts return to baseline. Slice 190 encounter amendment history readiness follows the same temporary encounter-signature pattern: it creates a `MOD-PAT-0002` encounter, records three temporary signature rows with two nonblank amendments and one blank note, verifies only the nonblank amendments appear in newest-first history, then deletes the signatures and encounter during cleanup. Slice 78 encounter document upload readiness also avoids permanent seed changes: it creates a temporary text document linked to `MOD-PAT-0001` encounter `1000013`, verifies legacy `documents.encounter_id` or modernized encounter document facts and rendering, then hard-deletes the document so seeded document and encounter-document counts return to baseline. Slice 79 encounter binary document upload readiness follows the same pattern with a temporary PDF/binary document, verifies preview/download metadata and downloaded content, and hard-deletes the document so no permanent binary rows are added. Slice 126 encounter scanned attachment readiness follows the same temporary-row pattern with a PDF document whose notes include `Scan source: front-desk scanner; OCR pending`, verifies derived scan status, capture source, scanned page count, OCR status, and modernized Encounter card rendering, and hard-deletes the document so seeded document and encounter-document counts return to baseline. Slice 80 encounter document sign-off readiness creates another temporary encounter-linked text document, verifies pending review state, signs it as `admin`, verifies approved review metadata, and hard-deletes it so review rows and document counts return to baseline. Slice 81 encounter document denial readiness uses the same temporary-document pattern, denies the encounter-linked document as `admin`, verifies denied review metadata, and hard-deletes it so seeded review rows and document counts remain unchanged. Slice 82 encounter document metadata readiness creates a temporary encounter-linked text document, refiles name, category, date, and notes while preserving the encounter link, verifies the updated metadata and UI rendering, and hard-deletes it so seeded document and encounter-document counts remain unchanged. Slice 83 encounter document move readiness creates a temporary encounter-linked text document, moves it from encounter `1000013` to same-patient encounter `1000011`, verifies source removal and target attachment, and hard-deletes it so seeded source and target encounter-document counts remain unchanged. Slice 84 encounter document content replacement readiness creates a temporary encounter-linked text document, replaces its stored payload in place, verifies replacement preview/current-version facts and UI rendering, and hard-deletes it so seeded document and encounter-document counts remain unchanged. Slice 85 encounter document archive/restore readiness creates a temporary encounter-linked text document, archives it, verifies active-detail hiding and archived-detail visibility, restores it, verifies active rendering again, and hard-deletes it so seeded document and encounter-document counts remain unchanged.

Slice 59 statement generation readiness also does not add permanent gold records. It derives the printable patient statement number, payment instructions, generated text, totals, and line items from the existing `MOD-PAT-0005` demographics, account statement readiness facts, and chronological ledger entries. Slice 60 statement PDF export readiness does not add permanent gold records either; it exports the same generated statement document as deterministic PDF content from the existing anchor. Slice 61 statement batch candidate readiness reuses the seeded billing and AR payment rows across the full patient population to derive a ranked statement work queue and top-candidate delivery metadata without adding permanent seed records. Slice 62 statement batch package export readiness reuses the same ranked candidates and generated statement documents to build a deterministic manifest, summary CSV, and PDF package without adding permanent seed records. Slice 63 collections work queue readiness reuses the same full-population billing and AR payment rows to rank positive past-due accounts by over-90 exposure, past-due amount, balance, and oldest age without adding permanent seed records. Slice 64 collections follow-up task readiness creates temporary pnotes-compatible message rows during smoke/parity tests and removes them during cleanup, so no permanent seed counts change. Slice 65 patient-message assignment readiness reuses the existing `MOD-PAT-0004` message anchor and creates only temporary pnotes-compatible rows for reassignment tests before archive/delete cleanup, so the seeded message count remains stable. Slice 66 patient-message content readiness uses the same anchor and temporary pnotes-compatible rows for title/body edit tests before archive/delete cleanup, so the seeded message count remains stable. Slice 156 patient-message reply readiness uses the same anchor and temporary pnotes-compatible rows for reply-append tests before archive/delete cleanup, so the seeded message count remains stable. Slice 157 patient-message portal metadata readiness updates the permanent seeded message projection with deterministic `portal:{canonicalId}` relations on seeded portal messages and plaintext encryption flags, but it does not change the seeded 1,200-message count. Slice 204 reuses the same portal-enabled `MOD-PAT-0004` anchor for account readiness: the seeded CMS login, portal username, and login username are `mod-pat-0004@example.test`, the seeded password status is `1` / patient-managed, and there is no pending one-time reset token. Slice 205 also reuses `MOD-PAT-0004`, temporarily issues `reset-mod-pat-0004`-style one-time reset state with password status `0`, verifies the reset labels, then clears the reset back to password status `1` without changing permanent seed counts. Slice 158 patient-message update metadata readiness uses the same anchor and temporary pnotes-compatible rows to verify blank create-time update metadata and edit-time `update_by`/`update_date` stamping before archive/delete cleanup, so the seeded message count remains stable. Slice 159 admin login readiness adds one modernized-only `auth_accounts` seed row for local demo credential verification (`admin` with a deterministic salted hash for password `pass`) and does not alter canonical patient, staff, or message counts. Slice 160 admin login audit readiness adds an empty modernized `auth_audit_events` table that is populated only by actual login attempts during smoke/parity runs; those audit rows are operational evidence and do not change canonical patient, staff, or message counts. Slice 161 admin session readiness adds an empty modernized `auth_sessions` table that is populated only by successful login attempts during smoke/parity runs; session rows are operational evidence and do not change canonical patient, staff, or message counts. Slice 162 admin audit protection readiness reuses `auth_accounts`, `auth_audit_events`, and `auth_sessions` and adds no permanent seed rows; it verifies protected access behavior around operational audit evidence. Slice 163 admin directory protection readiness reuses the same admin auth seed tables and adds no permanent seed rows; it verifies protected access behavior around the seeded administration directory and existing admin/ACL mutation anchors. Slice 164 operational reports protection readiness reuses the same admin auth seed tables and existing operational report data without adding permanent seed rows; it verifies protected access behavior around aggregate report counts and CSV export rows. Slice 165 patient chart protection readiness reuses the admin auth seed tables plus the existing `MOD-PAT-0001` patient anchor to verify legacy patient-summary no-session blocking, modernized `/api/patients` no-session `401` behavior, active-session patient search/chart retrieval, and Patient/Client sign-in gating. Slice 166 clinical-list protection readiness reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` clinical-list anchors to verify legacy patient-summary list no-session blocking, modernized `/api/clinical-lists` no-session `401` behavior, active-session list retrieval and mutations, and Lists workspace sign-in gating. Slice 167 appointment protection readiness reuses the same admin auth seed tables plus the existing `MOD-PAT-0003` scheduling anchors to verify legacy scheduler-page no-session blocking, modernized `/api/appointments` no-session `401` behavior, active-session appointment search/detail retrieval, authenticated appointment mutations, and Calendar workspace sign-in gating. Slice 168 encounter protection readiness reuses the same admin auth seed tables plus `MOD-PAT-0001` encounter `1000013` to verify legacy encounter-page no-session blocking, modernized `/api/encounters` no-session `401` behavior, active-session encounter search/detail retrieval, authenticated encounter mutations, and Encounters workspace sign-in gating. Slice 169 document protection readiness reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` patient document anchors to verify legacy document-page no-session blocking, modernized `/api/documents` no-session `401` behavior for list/content/create, active-session document retrieval, and Documents workspace sign-in gating. Slice 170 message protection readiness reuses the same admin auth seed tables plus the existing `MOD-PAT-0004` patient-message anchors to verify legacy patient-notes no-session blocking, modernized `/api/messages` no-session `401` behavior for list/create, active-session message retrieval, authenticated message mutations, and Messages workspace sign-in gating. Slice 171 billing protection readiness reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` fee-sheet and revenue-cycle anchors to verify legacy fee-sheet no-session blocking, modernized `/api/billing` no-session `401` behavior, active-session billing retrieval, authenticated billing mutations, and Fees workspace sign-in gating. Slice 172 procedure protection readiness reuses the same admin auth seed tables plus the existing `MOD-PAT-0009` procedure-result anchors, `MOD-PAT-0701` scheduled-order anchors, and seeded order catalog/provider rows to verify legacy procedure result no-session blocking, modernized `/api/procedures` no-session `401` behavior, active-session procedure retrieval, authenticated procedure mutations, and Procedures workspace sign-in gating. Slice 173 administration authorization-policy readiness adds one modernized-only `auth_accounts` seed row for front-desk demo credential verification (`gold-frontdesk-01` with password `pass`, display name `Parker Fleming`, role `frontdesk`, and staff id `117`) and does not alter canonical patient, staff, or clinical counts; it verifies that the authenticated front-desk session receives `403` for administration API access while `admin` remains authorized. Slice 174 operational reports authorization-policy readiness adds no permanent seed rows; it reuses the same admin/front-desk auth accounts, seeded ACL matrix, and existing operational report data to verify that front desk receives `403` for reports JSON and CSV export while `admin` remains authorized through the OpenEMR return-value hierarchy. Slice 175 clinical-list authorization-policy readiness adds no permanent canonical patient, staff, or clinical rows; it reuses the front-desk account and proves Medical/History denial for that session. Slice 176 appointment authorization-policy readiness adds one modernized ACL membership row mapping `gold-frontdesk-01` to the seeded Front Office group so appointment authorization can prove positive front-desk schedule access through the Appointment ACL. Slice 177 encounter authorization-policy readiness adds no permanent seed rows; it reuses the same admin/front-desk auth accounts, ACL matrix, and `MOD-PAT-0001` encounter `1000013` anchor to prove front-desk `403` behavior and admin encounter access. Slice 178 patient chart authorization-policy readiness adds no permanent seed rows; it reuses the same admin/front-desk auth accounts, ACL matrix, Front Office membership, and `MOD-PAT-0001` patient anchor to prove Demographics access for both admin and front desk while preserving no-session `401` behavior. Slice 179 patient document authorization-policy readiness adds no permanent seed rows; it reuses the same admin/front-desk auth accounts, ACL matrix, Front Office membership, and `MOD-PAT-0001` patient document anchors to prove front-desk `403` behavior and admin Documents access. Slice 180 patient message authorization-policy readiness adds no permanent seed rows; it reuses the same admin/front-desk auth accounts, ACL matrix, Front Office membership, and `MOD-PAT-0004` patient-message anchors to prove front-desk `403` behavior and admin Patient Notes access. Slice 181 billing authorization-policy readiness adds no permanent seed rows; it reuses the same front-desk/admin auth accounts, ACL matrix, Front Office membership, existing `MOD-PAT-0001` billing anchors, statement batch data, and revenue-cycle rows to prove front-desk Billing denial and admin Billing access. Slice 182 procedure authorization-policy readiness adds no permanent seed rows; it reuses the same front-desk/admin auth accounts, ACL matrix, Front Office membership, existing `MOD-PAT-0009` procedure-result anchors, `MOD-PAT-0701` scheduled-order anchors, and seeded order catalog/provider rows to prove front-desk Lab Results denial and admin procedure access. These admin-auth rows support parity readiness and smoke checks only; ASP.NET Core Identity, MFA, password reset/rotation, production policy enforcement beyond the focused administration, operational-report, patient, clinical-list, appointment, encounter, document, message, billing, and procedure ACL gates, broad audit coverage, and audit export remain separate future work. Slice 67 encounter document attachment readiness reuses the existing `MOD-PAT-0001` encounter `1000013` plus its two seeded active linked documents, so no permanent seed counts change. Slice 68 encounter billing linkage readiness also reuses `MOD-PAT-0001` encounter `1000013` plus its two active CPT4 billing rows, so no permanent seed counts change. Slice 69 encounter claim linkage readiness reuses the same encounter plus existing claim row `CLAIM-1000013-1`, so no permanent seed counts change. Slice 70 encounter procedure order linkage readiness reuses the existing `MOD-PAT-0001` encounter `1000011`, procedure order `5000001`, report `6000001`, and four seeded lab result rows, so no permanent seed counts change.

Slice 165 patient chart protection readiness also does not add permanent gold records. It reuses the admin auth seed tables plus the existing `MOD-PAT-0001` patient anchor to verify legacy patient-summary no-session blocking, modernized `/api/patients` no-session `401` behavior, active-session patient search/chart retrieval, and Patient/Client sign-in gating. Slice 166 clinical-list protection readiness also does not add permanent gold records. It reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` problem/allergy/medication/prescription/immunization anchors to verify legacy patient-summary list no-session blocking, modernized `/api/clinical-lists` no-session `401` behavior, active-session list retrieval and list mutations, and Lists workspace sign-in gating. Slice 167 appointment protection readiness also does not add permanent gold records. It reuses the same admin auth seed tables plus the existing `MOD-PAT-0003` scheduling anchors to verify legacy scheduler-page no-session blocking, modernized `/api/appointments` no-session `401` behavior, active-session appointment search/detail retrieval, authenticated schedule mutations, and Calendar workspace sign-in gating. Slice 168 encounter protection readiness also does not add permanent gold records. It reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` encounter `1000013` anchor to verify legacy encounter-page no-session blocking, modernized `/api/encounters` no-session `401` behavior, active-session encounter search/detail retrieval, authenticated encounter mutations, and Encounters workspace sign-in gating. Slice 169 document protection readiness also does not add permanent gold records. It reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` patient document anchors to verify legacy document-page no-session blocking, modernized `/api/documents` no-session `401` behavior, active-session document retrieval, authenticated document mutations, and Documents workspace sign-in gating. Slice 170 message protection readiness also does not add permanent gold records. It reuses the same admin auth seed tables plus the existing `MOD-PAT-0004` patient-message anchors to verify legacy patient-notes no-session blocking, modernized `/api/messages` no-session `401` behavior, active-session message retrieval, authenticated message mutations, and Messages workspace sign-in gating. Slice 171 billing protection readiness also does not add permanent gold records. It reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` billing anchors and established statement/collections data to prove no-session blocking plus active-session access. Slice 172 procedure protection readiness also does not add permanent gold records. It reuses the same admin auth seed tables plus `MOD-PAT-0009` procedure-result anchors, `MOD-PAT-0701` scheduled-order anchors, and existing procedure catalog/provider rows to prove no-session blocking plus active-session access. Slice 173 administration authorization-policy readiness adds one modernized-only front-desk auth account for non-admin authorization evidence without changing canonical patient, staff, or clinical counts. Slice 174 operational reports authorization-policy readiness adds no permanent gold records; it reuses the front-desk account, admin account, ACL rows, and seeded report aggregates to prove reports API/CSV `403` behavior for front desk and admin authorization via the return-value hierarchy. Slice 175 clinical-list authorization-policy readiness adds no permanent canonical gold records and proves Medical/History denial for the same front-desk account. Slice 176 appointment authorization-policy readiness adds one modernized access-user-membership row tying `gold-frontdesk-01` to Front Office, increasing the modernized ACL membership seed count from 2 to 3 while leaving canonical patient/staff/clinical counts unchanged. Slice 177 encounter authorization-policy readiness adds no permanent seed rows; it reuses the front-desk account, admin account, ACL matrix, and existing encounter anchor to prove Authorize Any Encounter denial for front desk and admin authorization through the return-value hierarchy. Slice 178 patient chart authorization-policy readiness adds no permanent seed rows; it reuses the front-desk account, admin account, ACL matrix, Front Office membership, and existing `MOD-PAT-0001` chart anchor to prove Demographics access for both admin and front desk. Slice 179 patient document authorization-policy readiness adds no permanent seed rows; it reuses the front-desk account, admin account, ACL matrix, Front Office membership, and existing `MOD-PAT-0001` document anchors to prove front-desk Documents denial and admin Documents access. Slice 180 patient message authorization-policy readiness adds no permanent seed rows; it reuses the same front-desk account, admin account, ACL matrix, Front Office membership, and existing `MOD-PAT-0004` message anchors to prove front-desk Patient Notes denial and admin Patient Notes access. Slice 181 billing authorization-policy readiness adds no permanent seed rows; it reuses the same front-desk account, admin account, ACL matrix, Front Office membership, and existing `MOD-PAT-0001` billing anchors plus statement batch and revenue-cycle data to prove front-desk Billing denial and admin Billing access. Slice 182 procedure authorization-policy readiness adds no permanent seed rows; it reuses the same front-desk account, admin account, ACL matrix, Front Office membership, existing `MOD-PAT-0009` procedure-result anchors, `MOD-PAT-0701` scheduled-order anchors, and existing procedure catalog/provider rows to prove front-desk Lab Results denial and admin procedure access. Slice 183 procedure mutation authorization-policy readiness adds one modernized-only provider auth account (`gold-provider-01`, password `pass`, display name `Alex Walker`, role `provider`, staff id `101`) and one Clinicians ACL membership row, increasing the modernized ACL membership seed count from 3 to 4 while leaving canonical patient/staff/clinical counts unchanged; it reuses `MOD-PAT-0009` procedure anchors to prove clinician add-only create access and admin-only write/sign/delete behavior. Slice 184 patient mutation authorization-policy readiness adds no permanent seed rows; it reuses `gold-provider-01`, the Clinicians ACL membership, the existing Demographics add-only ACL matrix, and `MOD-PAT-0010` patient anchors to prove clinician read/register access, write-denial behavior, and admin cleanup of a temporary patient. Slice 185 encounter mutation authorization-policy readiness adds no permanent seed rows; it reuses `gold-provider-01`, the Clinicians ACL membership, the existing encounter ACL matrix, and the `MOD-PAT-0001` encounter anchor, temporarily grants Clinicians `encounters:auth_a view` during the test, proves clinician encounter read access plus write-level mutation denial, and revokes that temporary grant after the run. Slice 186 patient document mutation authorization-policy readiness adds no permanent seed rows; it reuses `gold-provider-01`, the Clinicians ACL membership, the existing `patients:docs addonly` ACL matrix, and the existing `MOD-PAT-0001` document anchor, creates one temporary document during the test, proves clinician document read/create access plus write/delete denial, and has admin hard-delete the temporary document for cleanup. Slice 187 patient message mutation authorization-policy readiness adds no permanent seed rows; it reuses `gold-provider-01`, the Clinicians ACL membership, the existing `patients:notes addonly` ACL matrix, and the existing `MOD-PAT-0004` message anchors, creates one temporary message during the test, proves clinician message read/create access plus write-level mutation denial, and has admin hard-delete the temporary message for cleanup. Slice 188 billing mutation authorization-policy readiness adds no permanent seed rows; it reuses `gold-provider-01`, the Clinicians ACL membership, the existing `acct:bill` ACL matrix, and the existing `MOD-PAT-0005` revenue-cycle anchors, temporarily grants Clinicians `acct:bill view` during the test, proves clinician billing read access plus write-level mutation denial, and revokes the temporary grant after the run. Slice 189 appointment mutation authorization-policy readiness adds no permanent seed rows; it reuses `gold-provider-01`, the Clinicians ACL membership, the existing `patients:appt write` ACL matrix, and the existing `MOD-PAT-0003` scheduling anchors, temporarily downgrades Clinicians to `patients:appt view` during the test, proves clinician appointment read access plus write-level mutation denial, and restores the permanent write grant after the run. Broader authorization policy depth remains future protected-access scope.

Current-year temporal coverage verified in the legacy MariaDB baseline on 2026-06-18:

- Appointments: 2,800 in 2026; 1,261 future-dated after 2026-06-18; latest date 2026-12-31.
- Prescriptions: 2,200 in 2026; 1,175 future-starting after 2026-06-18; date range 2026-01-01 to 2026-12-31.
- Medication list entries: 2,200 in 2026; 1,175 future-starting after 2026-06-18; date range 2026-01-01 to 2026-12-31.
- Procedure orders: 1,000 in 2026; 300 future scheduled orders after 2026-06-18; latest date 2026-12-31.
- Procedure results: 2,400 completed results in 2026; date range 2026-02-20 to 2026-05-20.
- Messages: 1,200 in 2026.
- Portal mailbox messages: 400 in 2026, projected into legacy `onsite_mail` for patient portal inbox parity.
- Billing line items: 1,650 in 2026.

Future scheduled procedure orders intentionally do not have final result rows. This gives tests a clean distinction between completed-results workflows and pending/scheduled procedure-order workflows. `legacy-openemr/scripts/Seed-LegacyGoldDataset.ps1` verifies both row counts and the generated `temporalCoverage` contract after each legacy reseed.

## Findings

OpenEMR does provide some sample and demo-data mechanisms, but there does not appear to be a single maintained, production-release test database that is suitable as our full modernization parity baseline.

Useful upstream sources:

- OpenEMR ships `sql/example_patient_data.sql` in the `v8_1_0` source tag. It inserts 14 rows into `patient_data`.
- OpenEMR ships `sql/example_patient_users.sql` in the `v8_1_0` source tag. It inserts 2 provider users referenced by the example patients.
- The public OpenEMR demo site has demo users and limited demo data, and the site is reset daily. This is useful for exploration, not for a deterministic local test baseline.
- The OpenEMR development Docker environment documents `dev-reset-install-demodata`, which resets and installs OpenEMR with demo data.
- The OpenEMR development Docker environment documents `import-random-patients`, which uses Synthea to create random patients and import them into OpenEMR.
- The `openemr/demo-data-generator` repository exists, but its README describes it as a work in progress and currently focused on patient and facility data.

Relevant upstream references:

- `https://github.com/openemr/openemr/blob/v8_1_0/sql/example_patient_data.sql`
- `https://github.com/openemr/openemr/blob/v8_1_0/sql/example_patient_users.sql`
- `https://www.open-emr.org/demo/`
- `https://github.com/openemr/demo-data-generator`
- `legacy-openemr/source/CONTRIBUTING.md`

## Decision

Use a layered seed-data strategy.

1. Keep the bundled OpenEMR example patient SQL as the small starter seed for quick baseline checks.
2. Treat `openemr-shared-synthetic-v1` as the project gold test dataset for meaningful workflow and parity tests.
3. Keep all seed data non-PHI, deterministic, resettable, and versioned.
4. Prefer seeding through repeatable scripts and documented import flows exposed through Workbench-managed actions.
5. Extend the dataset workflow by workflow as modernization slices are selected.

The project-owned seed data becomes part of the test contract. The same initial dataset should be usable by:

- Legacy OpenEMR tests.
- Modernized OpenEMR tests.
- Workbench status and comparison views.
- Future CI automation.

The canonical dataset folder is `modernization-workbench/seed-data/`. Target-specific seeders should adapt that canonical dataset into each database:

- Legacy OpenEMR MariaDB through scripts under `legacy-openemr/scripts/`.
- Future modernized OpenEMR PostgreSQL through scripts or services owned by the modernized target.

The Workbench should manage both paths from the same seed manifest so the operator can apply the same dataset to both systems before parity tests.

## Recommended Seed Levels

### Level 0: Installation Smoke Data

Purpose: prove the app is installed, healthy, and login works.

Current status: implemented through the baseline smoke test.

### Level 1: Starter Patient Data

Purpose: make the UI visibly useful and enable patient search/navigation tests.

Initial candidate:

- Import `legacy-openemr/source/sql/example_patient_users.sql`.
- Import `legacy-openemr/source/sql/example_patient_data.sql`.

Expected effect:

- Add 2 provider users.
- Add 14 patient demographic records.
- Remap the bundled sample provider references to the provider user IDs created in this local baseline.

Limitations:

- Mostly demographics only.
- Not enough for appointment, encounter, billing, message, portal, or clinical-note parity tests.

### Level 2: Gold Synthetic Workflow Data

Purpose: support real modernization parity tests.

Current status: implemented as `openemr-shared-synthetic-v1` and verified against the legacy MariaDB baseline and the modernized PostgreSQL seed through the implemented read-only slices plus patient contact, patient demographics, patient registration, patient insurance, patient portal account/reset/access/authentication/session/home/clinical-summary/lab-results/medical-report/generated-report/generated-report PDF/generated-report issue-selection/generated-report encounter-form selection/generated-report printable template/secure-message inbox/compose/reply/thread/archive readiness, appointment, appointment reschedule, appointment arrival, appointment check-out, appointment no-show, appointment category, appointment pending-status, encounter, encounter billing linkage, encounter diagnosis coding, encounter fee-sheet entry, encounter procedure-order entry, encounter procedure-result entry, encounter sign-off, encounter document upload, encounter binary document upload, encounter scanned attachment readiness, encounter document sign-off, encounter document denial, encounter document metadata refiling, encounter document movement, encounter document content replacement, encounter document replacement revision, encounter document archive/restore, encounter document lifecycle timeline, clinical-list allergy, problem-list, medication-list, patient-message, patient-message content, patient-message assignment, patient-message reply, patient-message portal metadata, patient-message update metadata, admin login readiness, admin login audit readiness, admin session readiness, admin audit protection readiness, admin directory protection readiness, operational reports protection readiness, patient chart protection readiness, clinical-list protection readiness, text patient-document, binary patient-document, image patient-document preview, image patient-document thumbnail, patient-document lifecycle timeline, patient-document scanned attachment, patient-document sign-off, patient-document denial, patient-document metadata refiling, patient-document archive restore, patient-document content replacement, patient-document binary content replacement, patient-document replacement revision, external-link patient-document, prescription, immunization, billing, billing diagnosis, billing correction, billing modifier, payment posting, claim status, procedure, administration facility, administration user, access-permission, and user group membership mutation slices, including the read-only default ACL access-control matrix slice, the read-only pending/scheduled procedure-order slice, the read-only patient documents slice, the read-only patient document preview readiness slice, the read-only patient document revision readiness slice, the read-only claim status slice, the read-only payment posting slice, the read-only account balance slice, the read-only account aging slice, the read-only account ledger slice, the read-only account statement readiness slice, the read-only patient statement generation slice, the read-only statement PDF export slice, the read-only statement batch candidate slice, the read-only statement batch package export slice, the read-only collections work queue slice, the read-only encounter document attachment slice, the read-only encounter billing linkage slice, the read-only encounter claim linkage slice, the read-only encounter procedure order linkage slice, and the read-only encounter diagnosis coding slice.

Slice 237 extends the current gold-data status with patient portal generated medical-report procedure-order artifact readiness. It creates cleanup-backed temporary procedure orders for the existing `MOD-PAT-0004` portal account and latest encounter, selects those orders in generated PDF and ZIP package requests, verifies modernized PDF/package delivery content plus legacy printable/PDF source artifacts, and deletes the temporary order tree so no permanent portal, lab, encounter, or procedure-order seed rows change. Slice 236 remains the patient portal generated medical-report procedure-order selection readiness slice and creates cleanup-backed temporary procedure orders for the same account to verify generated-report rendering. Slice 235 remains the patient portal secure-message Deleted-folder readiness slice and creates cleanup-backed temporary inbox and sent rows for the same account, archives them into transient deleted mailbox rows, verifies active folder counts return to baseline while Deleted-folder counts increase, and does not change the permanent 400-row portal mailbox projection. Slice 234 remains the patient portal secure-message forward-to-practice readiness slice and creates cleanup-backed temporary inbox rows plus transient practice-side patient-message rows for the same account.

Slice 219 extends the current gold-data status with patient portal appointment-list parity. It reuses `MOD-PAT-0004` appointment anchors in the permanent 2,800-appointment baseline and validates one upcoming plus two past signed-in portal appointments without changing seed counts. Slice 218 remains the patient portal document list/download parity slice.

Slice 166 extends the current gold-data status with protected clinical-list access parity. It validates the same seeded problem, allergy, medication-list, prescription, and immunization anchors under an authenticated session and verifies that no list data is exposed without that session. Slice 165 remains available for protected patient chart access parity over the same seeded patient search/chart data.

This dataset includes named synthetic personas and workflow data for:

- Patient search and patient chart navigation.
- Patient creation and demographic editing.
- Appointments.
- Seeded OpenEMR appointment categories `9` Established Patient, `10` New Patient, and `13` Preventive Care Services.
- Appointment comments, regular recurrence metadata anchors, generated recurring-series read expectations, and seeded recurrence exception-date expectations.
- Encounters.
- Vitals.
- Notes.
- Messages.
- Problems, allergies, medications, and prescriptions where appropriate.
- 2026 current-year and future-dated prescription coverage.
- Billing line-item, diagnosis coding, charge correction, and modifier coverage.
- 2026 current-year and future scheduled procedure-order coverage.
- Lab order/report lifecycle fields for priority, procedure type, instructions, review status, and report notes.
- Patient document metadata, OpenEMR document categories, encounter links, deterministic text payload previews, normalized pending review state, and deleted-state archive/restore behavior.
- Claim status, payment posting, account balance, account aging, account ledger, account statement readiness, patient statement generation, statement PDF export, statement batch candidate, statement batch package export, and collections work queue coverage, with deeper revenue-cycle behavior later once the project selects those workflows.
- Encounter billing linkage mutation coverage that proves temporary fee-sheet rows appear through encounter-linked billing and diagnosis surfaces and disappear after billed/inactive cleanup.
- Encounter fee-sheet entry mutation coverage that proves temporary CPT and ICD10 rows can be added from the modernized Encounter workspace and cleaned up without changing permanent seed counts.
- Patient document preview, lifecycle, scanned attachment, and revision readiness coverage, with deeper OCR extraction, scanner-device ingestion, external storage adapters, and true multi-version history deferred to future document slices.
- Portal-enabled patient coverage.
- Portal mailbox selection coverage for compose, reply, thread view, single archive, read status, selected-message batch archive, and active All-folder parity without changing permanent mailbox counts.

Each workflow slice should define:

- Seed records.
- Setup script.
- Verification query.
- Legacy UI/API tests.
- Expected normalized output for modernized parity tests.

Stable tests should reference canonical patient identifiers such as `MOD-PAT-0001`, not legacy database auto-increment IDs.

Patient payment capture tests use `MOD-PAT-0005` and encounter `1000052` as the stable revenue-cycle anchor. The workflow creates only temporary `payer_id = 0` / `payer_type = 0` patient-payment AR rows, so the canonical seeded claim, billing, payment session, and payment activity counts are unchanged after cleanup.

Encounter billing linkage mutation tests use `MOD-PAT-0001` and encounter `1000013` as the stable clinical billing anchor. The workflow creates only a temporary `CPT4 99499` fee-sheet row, so the canonical seeded billing rows for the encounter are unchanged after cleanup.

Encounter diagnosis coding mutation tests use `MOD-PAT-0001` and encounter `1000013` as the stable clinical diagnosis anchor. The workflow creates only a temporary `ICD10 R73.03` fee-sheet diagnosis row, so the canonical seeded billing rows and diagnosis evidence for the encounter are unchanged after cleanup.

Encounter fee-sheet entry tests use `MOD-PAT-0001` and encounter `1000013` as the stable encounter coding anchor. The workflow creates only temporary `CPT4 99499` and `ICD10 R73.03` rows, so the canonical seeded billing rows and diagnosis evidence for the encounter are unchanged after cleanup.

The first modernized seed adapter lives in `modernized-openemr/scripts/Seed-ModernizedGoldDataset.ps1`. It consumes the canonical gold dataset, generates a PostgreSQL seed script under ignored artifacts, and loads the same patient and workflow records into modernized tables for patient search/chart summary behavior, patient demographics mutation behavior, patient registration lifecycle behavior, patient insurance coverage behavior, patient insurance mutation behavior, patient contact mutation behavior, appointment mutation behavior, appointment reschedule behavior, appointment arrival behavior, appointment check-out behavior, appointment no-show behavior, appointment category behavior, appointment pending-status behavior, appointment provider reassignment behavior, appointment facility reassignment behavior, appointment billing-location reassignment behavior, appointment comments behavior, appointment recurrence metadata behavior, appointment recurring-series expansion, appointment recurrence-exception expansion, appointment occurrence-cancel/restore/reschedule behavior, appointment recurrence exception-list edit behavior, appointment series root update behavior, appointment series root metadata behavior, appointment monthly recurrence behavior, appointment recurrence unit matrix behavior, appointment days-of-week recurrence behavior, appointment monthly repeat-on recurrence behavior, appointment series recurrence update behavior, encounter mutation behavior, encounter metadata mutation behavior, encounter billing linkage mutation behavior, encounter diagnosis coding mutation behavior, encounter fee-sheet entry behavior, encounter procedure-order entry behavior, encounter document sign-off behavior, encounter document denial behavior, encounter document metadata refiling behavior, encounter document move behavior, encounter document content replacement behavior, encounter document replacement revision behavior, encounter document archive/restore behavior, clinical-list allergy mutation behavior, problem-list mutation behavior, medication-list mutation behavior, patient-message mutation behavior, patient-message content edit behavior, patient-message assignment behavior, patient-message reply behavior, patient-message portal metadata behavior, patient-message update metadata behavior, patient-message mutation authorization-policy behavior, admin login readiness behavior, admin login audit readiness behavior, admin session readiness behavior, admin audit protection behavior, admin directory protection behavior, operational reports protection behavior, patient chart protection behavior, clinical-list protection behavior, collections follow-up task behavior, text patient-document mutation behavior, binary patient-document mutation behavior, image patient-document preview behavior, image patient-document thumbnail behavior, patient-document sign-off behavior, patient-document denial behavior, patient-document metadata refiling behavior, patient-document archive restore behavior, patient-document content replacement behavior, patient-document binary content replacement behavior, patient-document replacement revision behavior, external-link patient-document behavior, prescription mutation behavior, immunization mutation behavior, billing mutation behavior, billing diagnosis mutation behavior, billing correction mutation behavior, billing modifier mutation behavior, payment posting mutation behavior, claim status mutation behavior, procedure mutation behavior, procedure result correction behavior, procedure specimen metadata behavior, procedure specimen detail behavior, procedure order correction behavior, procedure report correction behavior, procedure report sign-off behavior, procedure report review queue behavior, procedure report review queue filter behavior, procedure report review queue provider filter behavior, procedure report review queue lab filter behavior, procedure lab provider catalog behavior, procedure lab provider directory behavior, procedure lab provider lifecycle behavior, procedure lab provider configuration behavior, procedure lab provider address-book behavior, procedure order catalog behavior, procedure order catalog lifecycle behavior, administration facility mutation behavior, administration user mutation behavior, access-permission mutation behavior, user group membership mutation behavior, read-only scheduling behavior, read-only encounter SOAP/vitals behavior, read-only encounter document attachment behavior, read-only encounter billing linkage behavior, read-only encounter claim linkage behavior, read-only encounter procedure order linkage behavior, read-only encounter diagnosis coding behavior, read-only clinical-list behavior, read-only messaging behavior, read-only procedure-result behavior, read-only pending/scheduled procedure-order behavior, read-only fee-sheet billing behavior, read-only claim status behavior, read-only payment posting behavior, read-only account balance behavior, read-only account aging behavior, read-only account ledger behavior, read-only account statement readiness behavior, read-only patient statement generation behavior, read-only statement PDF export behavior, read-only statement batch candidate behavior, read-only statement batch package export behavior, read-only collections work queue behavior, read-only administration directory behavior, read-only access-control behavior, read-only operational reporting behavior, operational reports export behavior, read-only patient document behavior, patient document content retrieval behavior, patient document preview readiness behavior, patient document lifecycle timeline behavior, patient document scanned attachment readiness behavior, patient document revision readiness behavior, patient portal report audit behavior, patient portal message audit behavior, read-only immunization history behavior, and normalized database parity checks. Later slice notes below record the temporary mutation anchors and cleanup expectations that keep the permanent gold-data counts stable.

Slice 166 adds protected clinical-list access to that adapter contract without changing the generated patient or clinical-list rows. Slice 165 added protected patient chart access without changing generated patient rows. The modernized seed-count report also includes `labProviders` so the database contract remains aligned with the current shared summary manifest.

Slice 78 reuses the existing patient-document schema and encounter document link fields. The seed adapter does not add permanent rows; parity and smoke checks create temporary encounter-linked text documents and hard-delete them after validating attached-document rendering. Slice 79 also reuses the existing patient-document schema, binary content bytes, MIME/download metadata, and encounter link fields; parity and smoke checks create temporary encounter-linked PDF/binary documents and hard-delete them after validating preview, rendering, and download content. Slice 80 reuses the same patient-document review fields introduced for patient document sign-off; parity and smoke checks create temporary encounter-linked text documents, sign them as `admin`, verify approved review metadata, and hard-delete them so seeded review state remains unchanged. Slice 81 reuses those same review fields for encounter-scoped denial; parity and smoke checks create temporary encounter-linked text documents, deny them as `admin`, verify denied review metadata, and hard-delete them so seeded review state remains unchanged. Slice 82 reuses the patient-document metadata fields for encounter-scoped refiling; parity and smoke checks create temporary encounter-linked text documents, update name, category, date, and notes while preserving the encounter link, verify metadata rendering, and hard-delete them so seeded document state remains unchanged. Slice 83 reuses the same encounter link metadata; parity and smoke checks create temporary encounter-linked text documents, move them to another same-patient encounter, verify source and target attachment counts, and hard-delete them so seeded document state remains unchanged. Slice 84 reuses the same patient-document content, hash, revision, preview, and encounter-link fields; parity and smoke checks create temporary encounter-linked text documents, replace text content in place, verify replacement preview and current-version facts, and hard-delete them so seeded document state remains unchanged. Slice 85 reuses the same patient-document deleted state and encounter-link fields; parity and smoke checks create temporary encounter-linked text documents, archive them, verify active-detail hiding plus archived-detail visibility, restore them, verify active rendering, and hard-delete them so seeded document state remains unchanged. Slice 86 reuses the same patient-document upload, revision, review, deleted-state, and encounter-link fields; parity and smoke checks create temporary encounter-linked text documents, verify derived lifecycle events across pending, approved, archived, and restored states, render those lifecycle events in the modernized Encounter UI, and hard-delete the documents so seeded document state remains unchanged. Slice 87 reuses the same patient-document URL, MIME, storage-method, deleted-state, and encounter-link fields; parity and smoke checks create temporary encounter-linked web URL documents, verify `text/uri-list`, `web_url`, URL preview/rendering, archive behavior, and hard-delete the documents so seeded document state remains unchanged. Slice 88 reuses the same patient-document binary content, MIME, preview, deleted-state, and download fields; parity and smoke checks create temporary patient `image/svg+xml` documents, verify inline image preview metadata, browser-visible modernized image rendering, byte-preserving download behavior, archive behavior, and hard-delete the documents so seeded document state remains unchanged. Slice 89 reuses those same temporary image documents and stored bytes; parity and smoke checks verify normalized `thumbnailDataUri` facts and modernized document-card thumbnail rendering, then archive and hard-delete the documents so seeded document state remains unchanged. Slice 90 reuses the existing patient-document binary content, MIME, preview, deleted-state, and download fields; parity and smoke checks create temporary patient `application/pdf` documents, verify inline PDF preview metadata, browser-visible modernized iframe rendering, byte-preserving download behavior, archive behavior, and hard-delete the documents so seeded document state remains unchanged. Slice 91 reuses the same patient-document upload, revision, review, deleted-state, and patient-scope fields; parity and smoke checks create temporary patient text documents, verify filed/current-version/review-pending/active lifecycle events, sign them as `admin`, verify review-approved events, archive and restore them to verify archived/active events, render those events in the modernized Documents card and viewer, and hard-delete the documents so seeded document state remains unchanged. Slice 92 reuses the same patient-document binary content, MIME, notes, preview, deleted-state, and patient-scope fields; parity and smoke checks create temporary scanned PDF documents, derive scan status, capture source, scanned page count, and OCR status from notes/metadata, render those facts in the modernized Documents card and viewer, archive the document, and hard-delete it so seeded document state remains unchanged. Slice 93 reuses the existing scheduling seed anchor and appointment table shape; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, reschedule its title/date/time/duration/status/room, render the changed appointment, and hard-delete it so seeded appointment counts remain unchanged. Slice 94 reuses the same scheduling anchor and appointment table shape; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, mark it arrived with status `@`, render the arrived appointment, and hard-delete it so seeded appointment counts remain unchanged. Slice 95 reuses the same scheduling anchor and appointment table shape; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, mark it arrived with status `@`, check it out with status `>`, render the checked-out appointment, and hard-delete it so seeded appointment counts remain unchanged. Slice 96 reuses the same scheduling anchor and appointment table shape; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, mark it no-show with status `?`, render the no-show appointment, and hard-delete it so seeded appointment counts remain unchanged. Slice 97 reuses the same scheduling anchor, appointment table shape, and seeded OpenEMR appointment categories; parity and smoke checks create a temporary future appointment with category `13` Preventive Care Services, update it to category `10` New Patient, render the category state, and hard-delete it so seeded appointment counts remain unchanged. Slice 98 reuses the same scheduling anchor and appointment table shape; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, update it to pending status `~`, render the pending state, and hard-delete it so seeded appointment counts remain unchanged. Slice 99 reuses the same scheduling anchor, appointment table shape, and seeded provider IDs; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, reassign it from provider `101` to provider `102`, render the provider reassignment, and hard-delete it so seeded appointment counts remain unchanged. Slice 100 reuses the same scheduling anchor, appointment table shape, and seeded facility IDs; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, reassign it from facility `10` to facility `11`, render the facility reassignment, and hard-delete it so seeded appointment counts remain unchanged. Slice 101 extends the modernized appointment table shape with `billing_location_id`; parity and smoke checks create a temporary future appointment for `MOD-PAT-0003`, keep service facility `10`, reassign billing location from `10` to `11`, render both facility facts, and hard-delete it so seeded appointment counts remain unchanged.

Slice 134 extends the modernized seed adapter contract with procedure report sign-off behavior by carrying reviewed metadata into modernized `lab_reports.reviewed_by` and `lab_reports.reviewed_at` for reviewed seed reports while keeping the mutable parity workflow temporary.

Slice 135 reuses those reviewed metadata fields for procedure report review queue coverage. It does not add permanent gold records; the queue-transition parity workflow creates a temporary received report, verifies unreviewed queue membership, signs it, verifies reviewed queue membership, and hard-deletes the temporary order tree and encounter.

Slice 136 reuses the same temporary report pattern for review queue patient/date filter coverage. It does not add permanent gold records; the filter parity workflow creates a temporary received report for `MOD-PAT-0009`, verifies patient and order-date filtered queue inclusion, verifies outside-date exclusion, signs it, verifies filtered reviewed queue membership, and hard-deletes the temporary order tree and encounter.

Slice 137 reuses the same temporary report pattern for review queue provider filter coverage. It does not add permanent gold records; the provider filter parity workflow creates a temporary received report for `MOD-PAT-0009` with a known provider, verifies provider-filtered queue inclusion, verifies outside-provider exclusion, signs it, verifies provider-filtered reviewed queue membership, and hard-deletes the temporary order tree and encounter.

Slice 138 extends the modernized procedure schema with `lab_orders.lab_id` and an empty `lab_providers` table so the temporary workflow can model legacy `procedure_order.lab_id` / `procedure_providers` lab ownership. It does not add permanent lab-provider gold records; the lab filter parity workflow creates matching and outside temporary lab providers, assigns the temporary report to the matching lab, verifies lab-filtered queue inclusion and outside-lab exclusion, signs it, verifies lab-filtered reviewed queue membership, and hard-deletes the temporary order tree, encounter, and lab-provider rows.

Slice 139 promotes lab providers into the permanent shared gold dataset. The generator creates five active lab providers with stable IDs `501` through `505`, assigns all 1,000 seeded lab orders to those providers through legacy `procedure_order.lab_id` and modernized `lab_orders.lab_id`, inserts the legacy catalog into `procedure_providers`, and imports the modernized catalog into `lab_providers`. The stable catalog anchor is `MOD-PAT-0009`, order `5000009`, report `6000009`, lab `504` (`Pacific Women's Health Laboratory`), and order date `2026-02-26`.

Slice 140 uses that same permanent catalog as the provider-directory contract. The balanced gold-data distribution gives each provider exactly 200 lab orders, 140 completed/reviewed reports, and 60 future scheduled orders, with no inactive providers in the current catalog. This supports directory tests for active filtering, NPI display, OpenEMR's default `DL` protocol value, and provider workload counts without adding new seed rows.

Slice 141 keeps the permanent five-provider catalog stable and uses temporary lifecycle rows for create/deactivate/include-inactive/delete parity. The modernized PostgreSQL seed now stores `lab_providers.protocol` with default `DL` so lifecycle mutations preserve the same protocol field the legacy `procedure_providers` table exposes. The Slice 141 workflow cleans up its temporary provider after each test and should not change the canonical five-provider counts after reset.

Slice 142 keeps the permanent five-provider catalog stable and uses temporary provider rows for configuration parity. The modernized PostgreSQL seed now stores legacy-aligned provider configuration columns for usage, direction, sender/receiver application and facility IDs, remote host, login, password, order/result paths, and notes. Permanent providers seed those optional transport fields as empty strings to match legacy not-set defaults, while the Slice 142 workflow updates a temporary provider to non-empty values and deletes it after verification so canonical provider counts remain unchanged.

Slice 144 keeps the permanent five-provider catalog and the permanent address-book baseline stable. The modernized PostgreSQL seed adds an empty `lab_provider_address_book` table and a nullable `lab_providers.lab_director_id` link to model the legacy relationship between `users` order-service address-book rows and `procedure_providers.lab_director`. The parity workflow creates only temporary `ord_lab` address-book organizations, links a temporary provider to them, verifies provider-name derivation from the organization, then deletes both provider and organizations so canonical provider counts remain unchanged.

Slice 145 adds the permanent procedure order catalog contract. The canonical dataset now includes 21 `procedureOrderCatalog` items: root `9000`, provider groups `9010` through `9050`, and 15 orderable panel rows under the five permanent lab providers. The legacy seed inserts those rows into `procedure_type` IDs `9000` through `9999`; the modernized seed imports the same rows into `lab_order_catalog`. The stable anchor is provider group `9040` for lab `504` (`Pacific Women's Health Laboratory`) with procedure codes `83036`, `80053`, and `85025`. The Slice 145 plan is read-only and should not change any permanent catalog counts.

Slice 147 adds temporary procedure order catalog lifecycle coverage without changing the permanent seed contract. The parity plan creates a temporary orderable row under provider group `9040` / lab `504`, updates code, name, specimen, description, standard code, sequence, and active state, renders the active row through both targets, and deletes it during cleanup. After cleanup the permanent 21-row catalog count remains the expected baseline.

Slice 148 adds temporary vendor compendium import coverage without changing the permanent seed contract. The parity plan creates a temporary catalog group under root group `9000` for lab `504`, imports PathGroup-style order/result CSV rows, re-imports a changed CSV to verify OpenEMR-style deactivate/reactivate behavior, renders imported order rows through both targets, and deletes the temporary catalog subtree during cleanup. After cleanup the permanent 21-row catalog count remains the expected baseline.

Slice 149 adds temporary procedure order queue coverage without changing the permanent seed contract. The parity plan creates a temporary `MOD-PAT-0009` encounter and reportless lab order for lab `504`, verifies ready-to-send queue membership, verifies reported queue exclusion before a report exists, attaches a temporary report, verifies reported queue membership and ready-to-send exclusion, renders both states through legacy `list_reports.php` and the modernized Reports workspace, and deletes the temporary order tree and encounter during cleanup.

Slice 151 adds temporary procedure order transmit coverage without changing the permanent seed contract. The parity plan creates a temporary `MOD-PAT-0009` encounter and reportless lab order for lab `504`, verifies ready-to-send queue membership with no transmitted timestamp, marks the order transmitted at `2026-06-21 10:40:00`, verifies sent-awaiting-results/transmitted-pending queue membership with normalized `2026-06-21 10:40` display state, renders the sent state through legacy `list_reports.php` option `4` and the modernized Reports workspace, and deletes the temporary order tree and encounter during cleanup.

Slice 153 adds temporary procedure report bulk sign-off coverage without changing the permanent seed contract. The parity plan creates one temporary `MOD-PAT-0009` encounter, two complete lab orders dated `2026-06-21`, two unreviewed final reports, verifies unreviewed queue membership, signs both reports as `admin` at `2026-06-21 11:25:00` in one bulk operation, verifies reviewed queue membership with normalized `2026-06-21 11:25` display state, renders the reviewed state through legacy `list_reports.php` option `2` and the modernized Reports workspace, and deletes both temporary order trees plus the encounter during cleanup.

Slice 76 extends the same temporary-procedure pattern into encounter procedure-result entry coverage. The seed adapter does not add permanent rows; the parity and smoke checks create the temporary order/report/result through the workflow/API layer and hard-delete the order after validating cascade cleanup. Slice 77 adds an empty `encounter_signatures` table to the modernized schema for attestation parity. It still does not add permanent signature rows; parity and smoke checks create temporary signatures and delete them after validating the sign-off lifecycle.

The scheduling slice now uses `MOD-PAT-0003` as a stable appointment anchor. Both legacy MariaDB and modernized PostgreSQL probes locate that patient's next future appointment after `2026-06-18`, and the `slice-2-scheduling-readiness` plan verifies the appointment facts plus browser-visible appointment detail behavior against both targets.

The encounter slice now uses `MOD-PAT-0001` as a stable clinical anchor. Both legacy MariaDB and modernized PostgreSQL probes locate that patient's latest encounter in 2026, including SOAP assessment text and vitals blood-pressure data, and the `slice-3-encounters-readiness` plan verifies those facts plus browser-visible encounter detail behavior against both targets.

The clinical-lists slice also uses `MOD-PAT-0001` as a stable clinical-list anchor. Both legacy MariaDB and modernized PostgreSQL probes locate that patient's active problems, allergies, medication list entries, and prescriptions. The current anchor facts include `Type 2 diabetes mellitus without complications`, `Asthma, uncomplicated`, `Penicillin` allergy with rash and moderate severity, `Metformin 500 mg`, `Atorvastatin 20 mg`, `Albuterol inhaler 90 mcg`, and a matching `Metformin` prescription. The `slice-4-clinical-lists-readiness` plan verifies these facts plus browser-visible clinical-list behavior against both targets.

The clinical-list mutation slice uses `MOD-PAT-0006` as a stable allergy-lifecycle anchor. The shared parity workflow creates a temporary `allergy` list entry, verifies direct row state and active UI rendering, sets `activity = 0` with an inactive comment, and then deletes the temporary row so the patient allergy count returns to the seeded baseline. The `slice-13-clinical-list-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The problem-list mutation slice also uses `MOD-PAT-0006` as a stable medical-problem lifecycle anchor. The shared parity workflow creates a temporary `medical_problem` list entry with diagnosis `ICD10:Z00.00`, verifies direct row state and active UI rendering, sets `activity = 0` with an inactive comment, and then deletes the temporary row so the patient problem count returns to the seeded baseline. The `slice-31-problem-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The medication-list mutation slice also uses `MOD-PAT-0006` as a stable medication-list lifecycle anchor. The shared parity workflow creates a temporary `medication` list entry with diagnosis `ICD10:Z00.00`, verifies direct row state and active UI rendering, sets `activity = 0` with an inactive comment, and then deletes the temporary row so the patient medication-list count returns to the seeded baseline. The `slice-32-medication-list-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The messaging slice uses `MOD-PAT-0004` as a stable portal-messaging anchor. Both legacy MariaDB and modernized PostgreSQL probes locate that patient's portal-enabled flag and seeded patient messages. The current anchor facts include portal access enabled, a `Care team follow-up` message with `New` status and body `Follow-up message for Nora Kim.`, and a `Portal message` with `Done` status and body `Patient portal question about medications.` The `slice-5-messaging-readiness` plan verifies these facts plus browser-visible patient-message behavior against both targets.

The message mutation slice also uses `MOD-PAT-0004` as a stable patient-message lifecycle anchor. The shared parity workflow creates a temporary patient message assigned to `admin`, verifies direct row state and browser rendering after it is closed with `Done` status, soft-deletes the row so it no longer appears in active patient-message lists, and then hard-deletes the temporary row so the patient message count returns to the seeded baseline. The `slice-14-message-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The patient-message reply readiness slice uses the same `MOD-PAT-0004` anchor and the same temporary pnotes-compatible row pattern. The shared parity workflow creates a temporary active message assigned to `admin`, appends a timestamped `admin to admin` reply line to the message body, verifies direct row state, count stability, legacy pnotes rendering, and modernized Messages UI rendering, then archives and hard-deletes the temporary row so the patient message count returns to the seeded baseline. The `slice-156-message-reply-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The patient-message portal metadata readiness slice adds permanent metadata fields to the seeded message projection without changing message counts. Seeded `Portal message` rows carry `portal:{canonicalId}` in legacy `pnotes.portal_relation` and modernized `messages.portal_relation`, while `Care team follow-up` rows keep a blank relation. All seeded message rows keep plaintext body content and `is_msg_encrypted` / `is_encrypted = false`. The `slice-157-message-portal-metadata-readiness` plan verifies `MOD-PAT-0004` metadata facts against both databases and verifies the modernized Messages card renders the portal relation and plaintext state.

The patient-message update metadata readiness slice uses the same `MOD-PAT-0004` anchor and temporary pnotes-compatible row pattern. The modernized message schema carries `updated_by` and `updated_at` equivalents to legacy `pnotes.update_by` and `pnotes.update_date`; seeded rows stay blank because they are not mutation artifacts. The `slice-158-message-update-metadata-readiness` plan creates a temporary message, verifies blank update metadata after create, edits the message, verifies `updatedBy = 1` plus a normalized timestamp on both targets, verifies the modernized Messages card renders the update user, and cleans up the temporary row so the 1,200-message baseline remains stable.

The admin login readiness slice seeds a modernized-only `auth_accounts` table with one local demo account, `admin`, whose password `pass` is stored as a deterministic salted SHA-256 hash for repeatable local parity. Slice 173 adds a second modernized-only local demo account, `gold-frontdesk-01`, with the same demo password, display name `Parker Fleming`, role `frontdesk`, and staff id `117` so parity can prove authenticated non-admin authorization boundaries. Slice 176 maps that account into the modernized Front Office ACL membership seed. Slice 183 adds `gold-provider-01` with password `pass`, display name `Alex Walker`, role `provider`, staff id `101`, and Clinicians ACL membership for mutation-policy evidence, increasing the modernized ACL membership seed count to 4. Slice 185 adds no permanent auth or ACL rows; it temporarily grants and revokes Clinicians `encounters:auth_a view` inside the encounter mutation authorization-policy run. Slice 186 adds no permanent auth or ACL rows; it reuses the permanent Clinicians `patients:docs addonly` grant and denies write/delete document authority. Slice 187 adds no permanent auth or ACL rows; it reuses the permanent Clinicians `patients:notes addonly` grant and denies write-level message mutation authority. Slice 189 adds no permanent auth or ACL rows; it reuses the permanent Clinicians `patients:appt write` grant, temporarily downgrades that grant to `view` inside the appointment mutation authorization-policy run, and restores `write` during cleanup. These local credentials do not represent production ASP.NET Core Identity, sessions, MFA, password lifecycle, or full authorization-policy enforcement.

The admin login audit readiness slice creates the modernized `auth_audit_events` table empty on seed and lets real login attempts populate it. The `slice-160-admin-login-audit-readiness` plan captures the maximum audit row id before the test, performs one successful `admin` login and one rejected login, then verifies only the new rows. Legacy parity reads OpenEMR `log` rows with decoded base64 comments such as `success: <ip>` and `failure: <ip>. user password incorrect`; modernized parity reads `auth_audit_events`, `/api/auth/login-audit`, and the Admin Login Audit panel. Audit rows are intentionally not canonical patient/staff seed data and broader audit coverage remains future scope.

The admin session readiness slice creates the modernized `auth_sessions` table empty on seed and lets successful login attempts populate it. The `slice-161-admin-session-readiness` plan verifies that legacy OpenEMR creates an `OpenEMR` session cookie and invalidates access after `interface/logout.php`, while the modernized target issues a session id from `/api/auth/login`, validates it through `/api/auth/session`, ends it through `/api/auth/logout`, stamps `ended_at`, and renders validate/end-session behavior in the Admin Session Readiness panel. Session rows are operational evidence and do not change canonical seed counts.

The admin audit protection readiness slice reuses the admin auth seed tables and adds no seed rows. The `slice-162-admin-audit-protection-readiness` plan verifies that legacy `interface/logview/logview.php` is not visible without a session and renders after admin login, while the modernized target rejects `/api/auth/login-audit` without `X-OpenEMR-Session`, allows it with an active admin session, rejects it again after logout, and only renders the Admin Login Audit panel after sign-in. Broader non-admin protected-API enforcement remains future scope.

The admin directory protection readiness slice reuses the admin auth seed tables and adds no seed rows. The `slice-163-admin-directory-protection-readiness` plan verifies that legacy user/facility administration pages are blocked without a session and render after admin login, while the modernized target rejects `/api/administration/directory` and administration mutations without `X-OpenEMR-Session`, allows directory retrieval with an active admin session, and only renders the Admin directory after sign-in. Remaining non-admin protected API enforcement beyond operational reports remains future scope.

The operational reports protection readiness slice reuses the admin auth seed tables and existing operational reporting data, adding no seed rows. The `slice-164-reports-protection-readiness` plan verifies that legacy report pages are blocked without a session and render after admin login, while the modernized target rejects `/api/reports/operational` and `/api/reports/operational/export` without `X-OpenEMR-Session`, allows report JSON and CSV retrieval with an active session, and only renders operational report data on the Reports page after sign-in. Remaining non-admin protected API enforcement remains future scope.

The patient chart protection readiness slice reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` patient anchor, adding no seed rows. The `slice-165-patient-protection-readiness` plan verifies that legacy patient summary access is blocked without a session and renders after admin login, while the modernized target rejects `/api/patients` search and chart requests without `X-OpenEMR-Session`, allows patient facts with an active session, and only renders Patient/Client search and chart data after sign-in. The clinical-list protection readiness slice reuses the same admin auth seed tables plus the existing `MOD-PAT-0001` clinical-list anchors, adding no seed rows. The `slice-166-clinical-list-protection-readiness` plan verifies that legacy patient-summary list facts are blocked without a session and render after admin login, while the modernized target rejects `/api/clinical-lists` retrieval and mutations without `X-OpenEMR-Session`, allows list facts with an active session, and only enables Lists workspace lookup/mutation controls after sign-in. Appointment, encounter, document, message, billing, and procedure protection are covered by Slice 167 through Slice 172; Slice 173 adds the first focused ACL-backed administration authorization-policy gate, Slice 174 extends that ACL-backed policy enforcement to operational reports JSON/CSV access, Slice 175 extends it to clinical-list retrieval and allergy mutation, Slice 176 proves Front Office appointment access, Slice 177 proves encounter denial for Front Office, Slice 178 proves patient Demographics access for admin and Front Office, Slice 179 proves patient document denial for Front Office plus admin Documents access, Slice 180 proves patient message denial for Front Office plus admin Patient Notes access, Slice 181 proves billing denial for Front Office plus admin Billing access, Slice 182 proves procedure Lab Results denial for Front Office plus admin procedure access, Slice 183 proves procedure mutation add/write/sign authorization using the modernized-only clinician auth/membership fixture, Slice 184 proves patient mutation add/write authorization by reusing that clinician fixture plus temporary patient cleanup, Slice 185 proves encounter mutation view/write authorization by temporarily granting Clinicians encounter view access while denying write-level encounter mutations and revoking the grant after the run, and Slice 186 proves document mutation add/write/delete authorization by reusing Clinicians Documents add-only access while denying write/delete document mutations, Slice 187 proves message mutation add/write authorization by reusing Clinicians Patient Notes add-only access while denying write-level message mutations, Slice 188 proves billing mutation view/write authorization by temporarily granting Clinicians Billing view access while denying write-level billing mutations and revoking the grant after the run, and Slice 189 proves appointment mutation view/write authorization by temporarily downgrading Clinicians from Appointment write to Appointment view access while denying write-level scheduling mutations and restoring the write grant after the run. Broader authorization policy depth remains future protected-access scope.

The prescription mutation slice uses `MOD-PAT-0008` as a stable prescription lifecycle anchor. The shared parity workflow creates a temporary oral prescription with dosage, quantity, refills, note, RxNorm code, and diagnosis, verifies direct row state and browser-visible active prescription rendering, deactivates the row with an end date and inactive note, and then hard-deletes the temporary row so the patient prescription count returns to the seeded baseline. The `slice-15-prescription-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The billing mutation slice uses `MOD-PAT-0001` and seeded encounter `1000013` as a stable fee-sheet lifecycle anchor. The shared parity workflow creates a temporary CPT billing line, verifies billing count changes, verifies direct row state and browser-visible active fee-sheet rendering, marks the line billed and inactive, then hard-deletes the temporary billing row so the patient counts return to the seeded baseline. The `slice-16-billing-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The procedure-results slice uses `MOD-PAT-0009` as a stable completed-lab anchor. Both legacy MariaDB and modernized PostgreSQL probes locate that patient's completed `Complete blood count` procedure order with code `85025`, completed report, and final result rows for `Erythrocytes`, `Hemoglobin`, `Platelets`, and `Leukocytes`. The current anchor facts include `Hemoglobin` result `13.8 g/dL`, normal range `12.0-17.5`, and `final` result status. The `slice-6-procedures-readiness` plan verifies these facts plus browser-visible procedure-result behavior against both targets.

The procedure mutation slice also uses `MOD-PAT-0009` as the shared lab lifecycle anchor. The shared parity workflow creates a temporary CMP-style procedure order against a seeded encounter, verifies order count changes and direct row state, marks the order complete, creates a reviewed final report, creates a final glucose result, verifies browser-visible order/report/result rendering, and then cascade-deletes the temporary order tree so the patient procedure counts return to the seeded baseline. The `slice-17-procedure-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The pending/scheduled procedure-order slice uses `MOD-PAT-0701` as a stable reportless lab-order anchor. Both legacy MariaDB and modernized PostgreSQL probes locate that patient's future scheduled `Complete blood count` procedure order with code `85025`, order date `2026-06-25`, status `scheduled`, and no linked report rows. The `slice-23-procedure-pending-orders-readiness` plan verifies these facts plus browser-visible pending/scheduled order behavior against both targets.

The fee-sheet billing slice uses `MOD-PAT-0001` as a stable billing anchor. Both legacy MariaDB and modernized PostgreSQL probes locate that patient's latest 2026 encounter `1000013`, diagnosis `E78.5`, and seeded CPT billing lines. The current anchor facts include `99214` / `Established patient office visit` with fee `168` and diagnosis justification `E78.5`, plus `36415` / `Routine venipuncture` with fee `18` and the same diagnosis justification. The `slice-7-billing-readiness` plan verifies these facts plus browser-visible fee-sheet behavior against both targets.

The insurance coverage slice uses `MOD-PAT-0005` as a stable coverage anchor. Both legacy MariaDB and modernized PostgreSQL probes locate primary `Northstar HMO` / `Medicare Advantage` coverage with policy `POL100005` and group `GRP104`, plus secondary `Acme Health` / `Family Choice` coverage with policy `SEC100005` and group `GRP204`. The canonical dataset keeps payer names as the portable domain value; the legacy seed maps those names to stable `insurance_companies` rows and stores the OpenEMR payer IDs in `insurance_data.provider`. The `slice-28-insurance-readiness` plan verifies those facts plus browser-visible coverage rendering in the legacy demographics screen and modernized Patient/Client chart.

The insurance mutation slice also uses `MOD-PAT-0005` as the stable coverage lifecycle anchor. The shared parity workflow creates a temporary tertiary coverage row, verifies direct row state and browser-visible rendering, updates payer, plan, policy, and group values, verifies the updated row in both targets, and then hard-deletes the temporary row so the patient coverage count returns to the seeded baseline. The `slice-34-insurance-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The encounter metadata mutation slice uses `MOD-PAT-0002` as a stable encounter-metadata lifecycle anchor. The shared parity workflow creates a temporary encounter with sensitivity, referral source, external ID, POS code, and billing note metadata, verifies direct row state, updates the metadata values, verifies the updated row in both targets, and then hard-deletes the temporary encounter so the patient encounter count returns to the seeded baseline. The `slice-35-encounter-metadata-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient demographics mutation slice uses `MOD-PAT-0010` as a stable demographics lifecycle anchor. The shared parity workflow updates first name, last name, preferred name, sex, DOB, street, city, state, postal code, marital status, and occupation, verifies direct row state for all fields, verifies browser-visible rendering for the demographics fields each target exposes, and then restores the original seeded demographics so the patient baseline remains unchanged. The `slice-36-patient-demographics-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient registration mutation slice uses generated `TMP-PAT-REG-*` temporary patients rather than changing or expanding the seeded patient population. The shared parity workflow creates a temporary patient with public ID, identity, DOB, address, marital status, occupation, phone, email, and HIPAA permission fields, verifies direct demographics and contact row state, verifies browser-visible chart rendering, and then hard-deletes the temporary patient so the 1,000-patient gold baseline remains unchanged. The `slice-37-patient-registration-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document sign-off mutation slice uses `MOD-PAT-0001` as the stable document lifecycle anchor and creates a temporary text document rather than changing seeded document rows. The shared parity workflow verifies the created document starts in pending review state, signs it to approved as `admin`, verifies normalized review status and timestamp state, verifies browser-visible rendering for each target, and then archives and hard-deletes the temporary document so the 1,200-document gold baseline remains unchanged. The `slice-38-document-signoff-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document external-link mutation slice uses `MOD-PAT-0001` as the stable document lifecycle anchor and creates a temporary `web_url` document rather than changing seeded document rows. The shared parity workflow verifies `mimetype = text/uri-list`, `storageMethod = web_url`, the stored URL, browser-visible rendering in the legacy document list and modernized Documents workspace, and then archives and hard-deletes the temporary document so the 1,200-document gold baseline remains unchanged. The `slice-39-document-external-link-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient image document preview slice also uses `MOD-PAT-0001` as the stable document lifecycle anchor and creates a temporary `image/svg+xml` document rather than changing seeded document rows. The shared parity workflow verifies `previewKind = image`, `previewStatus = Inline image preview`, `thumbnailLabel = IMG`, `canPreviewInline = true`, byte-for-byte content/download preservation, browser-visible rendering in the legacy document list, and inline image rendering in the modernized Documents viewer before archive/delete cleanup restores the 1,200-document gold baseline. The `slice-88-document-image-preview-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient image document thumbnail slice reuses the same `MOD-PAT-0001` lifecycle anchor and temporary `image/svg+xml` storage pattern. The shared parity workflow verifies `thumbnailDataUri = data:image/svg+xml;base64,...` from the stored document bytes, browser-visible rendering in the legacy document list, and an accessible modernized Documents card thumbnail image before archive/delete cleanup restores the 1,200-document gold baseline. The `slice-89-document-image-thumbnail-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient PDF document inline-preview slice reuses `MOD-PAT-0001` and seeded encounter `1000013` as the stable PDF lifecycle anchor. The shared parity workflow creates a temporary `application/pdf` document, verifies `previewKind = pdf`, `previewStatus = Inline PDF preview`, `thumbnailLabel = PDF`, `canPreviewInline = true`, byte-for-byte content/download preservation, browser-visible rendering in the legacy document list, and an accessible modernized Documents viewer iframe backed by the download endpoint before archive/delete cleanup restores the 1,200-document gold baseline. The `slice-90-document-pdf-preview-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document lifecycle timeline slice reuses `MOD-PAT-0001` and seeded encounter `1000013` as the stable patient-document lifecycle anchor. The shared parity workflow creates a temporary text document, verifies filed, current-version, review-pending, and active lifecycle facts, signs it as `admin` to verify review-approved facts, archives it to verify archived facts, restores it to verify active facts, renders the lifecycle timeline in the modernized Documents card and viewer, then hard-deletes the temporary document so the 1,200-document gold baseline is restored. The `slice-91-document-lifecycle-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient scanned attachment readiness slice reuses `MOD-PAT-0001` and seeded encounter `1000013` as the stable scanned-document lifecycle anchor. The shared parity workflow creates a temporary scanned PDF-style `Medical Record` document with notes carrying `Scan source: front-desk scanner` and `OCR pending`, verifies normalized scan status, capture source, scanned page count, and OCR status, renders those facts in the modernized Documents card and viewer, archives the document, and then hard-deletes it so the 1,200-document gold baseline is restored. The `slice-92-document-scanned-attachment-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document denial mutation slice uses `MOD-PAT-0001` as the stable document lifecycle anchor and creates a temporary text document rather than changing seeded document rows. The shared parity workflow verifies the created document starts pending, denies it as `admin`, verifies normalized denied review status and timestamp state, verifies browser-visible rendering for each target, and then archives and hard-deletes the temporary document so the 1,200-document gold baseline remains unchanged. The `slice-40-document-denial-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document metadata mutation slice uses `MOD-PAT-0001` as the stable document lifecycle anchor and creates a temporary text document rather than changing seeded document rows. The shared parity workflow verifies the created document starts in Medical Record, refiles it to Advance Directive with updated title, document date, encounter, and notes, verifies normalized metadata state plus browser-visible rendering for each target, and then archives and hard-deletes the temporary document so the 1,200-document gold baseline remains unchanged. The `slice-41-document-metadata-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document archive restore mutation slice uses `MOD-PAT-0001` as the stable document lifecycle anchor and creates a temporary text document rather than changing seeded document rows. The shared parity workflow archives the document, verifies active document counts and content retrieval behave as hidden/deleted, restores the document, verifies browser-visible rendering again, and then hard-deletes the temporary document so the 1,200-document gold baseline remains unchanged. The `slice-42-document-archive-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document content replacement mutation slice uses `MOD-PAT-0001` as the stable document lifecycle anchor and creates a temporary text document rather than changing seeded document rows. The shared parity workflow replaces the active document payload, verifies updated preview/content/download semantics, renders the document in the browser, archives it, and then hard-deletes the temporary document so the 1,200-document gold baseline remains unchanged. The `slice-43-document-content-replace-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document replacement revision slice uses the same temporary-document anchor and lifecycle. The shared parity workflow captures the current revision timestamp/hash after create, waits for a distinct replacement timestamp, replaces the active payload, verifies the current revision timestamp moves forward, verifies the revision hash matches the new stored hash, confirms the single-current-version contract remains visible, then archives and hard-deletes the temporary document so the seeded document baseline remains unchanged. The `slice-55-document-revision-replace-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The billing diagnosis mutation slice uses `MOD-PAT-0001` as the stable fee-sheet anchor and creates a temporary `ICD10` billing row rather than changing seeded CPT charge rows. The shared parity workflow verifies `code_type = ICD10`, zero fee, diagnosis justification, browser-visible legacy Fee Sheet rendering, modernized Fees rendering, inactive status hiding, and hard-delete cleanup. The `slice-44-billing-diagnosis-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The billing correction mutation slice uses `MOD-PAT-0001` as the stable fee-sheet anchor and creates a temporary CPT billing row rather than changing seeded charge rows. The shared parity workflow corrects code text, fee, units, and diagnosis justification, verifies browser-visible legacy Fee Sheet rendering, modernized Fees rendering, inactive status hiding, and hard-delete cleanup. The `slice-45-billing-correction-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The billing modifier mutation slice adds the legacy-compatible `modifier` field to the canonical billing model and maps 334 seeded follow-up office-visit rows with modifier `25`. The shared `slice-46-billing-modifier-readiness` plan still uses a temporary `MOD-PAT-0001` CPT row for mutation behavior, applying modifier `25`, verifying direct row state, checking legacy Fee Sheet and modernized Fees rendering, deactivating the row, and hard-deleting it so the seeded modifier baseline remains unchanged.

The claim status readiness slice adds 700 deterministic OpenEMR `claims` rows to the canonical dataset. These map to legacy MariaDB's native `claims` table and to the modernized PostgreSQL `claims` table. The `MOD-PAT-0005` billing anchor intentionally includes queued, generated-to-file, and cleared primary-payer claim examples so the `slice-47-claim-status-readiness` plan can verify direct database facts and modernized Fees rendering from the same gold data. This read-only slice does not mutate claim rows.

The payment posting readiness slice adds 420 deterministic OpenEMR `ar_session` rows and 617 `ar_activity` rows to the canonical dataset. These map to legacy MariaDB's native AR tables and to the modernized PostgreSQL `payment_sessions` and `payment_activities` tables. The `MOD-PAT-0005` billing anchor includes an insurance payment and contractual adjustment for encounter `1000052`, reference `EOB-NSTAR-1000052`, payment `$126.00`, adjustment `$42.00`, reason `CO-45`, and payer claim number `NSTAR-CLM-1000052`. The `slice-48-payment-posting-readiness` plan verifies direct database facts and modernized Fees rendering from the same gold data. This slice is read-only; full ERA/EOB import, adjudication, statement delivery, persisted collections, and revenue-cycle audit workflows remain future slices. Printable patient statement generation is covered by Slice 59 from the same seeded ledger, deterministic statement PDF export is covered by Slice 60, ranked statement batch candidate readiness is covered by Slice 61, statement batch package export readiness is covered by Slice 62, collections work queue readiness is covered by Slice 63, and cleanup-backed collections follow-up task readiness is covered by Slice 64.

The payment posting mutation slice reuses `MOD-PAT-0005` encounter `1000052` and creates only temporary AR rows. The shared workflow posts a payment of `$21.00` and a contractual adjustment of `$3.50`, verifies the temporary posting is visible in active payment rows, verifies account balance decreases by the payment plus adjustment, verifies account ledger entries are added, voids the posting so active payment and balance totals return to baseline, and hard-deletes the temporary payment session/activity rows. The `slice-56-payment-posting-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The claim status mutation slice reuses `MOD-PAT-0005` encounter `1000052` and creates only a temporary claim row. The shared workflow creates a queued primary-payer claim, verifies the new versioned claim row, moves it to generated X12/file state, clears it back to HCFA/no-file state, verifies modernized Fees rendering, and hard-deletes the temporary claim row. The `slice-57-claim-status-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient payment capture mutation slice reuses `MOD-PAT-0005` encounter `1000052` and creates only temporary patient-responsibility AR rows. The shared workflow posts a `$35.00` patient payment with `payer_id = 0`, `payer_type = 0`, method `credit_card`, and receipt-style reference `RCPT-PARITY-*`, verifies the active payment row, account balance decrease, and single payment ledger entry, verifies modernized Fees rendering as a patient payment, voids the row so active payment and balance totals return to baseline, and hard-deletes the temporary payment session/activity rows. The `slice-58-patient-payment-capture-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The account balance readiness slice reuses the same seeded billing, claim, and AR payment rows rather than adding new fixture data. The `MOD-PAT-0005` account anchor proves patient-level totals of `$635.00` charges, `$206.00` paid, `$64.25` adjusted, and `$364.75` balance, plus encounter `1000052` totals of `$186.00` charges, `$126.00` paid, `$42.00` adjusted, and `$18.00` balance. The `slice-49-account-balance-readiness` plan verifies those rollups against both target databases and modernized Fees rendering.

The account aging readiness slice derives deterministic aging buckets from the same `MOD-PAT-0005` billing/payment anchor and the dataset base date `2026-06-18`. The expected bucket totals are `$83.75` Current, `$18.00` 31-60, `$0.00` 61-90, `$263.00` Over 90, and `$364.75` total balance. The encounter anchors are `1000053` at 6 days in Current, `1000052` at 56 days in 31-60, and `1000051` at 361 days in Over 90. The `slice-50-account-aging-readiness` plan verifies those derived facts against both target databases and modernized Fees rendering. Statement delivery, recurring persisted collection workflows, and payment reversal behavior remain future revenue-cycle slices; printable statement generation is covered by Slice 59, deterministic PDF export is covered by Slice 60, statement batch candidate readiness is covered by Slice 61, statement batch package export readiness is covered by Slice 62, collections work queue readiness is covered by Slice 63, focused collections follow-up task lifecycle parity is covered by Slice 64, pnotes/message assignment parity is covered by Slice 65, pnotes/message content edit parity is covered by Slice 66, pnotes/message reply append parity is covered by Slice 156, and encounter document attachment parity is covered by Slice 67.

The account ledger readiness slice also derives from the existing `MOD-PAT-0005` billing/payment anchor. The canonical ledger has 10 entries: six charge rows, two payment rows, and two adjustment rows ordered by date, encounter, entry type, code, and description so legacy and modernized database IDs do not become part of the behavioral contract. The expected totals are `$635.00` charges, `$206.00` payments, `$64.25` adjustments, and `$364.75` ending balance. The ledger starts on `2025-06-22`, ends on `2026-06-25`, includes reference `EOB-NSTAR-1000052`, and is verified by the `slice-51-account-ledger-readiness` plan against both target databases and modernized Fees rendering.

The statement generation readiness slice derives from the same `MOD-PAT-0005` billing/payment/account statement anchor. The generated statement number is `STMT-MOD-PAT-0005-20260625`, payment instructions are `Please pay $364.75 by 2026-07-25.`, the document has 10 line items, and the totals remain `$635.00` charges, `$206.00` payments, `$64.25` adjustments, and `$364.75` balance due. The `slice-59-statement-generation-readiness` plan verifies the generated legacy-derived document, modernized API response, and modernized Fees rendering against both targets. The Slice 60 statement PDF export readiness slice uses the same derived document and verifies that the modernized endpoint exports `STMT-MOD-PAT-0005-20260625.pdf` with deterministic PDF text containing the statement number, payment instructions, balance due, and EOB-backed payment reference. The Slice 61 statement batch candidate readiness slice expands from one anchor to the full seeded billing population, deriving the top five positive-balance statement candidates and all-candidate totals from the shared ledger. The Slice 62 statement batch package readiness slice uses those candidates and generated statement PDFs to verify `manifest.json`, `summary.csv`, and packaged PDF artifacts. The Slice 63 collections work queue readiness slice derives a past-due follow-up queue from the same full-population ledger, adding high-priority counts, over-90 exposure, recommended actions, and contact method metadata. The Slice 64 collections follow-up task readiness slice creates temporary pnotes-compatible follow-up tasks from that queue, verifies close/archive/delete behavior and UI rendering, and cleans them up so no permanent seed counts change. The Slice 65 patient-message assignment readiness slice reassigns a temporary `MOD-PAT-0004` pnotes-compatible message from `admin` to `billing`, verifies count stability and UI rendering, and cleans up the row so no permanent seed counts change. The Slice 66 patient-message content readiness slice edits the title and body of a temporary `MOD-PAT-0004` pnotes-compatible message, verifies count stability and UI rendering, and cleans up the row so no permanent seed counts change. The Slice 156 patient-message reply readiness slice appends a reply line to a temporary `MOD-PAT-0004` pnotes-compatible message, verifies count stability and UI rendering, and cleans up the row so no permanent seed counts change. The Slice 67 encounter document attachment readiness slice reads the two existing linked documents for `MOD-PAT-0001` encounter `1000013`, verifies metadata/preview facts and UI rendering, and adds no permanent seed rows. The Slice 68 encounter billing linkage readiness slice reads the two active CPT4 fee-sheet rows for `MOD-PAT-0001` encounter `1000013`, verifies codes, text, fees, justifications, billed/activity state, and UI rendering, and adds no permanent seed rows. The Slice 69 encounter claim linkage readiness slice reads existing claim row `CLAIM-1000013-1`, verifies payer, version, status, bill-process, target, and UI rendering, and adds no permanent seed rows. The Slice 70 encounter procedure order linkage readiness slice reads the existing `MOD-PAT-0001` encounter `1000011` lab order `5000001`, verifies code `83036`, name `Hemoglobin A1c`, diagnosis `E11.9`, reviewed report `6000001`, four final result rows, and UI rendering, and adds no permanent seed rows. The Slice 71 encounter diagnosis coding readiness slice composes existing encounter diagnosis, fee-sheet justification, and procedure-order diagnosis evidence from `MOD-PAT-0001` encounters `1000013` and `1000011`, verifies `E78.5` and `E11.9` evidence, accounts for the legacy fee-sheet page's visible `E78.` rendering of the stored `E78.5` justification, and adds no permanent seed rows.

The Slice 72 encounter billing linkage mutation readiness slice creates a temporary `CPT4 99499` billing row on encounter `1000013`, verifies active encounter-linked billing and supporting diagnosis evidence, deactivates the row, and hard-deletes it so the seeded billing baseline remains unchanged.

The Slice 73 encounter diagnosis coding mutation readiness slice creates a temporary `ICD10 R73.03` fee-sheet diagnosis row on encounter `1000013`, verifies active encounter-linked diagnosis-coding evidence, deactivates the row, and hard-deletes it so the seeded billing baseline remains unchanged.

The Slice 74 encounter fee-sheet entry readiness slice creates temporary `CPT4 99499` and `ICD10 R73.03` fee-sheet rows on encounter `1000013`, verifies active encounter-linked billing and diagnosis evidence from the encounter workflow, deactivates both rows, and hard-deletes them so the seeded billing baseline remains unchanged.

The Slice 75 encounter procedure-order entry readiness slice creates a temporary pending `80053` laboratory order on encounter `1000013`, verifies active encounter-linked procedure-order evidence from the encounter workflow, and hard-deletes the order so the seeded procedure-order baseline remains unchanged. The Slice 76 encounter procedure-result entry readiness slice creates a temporary pending `80053` laboratory order plus reviewed final report/result on the same encounter, verifies active encounter-linked result evidence from the encounter workflow, and hard-deletes the order so the seeded procedure-order/report/result baseline remains unchanged. The Slice 77 encounter sign-off readiness slice creates a temporary encounter for `MOD-PAT-0002`, records and verifies a temporary `admin` attestation, deletes the signature, and deletes the encounter so no permanent signature rows remain. The Slice 78 encounter document upload readiness slice creates a temporary text document on `MOD-PAT-0001` encounter `1000013`, verifies encounter-scoped document facts and UI rendering, and hard-deletes the document so seeded document counts and encounter document links return to baseline. The Slice 79 encounter binary document upload readiness slice creates a temporary PDF/binary document on the same encounter, verifies preview/download facts plus byte-for-byte download content, and hard-deletes the document so seeded document counts and encounter document links return to baseline. The Slice 80 encounter document sign-off readiness slice creates a temporary text document on the same encounter, signs it as `admin`, verifies approved review state through normalized probes and modernized Encounter UI rendering, and hard-deletes the document so seeded document counts and encounter review state return to baseline. The Slice 81 encounter document denial readiness slice creates another temporary text document on the same encounter, denies it as `admin`, verifies denied review state through normalized probes and modernized Encounter UI rendering, and hard-deletes the document so seeded document counts and encounter review state return to baseline. The Slice 82 encounter document metadata readiness slice creates another temporary text document on the same encounter, refiles name, category, date, and notes while preserving the encounter link, verifies updated metadata through normalized probes and modernized Encounter UI rendering, and hard-deletes the document so seeded document counts and encounter document links return to baseline. The Slice 83 encounter document move readiness slice creates another temporary text document on encounter `1000013`, moves it to same-patient encounter `1000011`, verifies source removal, target attachment, and modernized Encounter UI rendering, and hard-deletes the document so seeded document counts and both encounter document links return to baseline. The Slice 84 encounter document content replacement readiness slice creates another temporary text document on encounter `1000013`, replaces its text payload, verifies replacement content, preview, revision hash, and modernized Encounter UI rendering, and hard-deletes the document so seeded document counts and encounter document links return to baseline. The Slice 85 encounter document archive/restore readiness slice creates another temporary text document on the same encounter, archives it, verifies active encounter detail hides it while archived detail includes it, restores it, verifies active rendering through the modernized Encounter UI, and hard-deletes the document so seeded document counts and encounter document links return to baseline. The Slice 86 encounter document lifecycle timeline readiness slice creates another temporary text document on the same encounter, verifies filed/current-version/review-pending/active lifecycle state, signs it, verifies approved lifecycle state, archives it, verifies archived lifecycle state, restores it, verifies active lifecycle state and modernized Encounter UI timeline rendering, and hard-deletes the document so seeded document counts and encounter document links return to baseline. The Slice 87 encounter external-link document readiness slice creates a temporary web URL document on the same encounter, verifies URL-backed document facts and modernized Encounter external-link rendering, archives it, and hard-deletes it so seeded document counts and encounter document links return to baseline. The Slice 88 patient image document preview readiness slice creates a temporary `image/svg+xml` patient document on `MOD-PAT-0001`, verifies inline-preview metadata and modernized Documents viewer image rendering, archives it, and hard-deletes it so seeded document counts return to baseline. The Slice 89 patient image document thumbnail readiness slice creates another temporary `image/svg+xml` patient document on `MOD-PAT-0001`, verifies stored-byte thumbnail data URI facts and modernized Documents card thumbnail rendering, archives it, and hard-deletes it so seeded document counts return to baseline. The Slice 90 patient PDF document inline-preview readiness slice creates a temporary `application/pdf` patient document on `MOD-PAT-0001`, verifies inline PDF preview metadata and modernized Documents viewer iframe rendering, archives it, and hard-deletes it so seeded document counts return to baseline. The Slice 91 patient document lifecycle timeline readiness slice creates a temporary text patient document on `MOD-PAT-0001`, verifies filed/current-version/review-pending/active state, signs it, verifies approved lifecycle state, archives it, verifies archived lifecycle state, restores it, verifies active lifecycle state and modernized Documents lifecycle rendering, and hard-deletes it so seeded document counts return to baseline. The Slice 92 patient scanned attachment readiness slice creates a temporary scanned PDF patient document on `MOD-PAT-0001`, verifies scan status, capture source, scanned page count, and OCR status, renders those facts in the modernized Documents card and viewer, archives it, and hard-deletes it so seeded document counts return to baseline. The Slice 93 appointment reschedule readiness slice creates a temporary future appointment on `MOD-PAT-0003`, reschedules it to `2026-10-22 14:15`, verifies updated schedule facts and modernized Calendar rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 94 appointment arrival readiness slice creates a temporary future appointment on `MOD-PAT-0003`, marks it arrived with status `@`, verifies arrived schedule facts and modernized Calendar rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 95 appointment check-out readiness slice creates a temporary future appointment on `MOD-PAT-0003`, marks it arrived with status `@`, checks it out with status `>`, verifies checked-out schedule facts and modernized Calendar rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 96 appointment no-show readiness slice creates a temporary future appointment on `MOD-PAT-0003`, marks it no-show with status `?`, verifies no-show schedule facts and modernized Calendar rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 97 appointment category readiness slice creates a temporary future appointment on `MOD-PAT-0003`, verifies category `13` Preventive Care Services, updates it to category `10` New Patient, verifies legacy and modernized category rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 98 appointment pending-status readiness slice creates a temporary future appointment on `MOD-PAT-0003`, updates it to status `~`, verifies legacy and modernized pending-status rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 99 appointment provider reassignment readiness slice creates a temporary future appointment on `MOD-PAT-0003`, reassigns it from provider `101` to provider `102`, verifies legacy and modernized provider rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 100 appointment facility reassignment readiness slice creates a temporary future appointment on `MOD-PAT-0003`, reassigns it from facility `10` to facility `11`, verifies legacy and modernized facility rendering, and hard-deletes it so seeded appointment counts return to baseline. The Slice 101 appointment billing-location reassignment readiness slice creates a temporary future appointment on `MOD-PAT-0003`, keeps service facility `10`, reassigns billing location from `10` to `11`, verifies legacy and modernized billing-location rendering, and hard-deletes it so seeded appointment counts return to baseline.

Slice 102 extends the scheduling anchor contract with appointment comments. The generated canonical appointments now carry comments, the legacy projection maps them to `openemr_postcalendar_events.pc_hometext`, the modernized PostgreSQL seed maps them to `appointments.comments`, and the parity workflow creates a temporary `2026-12-24 08:30` appointment whose comments are updated and rendered before hard-delete cleanup.

Slice 103 extends the scheduling anchor contract with appointment recurrence metadata. The generated canonical appointments now include 80 regular preventive-care recurrence anchors with `recurrenceType = 1`, every-two-weeks cadence, and an end date 84 days after the anchor appointment. The legacy projection maps these values to `openemr_postcalendar_events.pc_recurrtype`, serialized `pc_recurrspec`, and `pc_endDate`; the modernized PostgreSQL seed maps them to `appointments.recurrence_type`, `repeat_frequency`, `repeat_unit`, and `recurrence_end_date`. The parity workflow creates a temporary `2026-12-29 10:00` appointment, updates its cadence to every two weeks until `2027-01-28`, verifies legacy and modernized recurrence rendering, and hard-deletes it so seeded appointment counts return clean. Slice 105 extends the same permanent seed contract with a recurrence exception anchor: `APPT-MOD-PAT-0013-3` skips `2026-12-16`, with legacy `pc_recurrspec` storing the `exdate` value and the modernized seed storing it in `appointments.recurrence_exdates`. Slice 106 mutates that same anchor temporarily by appending `2026-12-30` as a generated-occurrence cancellation exception, verifies expansion after the skip, and restores the seeded single-exception contract during cleanup. Slice 107 uses the same anchor to prove exception removal: it temporarily adds `2026-12-30`, restores that skipped occurrence by removing the extra exception date, verifies the full occurrence set returns, and again leaves the seeded `2026-12-16` exception as the only permanent skipped date. Slice 108 uses the same anchor to prove occurrence rescheduling: it appends `2026-12-30` as the original generated date exception, creates a standalone `2027-01-06 14:00` preventive-care appointment, verifies the generated series now exposes dates `2026-12-02`, `2027-01-13`, and `2027-01-27` with occurrence numbers `3`, `6`, and `7`, then deletes the standalone row and restores the seeded single-exception contract. Slice 109 edits the same root exception list directly, adding `2026-12-30` beside the seeded `2026-12-16` skip, verifying the generated occurrence disappears, and restoring the seeded single-exception contract. Slice 110 edits the same root title/time to `Preventive Care Root Update` at `16:15`, verifies generated occurrences from `2026-11-04` through `2027-01-27` inherit those root values while preserving occurrence numbers `1`, `2`, `3`, `5`, `6`, and `7`, and restores the original `Preventive Care` root at `15:30` with the seeded single-exception contract. Slice 111 edits the same root metadata to provider `101`, service facility `10`, billing facility `10`, category `10`, status `~`, room `Series Meta`, and comments `Slice 111 recurring root metadata propagation check.`, verifies all generated occurrences inherit those values, and restores the seeded provider `102`, facility `11`, billing location `11`, category `13`, status `-`, room `Room 5`, comments, and single-exception contract. Slice 112 does not add permanent seed rows; it creates a temporary `MOD-PAT-0003` monthly recurring appointment on `2026-12-15`, verifies monthly expansion through `2027-04-15`, updates the cadence to every two months through `2027-08-15`, verifies generated dates and repeat controls, and hard-deletes the appointment so seeded appointment counts return clean. Slice 113 also does not add permanent seed rows; it creates temporary `MOD-PAT-0003` daily, workday, and yearly recurring appointments, verifies generated dates through `2028-06-30`, verifies legacy and modernized repeat controls, and hard-deletes the appointments so seeded appointment counts return clean. Slice 114 also does not add permanent seed rows; it creates a temporary Monday/Wednesday/Friday recurrence for `MOD-PAT-0003`, verifies generated dates through `2026-12-18`, verifies legacy `REPEAT_DAYS` and modernized weekday controls, and hard-deletes the appointment so seeded appointment counts return clean. Slice 115 also does not add permanent seed rows; it creates temporary second-Tuesday and last-Friday monthly repeat-on appointments for `MOD-PAT-0003`, verifies generated dates through `2027-04-30`, verifies legacy `REPEAT_ON` fields and modernized repeat-on controls, and hard-deletes the appointments so seeded appointment counts return clean. Slice 116 uses the permanent seeded `MOD-PAT-0013` recurring appointment root `APPT-MOD-PAT-0013-3`, temporarily changes its cadence from every two weeks through `2027-01-27` to every three weeks through `2027-02-10`, verifies generated dates `2026-11-04`, `2026-11-25`, `2027-01-06`, and `2027-01-27` while preserving the seeded `2026-12-16` skip, and restores the original recurrence values so seeded scheduling anchors remain unchanged. Slice 117 does not add permanent seed rows; it creates temporary same-provider, same-time appointments for `MOD-PAT-0003` and `MOD-PAT-0004` at `2026-12-04 09:00` with provider `102`, verifies both legacy and modernized targets tolerate the overlap and render distinct appointments, verifies modernized overlap counts/IDs, and hard-deletes both appointments so seeded appointment counts return clean.

Slice 118 also does not add permanent seed rows; it creates two temporary same-patient, same-time appointments for `MOD-PAT-0003` at `2026-12-05 09:00` with providers `101` and `102`, verifies both legacy and modernized targets tolerate the overlap and render distinct appointments, verifies modernized patient-overlap counts/IDs, and hard-deletes both appointments so seeded appointment counts return clean.

Slice 119 also does not add permanent seed rows; it creates two temporary same-room, same-time appointments for `MOD-PAT-0003` and `MOD-PAT-0004` at `2026-12-06 09:00` in `Parity Room Overlap`, verifies both legacy and modernized targets tolerate the overlap and render distinct appointments, verifies modernized room-overlap counts/IDs, and hard-deletes both appointments so seeded appointment counts return clean.

Slice 120 does not add permanent seed rows; it uses existing seeded appointment `APPT-MOD-PAT-0191-3` and legacy note key `MOD-PAT-0191-3` for `MOD-PAT-0191` on `2026-06-25`, exactly seven days after dataset base date `2026-06-18`, plus seeded HIPAA SMS/email consent and contact details. The reminder-readiness contract derives `Due now`, channel `SMS + Email`, contact `(619) 555-1191 / mod-pat-0191@example.test`, and lead `7` days from that same gold dataset anchor.

Slice 121 does not add permanent seed rows; it creates a temporary encounter for `MOD-PAT-0002`, records temporary `admin` and `gold-provider-02` signatures at `2026-06-18 11:20` and `2026-06-18 11:25`, verifies locked co-signature ordering, hash, and amendment/note facts, then deletes the signatures and encounter so encounter and signature counts return clean.

Slice 122 does not add permanent seed rows; it uses the existing seeded encounter-attached documents `DOC-MOD-PAT-0001-1` and `DOC-MOD-PAT-0001-2` on `MOD-PAT-0001` encounter `1000013`. The revision-readiness contract verifies `Version 1`, `Current version`, one-version history, no prior versions, revision timestamps `2026-06-10 14:30:00` and `2026-06-12 15:00:00`, and revision-hash equality against each document hash.

Slice 123 does not add permanent seed rows; it creates a temporary text document on `MOD-PAT-0001` encounter `1000013`, captures Version 1/current-revision facts, replaces the payload, verifies the current revision hash changes and still matches the active hash, then hard-deletes the document so document counts and encounter attachment counts return to baseline.

The administration directory slice uses the seeded provider/staff and facility records as stable anchors. Both legacy MariaDB and modernized PostgreSQL probes locate 20 `gold-*` users, 12 provider users with calendar access, and 3 facilities. The current anchor facts include `gold-provider-02` / `Morris, Jordan` as an authorized provider assigned to `North County Clinic`, `gold-billing-01` / `Grant, Drew` as a non-calendar billing user assigned to `Modernization Family Medicine`, and the MAIN/NORTH/EAST facilities. The `slice-8-admin-readiness` plan verifies these facts plus browser-visible Users and Facilities behavior against both targets.

The administration facility mutation slice uses temporary facilities rather than changing the three seeded MAIN/NORTH/EAST anchors. The shared parity workflow creates a temporary facility, verifies direct row state and browser-visible active rendering, updates the facility name and inactive state, verifies the updated inactive row and default hidden-inactive list behavior, and then hard-deletes the temporary facility so facility mutation rows return to the seeded baseline. The `slice-18-admin-facility-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The administration user mutation slice uses temporary users rather than changing the 20 seeded `gold-*` provider/staff anchors. The shared parity workflow creates a temporary active front-desk user, verifies direct row state and browser-visible active rendering, updates the user name and inactive state, verifies the updated inactive row and default hidden-inactive list behavior, and then hard-deletes the temporary user so user mutation rows return to the seeded baseline. The `slice-19-admin-user-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The administration access-control slice uses OpenEMR's default ACL topology rather than synthetic patient-specific records. The modernized seed mirrors 7 access groups, 65 visible permission objects, 203 group-permission assignments, and 2 default user memberships from the legacy default phpGACL data. The `slice-20-access-control-readiness` plan verifies group counts, key permission objects, representative group-permission assignments, default `admin` and `oe-system` Administrator memberships, and browser-visible access-control surfaces against both legacy MariaDB and modernized PostgreSQL. The `slice-22-user-group-membership-mutation-readiness` plan creates a temporary active user, assigns the Front Office group membership, verifies the membership row and modernized user-card rendering, revokes the membership, and deletes the temporary user.

The administration access-permission mutation slice uses the seeded Front Office `patients:demo` write assignment as a reversible ACL anchor. The shared parity workflow revokes that one assignment, verifies the group-permission count drops from 203 to 202 and Front Office drops from 6 to 5 permissions, then grants the same assignment back and verifies the seed baseline is restored. The `slice-21-access-permission-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB and modernized PostgreSQL.

The operational reports slice uses aggregate facts from the existing gold dataset rather than a separate report fixture table. Both legacy MariaDB and modernized PostgreSQL probes normalize the same reporting anchors: 1,000 patients, 200 portal-enabled patients, 2,800 current-year appointments, 1,261 future appointments after the 2026-06-18 dataset base date, 2,100 encounters, 1,100 current-year encounters, 3,000 billing lines, `$446,000.00` seeded charges, 700 lab reports, 1,200 messages, 1,200 patient documents, 12 providers, and 3 facilities. The current report anchors include `gold-provider-02` with 176 encounters and 253 billing lines, NORTH facility with 935 appointments and 701 encounters, and `Asthma, uncomplicated` / `ICD10:J45.909` with 188 active-problem rows. The `slice-9-reports-readiness` plan verifies these facts plus browser-visible report surfaces against both targets. The `slice-24-reports-export-readiness` plan reuses the same aggregates as normalized export rows with the stable `Section, Name, Metric, Value` shape so the modernized CSV endpoint can be compared to the legacy report-export contract.

The patient documents slice uses `MOD-PAT-0001` as a stable document anchor. The shared dataset includes `Primary care intake packet` in the `Medical Record` category and `Advance directive acknowledgement` in the `Advance Directive` category for that patient, plus 1,198 additional deterministic document records distributed across the synthetic population. The `slice-25-documents-readiness` plan verifies document counts, category ids/names, document dates, storage method, text payload previews, and browser-visible document lists against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document mutation slice also uses `MOD-PAT-0001` and seeded encounter `1000013` as a stable document lifecycle anchor. The shared parity workflow creates a temporary `Medical Record` text document, verifies the document count increases and browser-visible document rendering succeeds, soft-deletes the record to prove OpenEMR-style archive behavior, and then hard-deletes the temporary row so the patient document count returns to the seeded baseline. The `slice-26-document-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React.

The patient document content slice uses the same `MOD-PAT-0001` `Primary care intake packet` anchor. The full stored payload is part of the parity contract:

```text
Gold synthetic document DOC-MOD-PAT-0001-1
Patient: Avery Stone (MOD-PAT-0001)
Category: Medical Record
Document: Primary care intake packet
Document date: 2026-06-10
Encounter: 1000013
Purpose: Stable search and demographics navigation
```

The `slice-27-document-content-readiness` plan verifies the full payload through normalized legacy MariaDB and modernized PostgreSQL probes, then verifies modernized API retrieval, text download, and Documents workspace viewer rendering for that same document.

The binary patient document mutation slice also uses `MOD-PAT-0001` and seeded encounter `1000013` as a stable document lifecycle anchor. The shared parity workflow creates a temporary PDF-style `Medical Record` document with MIME type `application/pdf`, verifies direct row state, stored file size, and base64 byte equality, verifies browser-visible document filing, verifies modernized byte-preserving download, soft-deletes the row to prove OpenEMR-style archive behavior, and then hard-deletes the temporary row so the patient document count returns to the seeded baseline. The `slice-33-binary-document-mutation-readiness` plan verifies that lifecycle against both legacy MariaDB/OpenEMR and modernized PostgreSQL/React without adding permanent binary rows to the gold dataset.

The immunization history slice uses `MOD-PAT-0007` as the stable pediatric vaccine-history anchor. The shared dataset now includes 2,648 deterministic immunization records across the synthetic population. Every patient receives a current-year influenza record, chronic-care/adult cohorts receive additional pneumococcal and Td-style coverage, and pediatric cohorts receive a richer childhood vaccine set. The legacy seed maps these rows into OpenEMR's native `immunizations` table with OpenEMR immunization list IDs, CVX codes, manufacturer, lot, administered date, VIS dates, route/site, provider, and encounter linkage. The modernized PostgreSQL seed maps the same rows into normalized `immunizations` records and uses the same CVX-first display labels that legacy OpenEMR renders. The `slice-29-immunizations-readiness` plan verifies `MOD-PAT-0007` includes at least eight immunization records, including `Influenza, seasonal, injectable` with CVX `141` and `Hep A, ped/adol, 2 dose` manufactured by `GlaxoSmithKline`, and verifies browser-visible rendering on the legacy Immunizations page and modernized Lists workspace. The `slice-30-immunization-mutation-readiness` plan uses the same anchor but creates a temporary influenza immunization row, verifies active database counts and browser-visible rendering, marks the row entered in error so it leaves active counts, then hard-deletes it so the seeded vaccine-history baseline remains unchanged.

The patient contact mutation slice uses `MOD-PAT-0001` as the first shared mutation anchor. The legacy seed maps the canonical phone value into OpenEMR's `phone_home`, `phone_contact`, and `phone_cell` fields with HIPAA SMS/email permissions enabled. The modernized PostgreSQL seed maps the same canonical phone value into `phone`, `phone_home`, and `phone_cell`, with `hipaa_allow_sms` and `hipaa_allow_email` enabled. The `slice-10-contact-mutation-readiness` plan updates home phone, cell phone, email, and HIPAA contact permissions, verifies database state and browser-visible contact values, then restores the original seeded record on both targets.

The appointment mutation slice uses `MOD-PAT-0003` as the shared scheduling mutation anchor. The plan creates a temporary future appointment on `2026-10-15` at `10:30`, verifies the appointment count increases, marks the appointment cancelled with status `x`, verifies browser-visible cancelled detail, deletes the temporary appointment, and verifies the count returns to the seeded baseline. Slice 93 reuses the same anchor for appointment reschedule readiness: it creates a temporary future appointment, updates it to `2026-10-22` at `14:15` for 45 minutes with status `@` and room `Resched`, verifies database and browser-visible rescheduled facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 94 reuses the same anchor for appointment arrival readiness: it creates a temporary future appointment on `2026-10-29` at `09:00`, marks it arrived with status `@`, verifies the arrived title/status and browser-visible Calendar facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 95 reuses the same anchor for appointment check-out readiness: it creates a temporary future appointment on `2026-11-05` at `11:00`, marks it arrived with status `@`, checks it out with status `>`, verifies the checked-out title/status and browser-visible Calendar facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 96 reuses the same anchor for appointment no-show readiness: it creates a temporary future appointment on `2026-11-12` at `13:00`, marks it no-show with status `?`, verifies the no-show title/status and browser-visible Calendar facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 97 reuses the same anchor for appointment category readiness: it creates a temporary future appointment on `2026-11-19` at `09:15` with category `13` Preventive Care Services, updates it to category `10` New Patient, verifies database and browser-visible category facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 98 reuses the same anchor for appointment pending-status readiness: it creates a temporary future appointment on `2026-11-26` at `10:45`, updates it to status `~`, verifies database and browser-visible pending-status facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 99 reuses the same anchor for appointment provider reassignment readiness: it creates a temporary future appointment on `2026-12-03` at `09:30`, updates the provider from `101` to `102` while preserving facility `10`, verifies database and browser-visible provider facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 100 reuses the same anchor for appointment facility reassignment readiness: it creates a temporary future appointment on `2026-12-10` at `10:00`, updates the facility and billing facility from `10` to `11` while preserving provider assignment, verifies database and browser-visible facility facts, deletes the temporary appointment, and verifies seeded counts return clean. Slice 101 reuses the same anchor for appointment billing-location reassignment readiness: it creates a temporary future appointment on `2026-12-17` at `09:15`, keeps service facility `10`, updates billing location from `10` to `11`, verifies database and browser-visible billing-location facts, deletes the temporary appointment, and verifies seeded counts return clean. This keeps the shared gold dataset stable while proving the create/cancel/delete, reschedule/update, arrival/check-in, check-out, no-show, category, pending-status, provider reassignment, facility reassignment, and billing-location reassignment lifecycles against both legacy MariaDB and modernized PostgreSQL.

Slice 102 reuses the same `MOD-PAT-0003` scheduling anchor for appointment comments readiness: it creates a temporary future appointment on `2026-12-24` at `08:30`, updates its comments from the initial scheduling note to a referral-packet reminder, verifies the legacy `form_comments` field and modernized Calendar comments rendering, deletes the temporary appointment, and verifies seeded counts return clean. This extends the shared appointment lifecycle set to include comments alongside create/cancel/delete, reschedule/update, arrival/check-in, check-out, no-show, category, pending-status, provider reassignment, facility reassignment, and billing-location reassignment behavior.

Slice 103 reuses the same `MOD-PAT-0003` scheduling anchor for appointment recurrence metadata readiness: it creates a temporary future appointment on `2026-12-29` at `10:00` with weekly recurrence metadata, updates it to repeat every two weeks until `2027-01-28`, verifies legacy and modernized recurrence controls/rendering, deletes the temporary appointment, and verifies seeded counts return clean. This adds regular recurrence metadata preservation alongside comments, create/cancel/delete, reschedule/update, arrival/check-in, check-out, no-show, category, pending-status, provider reassignment, facility reassignment, and billing-location reassignment behavior while leaving recurring-series propagation for a later slice. Slice 105 uses the permanent `MOD-PAT-0013` preventive-care recurrence anchor to verify exception-date read behavior: search from `2026-12-02` returns `2026-12-02`, `2026-12-30`, `2027-01-13`, and `2027-01-27`, skips `2026-12-16`, and preserves occurrence numbers `3`, `5`, `6`, and `7`. Slice 106 uses that same anchor for mutation behavior: cancelling generated occurrence `2026-12-30` leaves `2026-12-02`, `2027-01-13`, and `2027-01-27`, preserves occurrence numbers `3`, `6`, and `7`, and restores the original `2026-12-16` exception list. Slice 107 adds the reverse mutation: after temporarily skipping `2026-12-30`, restoring that exception brings the visible occurrence dates back to `2026-12-02`, `2026-12-30`, `2027-01-13`, and `2027-01-27` with occurrence numbers `3`, `5`, `6`, and `7`. Slice 108 adds the individual occurrence move behavior: rescheduling generated occurrence `2026-12-30` creates a standalone `2027-01-06 14:00` appointment while the recurring expansion skips the original date and preserves later occurrence numbers `6` and `7`. Slice 109 adds direct exception-list editing on the recurring root: adding `2026-12-30` hides that generated occurrence, keeps later occurrence numbers at `6` and `7`, and restores the original `2026-12-16` exception list during cleanup. Slice 110 adds recurring root title/time propagation: changing the root to `Preventive Care Root Update` at `16:15` updates generated occurrences without changing the seeded exception list, then restores `Preventive Care` at `15:30`. Slice 111 adds recurring root metadata propagation: changing provider, service facility, billing facility, category, status, room, and comments updates generated occurrences without changing the seeded exception list, then restores the original seeded metadata. Slice 112 adds monthly recurrence coverage with a temporary monthly appointment, every-two-month update, generated dates, repeat controls, and cleanup. Slice 113 adds recurrence unit matrix coverage with temporary daily, workday, and yearly recurring appointments that verify generated dates, repeat controls, and cleanup without changing the seeded scheduling anchors. Slice 114 adds OpenEMR days-of-week recurrence coverage with a temporary Monday/Wednesday/Friday appointment for `MOD-PAT-0003`, verifies generated dates from `2026-12-07` through `2026-12-18`, verifies legacy `REPEAT_DAYS` and modernized weekday controls, and hard-deletes the appointment so seeded scheduling anchors remain unchanged. Slice 115 adds OpenEMR monthly repeat-on recurrence coverage with temporary second-Tuesday and last-Friday appointments for `MOD-PAT-0003`, verifies generated dates through `2027-04-30`, verifies legacy `REPEAT_ON` values and modernized repeat-on controls, and hard-deletes the appointments so seeded scheduling anchors remain unchanged. Slice 116 adds seeded recurring-series recurrence update coverage on `APPT-MOD-PAT-0013-3`, verifies every-three-weeks generated dates and skipped-date preservation after a temporary cadence/end-date edit, and restores the seeded every-two-weeks recurrence contract. Slice 117 adds provider-overlap tolerance coverage by creating overlapping `2026-12-04 09:00` appointments for two patients with provider `102`, verifying both systems keep them distinct and the modernized target exposes reciprocal overlap detail, then deleting both temporary rows.

Slice 118 adds patient-overlap tolerance coverage by creating overlapping `2026-12-05 09:00` appointments for `MOD-PAT-0003` with providers `101` and `102`, verifying both systems keep them distinct and the modernized target exposes reciprocal patient-overlap detail, then deleting both temporary rows.

Slice 119 adds room-overlap tolerance coverage by creating overlapping `2026-12-06 09:00` appointments for `MOD-PAT-0003` and `MOD-PAT-0004` in `Parity Room Overlap`, verifying both systems keep them distinct and the modernized target exposes reciprocal room-overlap detail, then deleting both temporary rows.

Slice 120 adds appointment reminder readiness coverage by deriving due/status/channel/contact/lead facts from the seeded `MOD-PAT-0191` appointment on `2026-06-25` and seeded contact-consent values, verifying those facts through the shared side-by-side probes and modernized Calendar rendering without mutating permanent seed data. Slice 189 appointment mutation authorization-policy readiness adds no permanent rows; it reuses the `MOD-PAT-0003` appointment anchors and permanent Clinicians `patients:appt write` grant, temporarily downgrades Clinicians to `patients:appt view`, proves view-only schedule reads and write-level mutation denial, and restores the write grant during cleanup.

Slice 121 adds encounter co-signature readiness coverage by creating a temporary `MOD-PAT-0002` encounter, adding primary and co-signature rows from distinct users, verifying locked co-signature rendering and database order/hash facts, and deleting all temporary rows without mutating permanent seed data.

Slice 190 adds encounter amendment history readiness coverage without permanent seed rows. It creates a temporary `MOD-PAT-0002` encounter, records ordered temporary signature rows with two nonblank amendments and one blank sign-off note, verifies legacy `esign_signatures` and modernized `encounter_signatures` project the same two history entries, proves the modernized API/UI filter out the blank note, and deletes the temporary signatures and encounter during cleanup.

Slice 191 adds patient duplicate detection readiness coverage without permanent seed rows. It reuses the existing `MOD-PAT-0010` patient anchor, creates a temporary registration with matching first name, last name, date of birth, phone, and email, verifies legacy `patient_data` matching facts and modernized duplicate-candidate scoring/API/UI projection, and deletes the temporary patient during cleanup.

Slice 192 adds patient registration validation readiness coverage without permanent seed rows. It uses invalid `TMP-PAT-REG-VAL-*` registration drafts to prove OpenEMR-style registration validation rules are represented and that the modernized API/UI return structured validation messages without inserting a patient row.

Slice 193 adds patient deceased-status readiness coverage without permanent seed rows. It extends the modernized patient schema with nullable deceased date and reason fields that mirror legacy `patient_data.deceased_date` and `patient_data.deceased_reason`, then reuses the existing `MOD-PAT-0010` anchor for restore-backed mutation tests that temporarily mark the patient deceased and restore the original values.

Slice 194 adds patient guardian-contact readiness coverage with permanent deterministic canonical patient fields. It maps mother name, guardian name, guardian relationship, guardian phone, and guardian email into legacy `patient_data` and modernized `patients`, then reuses the existing `MOD-PAT-0010` anchor for restore-backed mutation tests that temporarily update and restore those fields.

Slice 195 adds patient guardian demographic/address readiness coverage with permanent deterministic canonical patient fields. It maps guardian sex, address, city, state, postal code, country, and work phone into legacy `patient_data` and modernized `patients`, then reuses the existing `MOD-PAT-0010` anchor for restore-backed mutation tests that temporarily update and restore the expanded guardian block.

Slice 196 adds patient social-detail readiness coverage with permanent deterministic canonical patient fields. It maps race, ethnicity, interpreter preference, family size, monthly income, homeless status, and financial review date into legacy `patient_data` and modernized `patients`, then reuses the existing `MOD-PAT-0010` anchor for restore-backed mutation tests that temporarily update and restore the social-detail block.

Slice 197 adds patient employer core readiness coverage with permanent deterministic canonical patient employer fields. It maps employer name, street, city, state, postal code, and country into legacy `employer_data` and modernized `patient_employers`, then reuses the existing `MOD-PAT-0010` anchor for restore-backed mutation tests that temporarily update and restore the employer block.
Slice 198 adds patient provider assignment readiness without changing the permanent seed set. The plan reuses `MOD-PAT-0010`, temporarily changes its primary provider to seeded provider/user `103` (`Alex Chen`), verifies legacy `patient_data.providerID` and modernized `patients.provider_id` plus UI/API rendering, and restores the original provider assignment.
Slice 199 adds patient care-team readiness without adding permanent care-team memberships to the gold seed. The plan reuses `MOD-PAT-0010`, temporarily creates a lead care-team member using seeded provider/user `103` (`Alex Chen`), seeded facility `12` (`East County Care Center`), role `primary_care_provider`, status `active`, and provider-since date `2026-06-18`, verifies legacy `care_teams` / `care_team_member` and modernized `patient_care_teams` / `patient_care_team_members` plus UI/API rendering, and restores the original care-team state. Seed reset scripts explicitly clear synthetic care-team rows for the 1,000-patient PID range before loading patient records.
Slice 200 adds multi-member patient care-team readiness without adding permanent care-team memberships to the gold seed. The plan reuses `MOD-PAT-0010`, temporarily creates two care-team members using seeded provider/user `103` (`Alex Chen`) at facility `12` (`East County Care Center`) and seeded provider/user `104` (`Robin Morris`) at facility `10` (`Modernization Family Medicine`), verifies legacy `care_teams` / `care_team_member` and modernized `patient_care_teams` / `patient_care_team_members` plus UI/API rendering, and restores the original care-team members.

Slice 201 adds deterministic related-person/contact seed coverage without adding permanent care-team memberships to the gold seed. It reuses `MOD-PAT-0010`, temporarily creates a mixed care team using provider/user `103` (`Alex Chen`) at facility `12` (`East County Care Center`) and related contact `3200010` (`Casey Brooks`), verifies legacy `care_team_member.contact_id` plus modernized `patient_care_team_members.contact_id` / `patient_related_contacts`, and restores the original care-team members.

Slice 202 adds deterministic patient history/lifestyle seed coverage for every canonical patient without adding a mutation fixture. It reuses `MOD-PAT-0010` as a read-only anchor, verifies legacy OpenEMR `history_data` fields and History and Lifestyle page rendering, verifies modernized `patient_histories` plus the Patient/Client History and Lifestyle panel, and leaves the seeded history baseline unchanged.

Slice 203 adds deterministic insurance subscriber coverage to the existing permanent insurance rows without adding new coverage rows. It uses `MOD-PAT-0005` as the subscriber anchor: the secondary `Acme Health` coverage row has spouse subscriber `Jamie Morgan`, DOB `1976-05-05`, phone `619-555-7004`, address `2204 Mesa Partner Ave, Poway, CA 92064`, and employer `Harbor Health Logistics`. The parity plan verifies those seeded facts, then creates, updates, and deletes a temporary tertiary coverage row with subscriber details so the seeded 1,400-record insurance baseline remains stable.

Slice 204 adds deterministic portal account coverage for the existing 200 portal-enabled patients without changing the portal-enabled patient count. It uses `MOD-PAT-0004` as the read-only account anchor: CMS login, portal username, and portal login username are `mod-pat-0004@example.test`; password status is `1` / patient-managed; and the one-time reset token is empty so the readiness state is not pending. Slice 206 reuses that same anchor for access mutation tests: the permanent seed state is portal access enabled, and parity temporarily revokes then grants access while preserving the provisioned account fields. Slice 207 adds deterministic synthetic portal credentials to those 200 accounts: legacy OpenEMR receives a real bcrypt `patient_access_onsite.portal_pwd` value for `PortalPass207!`, the legacy seed enables the local OpenEMR patient portal via `globals.portal_onsite_two_enable`, and the modernized seed derives the matching salted test hash into `patient_portal_accounts.password_hash`. Slice 208 reuses the same credential/account anchor and creates only transient modernized `patient_portal_sessions` evidence rows during active-session read, logout, and reuse-rejection checks. Slice 209 reuses the same signed-in account anchor plus existing portal home facts: two messages for `MOD-PAT-0004` with one `New` and one `Done`, latest title `Portal message`, and the upcoming `2026-07-28 14:30` `Preventive Care` appointment.

Slice 217 reuses the `MOD-PAT-0004` portal account and does not change permanent seed counts. It creates cleanup-backed temporary mailbox rows above the seeded portal-mailbox range to verify that the legacy `onsite_mail` All folder and modernized `portal_mailbox_messages` All projection include active patient-owned inbox and sent rows while excluding rows archived with `message_status = Delete`.

Slice 218 reuses the same `MOD-PAT-0004` portal account and permanent document rows rather than creating temporary records. The portal document anchors are active, patient-owned rows in the existing document baseline: document `8000007` / `DOC-MOD-PAT-0004-1` in `Advance Directive` named `Advance directive acknowledgement 01`, and document `8000008` / `DOC-MOD-PAT-0004-2` in `CCDA` named `Continuity of care document 02`. The parity plan uses those rows for category-list and `patient_documents.zip` download checks on both targets.

Slice 219 reuses the same `MOD-PAT-0004` portal account and permanent appointment rows rather than creating temporary records. The portal appointment anchors are `APPT-MOD-PAT-0004-3` (`Preventive Care`, 2026-07-28 14:30, status `-`, category `Preventive Care Services`), `APPT-MOD-PAT-0004-2` (`Established Patient`, 2026-06-06 13:00), and `APPT-MOD-PAT-0004-1` (`New Patient`, 2026-02-21 12:30). The parity plan uses those rows for future/past appointment-list checks on both targets. Slice 220 reuses the same `MOD-PAT-0004` portal account but creates temporary request rows instead of permanent seed records: a pending `^` `Established Patient` appointment on `2026-09-22 09:30` with a unique request reason and a provider reminder titled `Patient Reminders`. Cleanup deletes both the temporary appointment and reminder by their generated IDs or unique reason text. Slice 221 uses the same `MOD-PAT-0004` portal account without mutations and verifies request option defaults of category `9`, provider `105`, provider-derived facility `11`, date `2026-09-22`, time `09:30`, and duration `15` minutes plus category duration evidence for New Patient and Preventive Care Services. Slice 222 also uses the same portal account without mutations and verifies the seeded clinical-summary facts: problems `Low back pain, unspecified` and `Anxiety disorder, unspecified`, allergy `Latex` with `skin irritation` / `low`, medication-list entries `Omeprazole 20 mg`, `Sumatriptan 50 mg`, and `Sertraline 50 mg`, and active prescriptions `Omeprazole`, `Sumatriptan`, and `Sertraline`. Slice 223 reuses the same portal account without mutations and verifies the seeded lab-results facts: order `5000004` (`Hemoglobin A1c`, order date `2026-02-21`), report `6000004` (`complete` / `reviewed`, report date `2026-02-23`), and final results `Hemoglobin A1c` `5.7 %`, `Glucose` `102 mg/dL`, `Cholesterol` `188 mg/dL`, and `HDL Cholesterol` `52 mg/dL`.

Slice 122 adds encounter document revision readiness coverage by reusing the seeded encounter attachments on encounter `1000013`, verifying current-version metadata through legacy/modernized database probes and modernized Encounter card rendering without mutating permanent seed data.

Slice 123 adds encounter document replacement revision coverage by creating a temporary encounter-attached document, replacing its text content, verifying current revision hash/timestamp facts through both targets, and hard-deleting the row without mutating permanent seed data.

Slice 126 adds encounter scanned attachment readiness coverage by creating a temporary PDF document on `MOD-PAT-0001` encounter `1000013`, deriving scan status, capture source, scanned page count, and OCR status from document metadata/notes in both targets, rendering those facts in the modernized Encounter attached-document card, and hard-deleting the row without mutating permanent seed data.

Slice 127 adds encounter binary document content replacement coverage by creating a temporary PDF document on `MOD-PAT-0001` encounter `1000013`, replacing its stored binary payload with a second PDF while preserving the same document and encounter identity, verifying replacement file name, MIME type, size, hash/revision, preview, API/UI, and byte-for-byte download facts, and hard-deleting the row so seeded document counts and encounter document links return to baseline.

Slice 128 adds patient binary document content replacement coverage by creating a temporary PDF document on `MOD-PAT-0001` with seeded encounter `1000013`, replacing its stored binary payload with a second PDF from the Documents workflow while preserving the same document and patient identity, verifying replacement file name, MIME type, size, hash/revision, preview, API/UI, and byte-for-byte download facts, and hard-deleting the row so seeded patient document counts return to baseline.

Slice 129 adds procedure result correction coverage by creating a temporary encounter, lab order, report, and result for `MOD-PAT-0009`, correcting the same result row in place to a `118` value with `70-110` range, `borderline` abnormal flag, and `corrected` status, verifying legacy and modernized database/API/UI facts, and deleting the temporary order tree and encounter so procedure and encounter counts return to baseline. The parity workflow uses compact temporary external IDs so the same fixture shape works against legacy OpenEMR field lengths and the modernized PostgreSQL schema.

Slice 130 adds procedure specimen coverage by extending the modernized gold seed projection with report collected date and specimen number, then creating a temporary `MOD-PAT-0009` encounter, lab order, report, and result with a compact `SPC*` specimen number. The shared plan verifies report collected date, report date, specimen number, status, review status, browser-visible specimen rendering, and result linkage on both targets, then deletes the temporary order tree and encounter so procedure and encounter counts return to baseline.

Slice 131 adds order-level procedure specimen detail coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter, lab order, and `procedure_specimen`/`lab_specimens` row with compact specimen and accession identifiers, then verifies specimen identifier, accession identifier, specimen type/code, collection method/code, specimen location/code, collected date/time, volume value/unit, condition/code, comments, database/API facts, and browser-visible modernized rendering. Cleanup deletes the temporary order tree and encounter so procedure, encounter, and specimen counts return to baseline.

Slice 132 adds procedure order correction coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter and lab order, corrects the same order in place from an initial CMP-style order to a CBC-style order, verifies corrected order date, procedure code/name/type, diagnosis, priority, status, instructions, browser-visible modernized rendering, legacy Procedure Results rendering after report/result creation, and normalized database/API facts. Cleanup deletes the temporary order tree and encounter so procedure and encounter counts return to baseline.

Slice 133 adds procedure report correction coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter, lab order, report, and result, corrects the same report in place with collected date `2026-06-19`, report date `2026-06-19`, compact corrected specimen number, `corrected` report status, `reviewed` review status, and corrected notes, then verifies browser-visible modernized rendering, legacy Procedure Results rendering, normalized database/API facts, and linked result preservation. Cleanup deletes the temporary order tree and encounter so procedure and encounter counts return to baseline.

Slice 134 adds procedure report sign-off coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter, lab order, report, and result, signs the report as `admin` at `2026-06-19 14:15`, verifies normalized legacy `source`/`review_status` sign-off state and modernized `reviewed_by`/`reviewed_at` state, checks UI/API/database facts and linked result preservation, and deletes the temporary order tree and encounter so procedure and encounter counts return to baseline.

Slice 135 adds procedure report review queue coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter, lab order, and `received` report, verifies the report appears in the unreviewed queue on both legacy and modernized targets, signs the report as `admin` at `2026-06-21 08:45`, verifies it appears in the reviewed queue and no longer appears in the unreviewed queue, and deletes the temporary order tree and encounter so procedure and encounter counts return to baseline.

Slice 136 adds procedure report review queue patient/date filter coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter, lab order dated `2026-06-21`, and `received` report, verifies patient/order-date filtered queue inclusion, verifies outside-date exclusion for `2026-06-20`, signs the report as `admin` at `2026-06-21 09:15`, verifies filtered reviewed queue membership, and deletes the temporary order tree and encounter so procedure and encounter counts return to baseline.

Slice 137 adds procedure report review queue provider filter coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter, lab order dated `2026-06-21`, and `received` report with provider `101`, verifies provider-filtered queue inclusion, verifies outside-provider exclusion for provider `102`, signs the report as `admin`, verifies provider-filtered reviewed queue membership, and deletes the temporary order tree and encounter so procedure and encounter counts return to baseline.

Slice 138 adds procedure report review queue lab filter coverage without adding permanent gold records. It creates temporary matching/outside lab-provider rows, a temporary `MOD-PAT-0009` encounter, lab order dated `2026-06-21`, and `received` report assigned to the matching lab, verifies lab-filtered queue inclusion, verifies outside-lab exclusion, signs the report as `admin`, verifies lab-filtered reviewed queue membership, and deletes the temporary order tree, encounter, and lab-provider rows so procedure, encounter, and lab-provider counts return to baseline.

Slice 154 adds procedure report reopen review coverage without adding permanent gold records. It creates a temporary `MOD-PAT-0009` encounter, lab order, and final report dated `2026-06-21`, signs the report as `admin` at `2026-06-21 12:25:00`, verifies reviewed queue membership, reopens review back to received/unreviewed state, verifies reviewed queue exclusion, unreviewed queue membership, blank visible reviewer metadata, legacy `list_reports.php` option `3` rendering, modernized Procedures and Reports workspace rendering, and deletes the temporary order tree plus encounter so procedure and encounter counts return to baseline.

Slice 139 adds permanent procedure lab-provider catalog coverage to the gold dataset. It uses the stable `MOD-PAT-0009` reviewed report anchor (`5000009` / `6000009`) to prove that lab `504` (`Pacific Women's Health Laboratory`) owns the seeded report, that outside lab `501` excludes the same report, and that both legacy OpenEMR and the modernized Reports workspace render the reviewed queue row from the same seed data.

Slice 140 adds procedure lab provider directory coverage over the same five-provider catalog. It verifies that provider IDs `501` through `505` render with their NPI values, active state, OpenEMR's default `DL` protocol value, 200 assigned orders, 140 reviewed reports, and 60 future scheduled orders on both targets.

Slice 141 adds temporary procedure lab provider lifecycle coverage. It creates a provider with NPI `1720123499`, protocol `DL`, and active state, verifies default active-directory rendering, deactivates the row, verifies include-inactive rendering, then deletes the provider and confirms cleanup on both targets.

Slice 144 adds temporary procedure lab provider address-book linkage coverage. It creates two `ord_lab` address-book organizations, links a temporary provider to each in sequence, verifies the derived provider organization/name and linked type in database/API/UI evidence, then deletes the provider and address-book organizations during cleanup.

Slice 145 adds permanent procedure order catalog coverage, while Slices 147, 148, 149, 151, 153, and 154 add temporary procedure catalog lifecycle, vendor compendium import, procedure order queue, procedure order transmit, procedure report bulk sign-off, and procedure report reopen-review coverage. Slice 149 creates a temporary reportless lab order dated `2026-06-21`, verifies ready-to-send queue inclusion and reported queue exclusion, attaches a temporary report, verifies reported queue inclusion and ready-to-send exclusion, and deletes the temporary order tree and encounter so procedure and encounter counts return to baseline. Slice 151 creates a temporary reportless lab order dated `2026-06-21`, marks it transmitted, verifies sent-awaiting-results queue inclusion and ready-to-send exclusion, and deletes the temporary order tree and encounter so procedure and encounter counts return to baseline. Slice 153 creates two temporary final reports dated `2026-06-21`, verifies unreviewed queue inclusion, bulk signs both reports, verifies reviewed queue inclusion and unreviewed exclusion, and deletes both temporary order trees plus the encounter so procedure and encounter counts return to baseline. Slice 154 creates one temporary signed final report dated `2026-06-21`, reopens it to received/unreviewed state, verifies reviewed exclusion plus unreviewed inclusion with blank reviewer metadata, and deletes the temporary order tree plus encounter so procedure and encounter counts return to baseline.

The encounter mutation slice uses `MOD-PAT-0002` as the shared clinical mutation anchor. The plan creates a temporary encounter on `2026-06-18`, records vitals, records a SOAP note, verifies encounter/vitals/clinical-note counts increase, updates the encounter reason and billing note, verifies browser-visible updated reason, blood pressure, billing note, and SOAP assessment, deletes the temporary encounter and child rows, and verifies the counts return to the seeded baseline.

### Level 3: Extended Synthetic Population

Purpose: scale testing, search performance, reporting, and realistic clinical histories.

Current status: optional future extension. The V1 gold dataset already contains 1,000 patients, 1,200 patient document records, 2,648 immunization records, 1,000 lab orders assigned to 5 lab providers, 700 seeded claim status rows, 420 payment sessions, 617 payment activity rows, 200 provisioned portal account rows, and 400 deterministic portal mailbox rows on both legacy and modernized targets. Future expansions may add richer Synthea-derived clinical history, additional billing/claims/payment depth, deeper scanned-document storage/OCR fixtures, vaccine-registry fixtures, or reporting-specific fixtures.

Potential future sources:

- OpenEMR development `import-random-patients` flow with Synthea.
- A project-owned Synthea-based generation process.
- The OpenEMR `demo-data-generator` if it proves compatible with the pinned version.

This level should be introduced only after the deterministic V1 gold workflow data is covered by baseline tests.

## Operating Rules

- Never use real patient data, PHI, production credentials, or production extracts.
- Do not treat public demo servers as the authoritative baseline because they are reset and mutable.
- Do not rely on browser-only manual data entry for core test state.
- Make seed scripts idempotent or provide a reset-before-seed workflow.
- Store generated artifacts under ignored artifact folders unless the artifact is an intentional source-controlled test fixture.
- Record expected counts and named seed personas in this document or a future seed-data manifest.

## Near-Term Next Step

Continue expanding reusable modernized parity adapters that consume the gold dataset:

- Add additional modernized workflow actions behind the same mutation-test intent as CRUD slices are implemented.
- Add PostgreSQL probes for newly implemented domain behavior where normalized database facts are useful.
- Add Playwright tests for each modernized workflow slice using the existing canonical anchors.
- Additional encounter mutation tests for templates, amendment policy controls beyond signature-derived history, diagnosis coding edits, order mutations, and billing linkage updates as those modernized workflows are implemented.
- Procedure-result mutation, broader e-prescribing, billing tests, and deeper messaging workflows as the next workflow slices are selected.

Patient payment capture is now covered by Slice 58, printable patient statement generation is covered by Slice 59, deterministic statement PDF export is covered by Slice 60, statement batch candidate readiness is covered by Slice 61, statement batch package export readiness is covered by Slice 62, collections work queue readiness is covered by Slice 63, and collections follow-up task lifecycle readiness is covered by Slice 64. Slice 171 billing protection readiness does not add new permanent gold rows; it reuses the existing `MOD-PAT-0001` fee-sheet anchor and the established billing/statement/collections data to prove no-session blocking plus active-session access. Slice 172 procedure protection readiness likewise adds no permanent gold rows; it reuses `MOD-PAT-0009` procedure-result anchors, `MOD-PAT-0701` scheduled-order anchors, seeded order catalog/provider rows, and the existing admin auth seed tables to prove no-session blocking plus active-session access. Slice 182 procedure authorization-policy readiness reuses those same anchors and the existing admin/front-desk auth plus ACL rows to prove front-desk Lab Results denial and admin procedure access without adding permanent gold rows. Slice 183 procedure mutation authorization-policy readiness adds only the modernized clinician auth/membership rows needed for ACL evidence and reuses the same procedure anchors plus temporary order/report rows that are cleaned up by the test plan. Slice 184 patient mutation authorization-policy readiness adds no permanent rows, reuses the clinician fixture and existing patient anchors, and creates only a temporary registered patient that admin cleanup removes during the test plan. Slice 185 encounter mutation authorization-policy readiness adds no permanent rows, reuses the clinician fixture and existing encounter anchor, and creates only a temporary view-only ACL grant that the test revokes. Slice 186 patient document mutation authorization-policy readiness adds no permanent rows, reuses the clinician fixture and existing patient document anchor, and creates only a temporary document that admin cleanup removes during the test plan. Slice 187 patient message mutation authorization-policy readiness adds no permanent rows, reuses the clinician fixture and existing patient-message anchors, and creates only a temporary message that admin cleanup removes during the test plan. Slice 188 billing mutation authorization-policy readiness adds no permanent rows, reuses the clinician fixture and existing billing anchors, and creates only a temporary view-only ACL grant that the test revokes. Slice 189 appointment mutation authorization-policy readiness adds no permanent rows, reuses the clinician fixture and existing appointment anchors, and creates only a temporary ACL downgrade from Appointment write to view that the test restores to write. Slice 190 encounter amendment history readiness adds no permanent rows, reuses the encounter signature infrastructure, and creates only temporary encounter/signature rows that the test removes. Slice 191 patient duplicate detection readiness adds no permanent rows, reuses the `MOD-PAT-0010` patient anchor, and creates only a temporary matching registration that the test removes. Slice 192 patient registration validation readiness adds no permanent rows and verifies invalid `TMP-PAT-REG-VAL-*` drafts leave no patient behind. Slice 193 patient deceased-status readiness adds no permanent rows, reuses the `MOD-PAT-0010` patient anchor, and temporarily marks and restores deceased date/reason fields. Slice 194 patient guardian-contact readiness adds permanent deterministic mother/guardian fields to the canonical patient dataset, reuses the `MOD-PAT-0010` patient anchor for parity, and temporarily updates/restores mother name, guardian name, relationship, phone, and email fields during the test plan. Future revenue-cycle data work should focus on refunds/reversals beyond simple voiding, statement delivery, receipt printing, recurring collection workflows, ERA/EOB import, richer statement batch delivery/layout, and audit history.

Slice 195 extends the permanent deterministic guardian data with sex, address, city, state, postal code, country, and work phone, and uses the same `MOD-PAT-0010` restore-backed parity pattern so the seeded guardian detail baseline remains stable after each run.

Slice 196 extends the permanent deterministic patient data with race, ethnicity, interpreter, family size, monthly income, homeless, and financial review date fields, and uses the same `MOD-PAT-0010` restore-backed parity pattern so the seeded social-detail baseline remains stable after each run.

Slice 197 extends the permanent deterministic patient data with employer identity and address fields, maps them through legacy `employer_data` and modernized `patient_employers`, and uses the same `MOD-PAT-0010` restore-backed parity pattern so the seeded employer baseline remains stable after each run.
Slice 198 reuses the deterministic provider catalog and the same `MOD-PAT-0010` restore-backed parity pattern so the seeded primary-provider baseline remains stable after each run.
Slice 199 reuses the deterministic provider and facility catalogs and the same `MOD-PAT-0010` restore-backed parity pattern so the seeded care-team baseline remains stable after each run.
Slice 200 reuses the deterministic provider and facility catalogs and the same `MOD-PAT-0010` restore-backed parity pattern so multi-member care-team tests can run without changing the permanent seed baseline.

Slice 201 adds deterministic related-contact rows to the permanent seed baseline and uses the same `MOD-PAT-0010` restore-backed parity pattern so mixed provider/contact care-team tests can run without leaving permanent care-team memberships behind.

Slice 202 adds deterministic history/lifestyle rows to the permanent seed baseline and uses `MOD-PAT-0010` as a read-only parity anchor, so the history seed count should remain exactly 1,000 after every run.

Slice 203 enriches the permanent insurance baseline with subscriber details and uses `MOD-PAT-0005` plus a temporary tertiary coverage lifecycle for parity, so the insurance seed count should remain exactly 1,400 after every run.

Slice 204 adds 200 portal account rows to the permanent seed baseline and uses `MOD-PAT-0004` as a read-only parity anchor, so the `portalAccounts` seed count should remain exactly 200 after every run.

Slice 205 reuses `MOD-PAT-0004` for temporary patient portal one-time reset mutation/cleanup, Slice 206 reuses the same anchor for temporary portal access revoke/grant mutation/cleanup, Slice 207 reuses it for valid sign-in, invalid-password rejection, disabled-access rejection, and pending-reset rejection checks, Slice 208 reuses it for patient portal session logout checks, Slice 209 reuses it for portal home identity, message summary, and upcoming appointment checks, and Slices 210 through 217 reuse it for secure-message inbox, compose/sent, reply, thread view, archive, read-status, selected-message archive, and All-folder checks; Slice 218 reuses permanent document anchors; Slice 219 reuses permanent appointment anchors; Slice 220 creates cleanup-backed temporary appointment/reminder rows; Slice 221 reuses existing provider, facility, category, and portal account anchors without mutations; Slice 222 reuses existing clinical-list and active-prescription anchors without mutations; Slice 223 reuses existing lab order/report/result anchors without mutations; Slice 224 reuses existing portal account, clinical-list issue, encounter/form, and lab order/report/result anchors without mutations; Slice 225 reuses the same portal account, billing, report-section, and lab order/report/result anchors to generate the default customized medical-history report without adding seed rows; Slice 226 reuses those same report, billing, and lab anchors to export the generated customized medical-history report PDF without adding seed rows; Slice 227 reuses the same portal account plus existing problem, allergy, and medication-list issue anchors to verify selected-issue generated-report behavior without adding seed rows; Slice 228 reuses the same portal account plus existing encounter/form anchors to verify selected Vitals/SOAP generated-report behavior while adding legacy `New Patient Encounter` wrapper rows for existing encounters so OpenEMR's portal report builder can display those form choices; Slice 229 reuses the same portal account, patient identity, billing-location facility, and generated-report anchors to verify printable template metadata while overlaying OpenEMR's installed default facility row with the gold primary facility identity in the legacy seed; Slice 230 reuses those same portal account, printable-report, PDF-report, and generated-report anchors to verify deterministic generated-report ZIP package delivery without adding seed rows; Slice 231 reuses the same anchors while creating only transient modernized `patient_portal_report_audit_events` rows during generated-report, PDF-download, and package-download checks; Slice 232 reuses the same portal account and existing mailbox anchors while creating only transient modernized `patient_portal_message_audit_events` rows during compose, reply, read, single-archive, and selected-archive checks; Slice 233 creates cleanup-backed temporary encrypted mailbox rows for the same portal account while normalizing encrypted-body visibility through the shared protected placeholder; Slice 234 creates cleanup-backed temporary inbox and practice-side patient-message rows for the same portal account while verifying forward-to-practice behavior; Slice 235 creates cleanup-backed temporary inbox and sent mailbox rows, archives them into transient Deleted-folder evidence, and cleans them up; Slice 236 creates cleanup-backed temporary procedure orders for the same portal account's latest encounter so selected procedure-order report generation can be verified; Slice 237 creates cleanup-backed temporary procedure orders for the same account so selected procedure-order PDF and package artifact delivery can be verified; Slice 238 reuses the same portal account, legacy admin user fallback, modernized `auth_accounts` admin row, and existing mailbox anchors to verify secure-message recipient-directory routing without adding seed rows; Slice 239 reuses the same portal account, recipient route, and static OpenEMR compose subject options (`General`, `Insurance`, `Prior Auth`, `Bill/Collect`, `Referral`, and `Pharmacy`) to verify secure-message subject-preset readiness without adding seed rows; Slice 240 creates cleanup-backed temporary HTML body inbox messages for secure-message body rendering; Slice 241 creates cleanup-backed temporary first-page and older second-page inbox messages to verify 20-message folder pagination; Slice 242 creates cleanup-backed temporary inbox messages to verify browser-only mark-all-read behavior while persisted mailbox statuses remain unchanged; Slice 243 reuses `MOD-PAT-0004`'s permanent active prescriptions while adding the modernized prescription modified-date projection to the generated Postgres seed; Slice 244 reuses the same permanent active prescriptions while adding the modernized prescription date-added projection to the generated Postgres seed; Slice 245 creates a cleanup-backed temporary prescription for that same portal account, ends it, verifies active portal prescription filtering, and deletes it; Slice 246 reuses the same permanent medication-list anchors while adding deterministic medication modified dates to the canonical dataset plus a cleanup-backed temporary ended medication-list row; Slice 247 reuses the same permanent problem-list anchors plus a cleanup-backed temporary ended problem-list row; Slice 248 reuses the same permanent allergy anchor plus a cleanup-backed temporary ended allergy row; Slice 249 reuses the same permanent portal-home influenza immunization plus a cleanup-backed temporary immunization marked entered in error; Slice 250 reuses existing `MOD-PAT-0004` patient demographics and primary/secondary insurance anchors without adding rows; Slice 251 reuses the same portal account and demographics anchors while creating cleanup-backed pending profile-review rows; Slice 252 reuses that cleanup-backed pending row to verify the staff-side review queue; Slice 253 reuses the same temporary pending row, accepts it, verifies chart demographics changed, then restores the seeded `MOD-PAT-0004` demographics and removes the profile audit rows; Slice 254 reuses the same temporary pending row, closes it through the staff-side `Revert Edits` path, verifies chart demographics stayed seeded, and removes the profile audit rows; Slice 255 creates a cleanup-backed active `patient_reminders` row for `MOD-PAT-0004`, verifies secure-message Inbox and All notification projection, verifies Sent and Deleted exclusion, and removes the temporary reminder; and Slice 510 creates cleanup-backed composed portal mailbox rows for `MOD-PAT-0004`, verifies sent and recipient message copies expose `attachmentCount: 0` with empty `attachments`, checks the modernized Portal sent card renders `Attachments 0`, and removes the temporary mailbox rows. These slices do not add permanent portal-account, mailbox, lab, encounter, facility, patient-message, immunization, insurance, procedure-order, or patient-reminder domain rows and should leave the 200-account portal baseline and 400-row permanent mailbox projection unchanged.

Slice 240 adds no permanent seed records. Its parity suite creates cleanup-backed temporary HTML body inbox messages for the `MOD-PAT-0004` portal account, preserving the permanent portal-account and mailbox seed while verifying secure-message body rendering.

Slice 241 adds no permanent seed records. Its parity suite creates 20 cleanup-backed first-page inbox messages dated after one cleanup-backed older inbox message for the `MOD-PAT-0004` portal account, verifies that the older row is reachable only after paging forward, and deletes the temporary mailbox rows so the permanent portal-account and mailbox seed remains unchanged.

Slice 242 adds no permanent seed records. Its parity suite creates two cleanup-backed temporary inbox messages for the `MOD-PAT-0004` portal account, verifies the browser-visible mark-all-read status change, refreshes/reloads mailbox data, and confirms the persisted mailbox status remains `New` before deleting the temporary rows.

Slice 243 adds no permanent seed records. It adds `modified_date` to the generated modernized Postgres `prescriptions` projection, populates it from the deterministic prescription start dates when no separate source value is present, and verifies `MOD-PAT-0004`'s permanent active Omeprazole, Sumatriptan, and Sertraline modified dates against legacy OpenEMR's portal prescription `Last Modified` view.

Slice 244 adds no permanent seed records. It adds `date_added` to the generated modernized Postgres `prescriptions` projection, populates it from the deterministic legacy seed timestamp pattern of `startDate 10:00:00`, and verifies `MOD-PAT-0004`'s permanent active Omeprazole, Sumatriptan, and Sertraline start-date timestamps against legacy OpenEMR's portal prescription `Start Date` view.

Slice 245 adds no permanent seed records. It creates a temporary prescription for `MOD-PAT-0004`, deactivates it with an `end_date`, verifies the signed-in portal still returns only the three permanent active prescriptions with null end dates, and deletes the temporary prescription during cleanup.

Slice 246 adds no new permanent medication-list rows, but it does make existing permanent medication list entries carry a deterministic `modifiedDate` in the canonical dataset. The legacy seed writes that value to `lists.modifydate`, and the modernized Postgres seed writes it to `medications.modified_date`. The parity suite creates a temporary medication for `MOD-PAT-0004`, deactivates it with the existing medication mutation end-date convention (`2026-06-18`), verifies the signed-in portal still displays ended medication-list rows like legacy OpenEMR's `/portal/get_medications.php`, and deletes the temporary medication during cleanup.

Slice 247 adds no permanent seed records. It creates a temporary problem for `MOD-PAT-0004`, deactivates it with the existing problem mutation end-date convention (`2026-06-18`), verifies the signed-in portal still displays ended problem-list rows like legacy OpenEMR's `/portal/get_problems.php`, and deletes the temporary problem during cleanup.

Slice 248 adds no permanent seed records. It creates a temporary allergy for `MOD-PAT-0004`, deactivates it with the existing clinical-list allergy mutation end-date convention (`2026-06-18`), verifies the signed-in portal still displays ended allergy rows and the legacy allergy date/referrer columns like OpenEMR's `/portal/get_allergies.php`, and deletes the temporary allergy during cleanup.

Slice 249 adds no permanent seed records. It creates a temporary immunization for `MOD-PAT-0004`, marks it entered in error, verifies the signed-in portal home still displays the row in the legacy `Patient Immunization` health snapshot and modernized Portal home immunization card, and deletes the temporary immunization during cleanup.

Slice 250 adds no permanent seed records. It reuses `MOD-PAT-0004`'s existing medical-record demographics plus primary `Community HMO` / `POL100004` and secondary `Standard Silver` / `SEC100004` insurance rows to verify the signed-in portal profile card against legacy OpenEMR's `portal/get_profile.php` fragment and the modernized `Profile From Medical Records` card.

Slice 251 adds no permanent patient-domain seed records. It adds an empty modernized `patient_portal_profile_change_requests` table to the generated Postgres schema and creates cleanup-backed pending profile-review rows for `MOD-PAT-0004` during parity runs; the legacy target uses matching cleanup-backed `onsite_portal_activity` rows. The submitted test edit changes email, home phone, cell phone, street, city, state, and ZIP in the pending request only, while the seeded medical-record demographics remain unchanged.

Slice 252 adds no permanent seed records. It reuses the Slice 251 cleanup-backed portal profile edit for `MOD-PAT-0004`, then verifies the waiting review row through the staff-side portal activity queue. The legacy target reads the temporary `onsite_portal_activity` row, while the modernized target reads the temporary `patient_portal_profile_change_requests` row through the administration directory API.

Slice 253 adds no permanent seed records. It reuses the Slice 251 cleanup-backed portal profile edit for `MOD-PAT-0004`, accepts the waiting review row, verifies the requested email, home phone, cell phone, street, city, state, and ZIP are committed to chart demographics, then restores the original seeded demographics and deletes the temporary profile activity rows during cleanup.

Slice 254 adds no permanent seed records. It reuses the Slice 251 cleanup-backed portal profile edit for `MOD-PAT-0004`, chooses the staff-side `Revert Edits` path, verifies the waiting review row closes with OpenEMR-style `closed` / `completed` / `accept` metadata while chart demographics remain unchanged, then deletes the temporary profile activity rows during cleanup.

Slice 255 adds no permanent seed records. It adds an empty generated PostgreSQL `patient_reminders` table for modernized parity and uses cleanup-backed temporary `patient_reminders` rows for `MOD-PAT-0004` to verify legacy-style patient portal notification projection.

Slice 510 adds no permanent seed records and stores no attachments or PHI-bearing files. It uses cleanup-backed composed secure-message rows for `MOD-PAT-0004` only to prove the current shared mailbox contract exposes explicit zero-attachment metadata (`attachmentCount: 0`, `attachments: []`) for both sent and recipient message projections.

Slice 511 adds no seed records. It extends the Slice 510 parity evidence only, attaching created-message and UI-cleanup payloads for the same cleanup-backed `MOD-PAT-0004` composed secure-message rows.

Slice 512 adds no permanent seed records and stores no attachment content. It reuses the same `MOD-PAT-0004` portal account, attempts a non-empty secure-message attachment submission through the shared parity workflow, verifies both targets reject the request before creating sent, inbox, or All-folder mailbox rows, and confirms active portal secure-message upload controls remain absent.

Slice 513 adds no permanent seed records. It creates cleanup-backed matching and decoy secure-message inbox rows for `MOD-PAT-0004`, verifies the shared folder-search query returns the matching message and excludes the decoy before pagination, drives target-specific portal UI evidence, and removes both temporary mailbox rows during cleanup.

Slice 514 adds no permanent seed records. It creates a cleanup-backed sent secure-message row and a cleanup-backed archived inbox row for `MOD-PAT-0004`, verifies Sent, All, and Deleted folder search projections, drives modernized rendered folder-search evidence, records the legacy no-input Sent/Archive baseline, and removes both temporary mailbox rows during cleanup.

Slice 515 adds no permanent seed records. It creates cleanup-backed Inbox, Sent, and archived Deleted secure-message rows for `MOD-PAT-0004`, verifies that the shared no-hit search query returns zero matches across Inbox, Sent, All, and Deleted folder projections, drives modernized empty-search UI evidence, records the legacy no-active-search-input baseline, and removes all temporary mailbox rows during cleanup.

Slice 516 adds no permanent seed records. It creates cleanup-backed Inbox, Sent, and archived Deleted secure-message rows for `MOD-PAT-0004`, verifies shared filtered/total search-count projections across Inbox, Sent, All, and Deleted folders, drives modernized search-count summary evidence, records the legacy no-active-search-input baseline, and removes all temporary mailbox rows during cleanup.

Slice 517 adds no permanent seed records. It creates cleanup-backed Inbox secure-message rows for `MOD-PAT-0004`, verifies a search query narrows the shared projection to the matching row and that clearing the query restores both rows, drives modernized Clear search UI evidence, records the legacy no-active-clear-search-control baseline, and removes the temporary mailbox rows during cleanup.

Slice 518 adds no permanent seed records. It creates cleanup-backed matching and decoy Inbox secure-message rows for `MOD-PAT-0004`, verifies padded mixed-case search text is trimmed and matched case-insensitively against the shared projection, drives modernized live search-summary UI evidence, records the legacy no-active-search-summary baseline, and removes the temporary mailbox rows during cleanup.

Slice 519 adds no permanent seed records. It creates cleanup-backed matching and selected-then-hidden Inbox secure-message rows for `MOD-PAT-0004`, verifies the search query hides the selected row from the shared projection, drives modernized selection-reset UI evidence, records the legacy no-active-search-filter baseline, and removes the temporary mailbox rows during cleanup.

Slice 520 adds no permanent seed records. It creates cleanup-backed matching and hidden unread Inbox secure-message rows for `MOD-PAT-0004`, verifies the search query hides one unread row from the shared projection, drives modernized filtered mark-all-read UI evidence, records the legacy no-active-search-filter baseline, and removes the temporary mailbox rows during cleanup.

Slice 521 adds no permanent seed records. It creates cleanup-backed newer nonmatching Inbox rows plus 21 matching Inbox rows for `MOD-PAT-0004`, verifies search-before-pagination projections over the temporary mailbox rows, drives modernized search pagination reset UI evidence, records the legacy no-active-search-input baseline, and removes the temporary mailbox rows during cleanup.

Slice 522 adds no permanent seed records. It creates a cleanup-backed appointment for `MOD-PAT-0004`, converts it into an encounter while preserving schedule context and source appointment linkage, then deletes the temporary encounter before deleting the temporary appointment so both targets return to their starting workflow counts.

Slice 523 adds no permanent seed records. It creates a cleanup-backed appointment for `MOD-PAT-0004`, converts it into an encounter, creates a starter `CPT4 99213` fee-sheet line on that converted encounter, verifies the active converted-charge count in the modernized appointment read model, then deletes the temporary billing line before deleting the temporary encounter and appointment so both targets return to their starting workflow counts.

Slice 524 adds no permanent seed records. It uses the existing `MOD-PAT-0001` billing anchor encounter, applies the office-visit charge template in the modernized Fees page, creates a temporary `CPT4 99213` fee-sheet line with the same payload on both targets, then deletes the temporary billing line so both targets return to their starting billing-line counts.

Slice 525 adds no permanent seed records. It uses the existing `MOD-PAT-0005` patient-payment billing anchor encounter, creates a temporary patient-responsibility cash payment on both targets, derives a deterministic receipt number from the patient public ID, post date, and payment sequence, verifies the modernized receipt PDF and Fees-row `Receipt` action, then voids and deletes the temporary payment so both targets return to their starting payment-session and payment-activity counts.

Slice 526 adds no permanent seed records. It uses the existing `MOD-PAT-0005` patient-payment billing anchor encounter, creates a temporary `patient_refund` row with a negative patient payment amount on both targets, verifies net payment decreases and balance increases by the refund amount, checks the modernized Fees refund row and statement-line rendering, then deletes the temporary refund so both targets return to their starting payment-session and payment-activity counts.

Slice 527 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter and the seeded Northstar HMO payer, creates a temporary `insurance_reversal` row with a negative insurer-backed payment amount on both targets, verifies payer identity is retained, net payment decreases and balance increases by the reversal amount, checks the modernized Fees reversal row rendering, then deletes the temporary reversal so both targets return to their starting payment-session and payment-activity counts.

Slice 528 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter and the seeded Northstar HMO payer, creates a temporary `adjustment_reversal` row with no payment amount and a negative adjustment amount on both targets, verifies payer identity is retained, net adjustment decreases and balance increases by the reversal amount, checks the modernized Fees adjustment-reversal row rendering, then deletes the temporary reversal so both targets return to their starting payment-session and payment-activity counts.

Slice 529 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary queued HCFA claim on both targets, updates that row to denied status with X12 denial response metadata, verifies the denial file, submitted-claim payload, payer identity, status label, and claim-count increment, checks the modernized Fees denial rendering, then hard-deletes the temporary claim so both targets return to their starting claim counts.

Slice 530 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary generated X12 claim on both targets, adjudicates it with a temporary insurer EOB payment plus contractual adjustment, verifies payer claim number, reason code, payment/adjustment balance movement, ledger rows, and modernized Fees adjudication rendering, then deletes the temporary payment and claim so both targets return to their starting claim, payment-session, and payment-activity counts.

Slice 531 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, imports a deterministic temporary two-line EOB batch (`EOB-BATCH-1000052-PRIMARY` and `EOB-BATCH-1000052-SECONDARY`) on the modernized Fees page and creates the equivalent payment postings on the legacy target, verifies payer identity, electronic payment method, payer claim numbers, reason codes, payment/adjustment balance movement, and ledger rows, then deletes the temporary payment rows so both targets return to their starting payment-session and payment-activity counts.

Slice 532 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary queued claim with no submitted payload, generates a deterministic 837P-style process file and submitted-claim payload through the modernized Fees `Generate` action or the equivalent legacy claim update, verifies X12 target metadata, claim-file naming, key 837P segments, and modernized claim rendering, then deletes the temporary claim so both targets return to their starting claim counts.

Slice 533 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary queued claim, writes a deterministic local scrub report from active CPT lines, payer presence, diagnosis pointers, fees, and units through the modernized Fees `Scrub` action or the equivalent legacy claim update, verifies PASS/no-issue metadata, process-file naming, modernized claim rendering, and cleanup, then deletes the temporary claim so both targets return to their starting claim counts.

Slice 534 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary denied X12 claim, requeues it through the modernized Fees `Resubmit` action or the equivalent legacy claim update, verifies queued status, X12 target metadata, deterministic resubmission process-file naming, compact resubmission payload content, modernized claim rendering, and cleanup, then deletes the temporary claim so both targets return to their starting claim counts.

Slice 535 adds no permanent seed records. It uses the existing `MOD-PAT-0001` billing anchor and latest seeded encounter, applies the modernized Fees `Complex follow-up` charge template or creates the equivalent legacy CPT line, verifies CPT `99214`, modifier `25`, two units, fee `210.00`, diagnosis pointer `K21.9`, count movement, modernized form/rendering behavior, and cleanup, then deletes the temporary billing line so both targets return to their starting billing-line counts.

Slice 536 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary CPT line with unsupported modifier `ZZ` plus a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `invalid-modifier:ZZ` report metadata, process-file naming, modernized claim rendering, claim-count and billing-line-count movement, and cleanup, then deletes both temporary rows so both targets return to their starting counts.

Slice 537 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary ICD10 diagnosis line for `K21.9`, a temporary CPT line with unsupported diagnosis pointer `Q99.99`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `invalid-diagnosis-pointer:Q99.99` report metadata, process-file naming, modernized claim rendering, claim-count and billing-line-count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 538 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary ICD10 diagnosis line for `K21.9`, a temporary CPT line with duplicate supported modifier text `25,25`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `duplicate-modifier:25` report metadata without misclassifying `25,25` as an unsupported modifier, verifies process-file naming, modernized claim rendering, claim-count and billing-line-count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 539 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary ICD10 diagnosis line for `K21.9`, a temporary CPT line with five supported modifiers encoded as compact modifier text `2559767795`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `modifier-count-exceeded:5` report metadata without invalid- or duplicate-modifier misclassification, verifies process-file naming, modernized claim rendering, claim-count and billing-line-count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 540 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary encounter plus CPT line with diagnosis pointer `K21.9` and a temporary queued claim without creating an ICD10 diagnosis line, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `missing-diagnosis-code` report metadata without missing-pointer or invalid-pointer misclassification, verifies process-file naming, modernized claim rendering, claim-count and billing-line-count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 541 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary encounter with ICD10 diagnosis lines for `K21.9` and `E11.9`, a CPT line justified by comma-separated diagnosis pointer text `K21.9,E11.9`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-PASS` report metadata with `issues=none`, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 542 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary encounter with ICD10 diagnosis lines for `K21.9`, `E11.9`, `I10`, `J45.909`, and `M54.50`, a CPT line justified by all five comma-separated diagnosis pointers, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `diagnosis-pointer-count-exceeded:5` report metadata without invalid-pointer or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 543 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary encounter with one ICD10 diagnosis line for `K21.9`, a CPT line justified by duplicate diagnosis pointer text `K21.9,K21.9`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `duplicate-diagnosis-pointer:K21.9` report metadata without invalid-pointer, missing-code, or pointer-count misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 544 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary encounter with two active ICD10 diagnosis lines for `K21.9`, a CPT line justified by `K21.9`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `duplicate-diagnosis-code:K21.9` report metadata without invalid-pointer, missing-code, or duplicate-pointer misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 545 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary encounter with one ICD10 diagnosis line for `K21.9`, a malformed CPT4 service line with code `99A14`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `invalid-cpt-code:99A14` report metadata without diagnosis-pointer or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 546 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary encounter with malformed ICD10 diagnosis code `BADCODE`, a valid CPT4 `99214` service line justified by that same diagnosis, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `invalid-diagnosis-code:BADCODE` report metadata without diagnosis-pointer, missing-code, or CPT-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 547 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary future-dated encounter and CPT4 `99214` service line on `2026-12-15` with valid ICD10 diagnosis support, and a temporary queued claim with the fixed claim-scrub business date `2026-06-18`, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `future-service-date:2026-12-15` report metadata without CPT-code, diagnosis-code, diagnosis-pointer, or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 548 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary same-day encounter with valid ICD10 diagnosis `K21.9`, a valid CPT4 `99214` service line carrying supported-but-incompatible modifier text `25,59`, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `incompatible-modifier-combination:25+59` report metadata without invalid-modifier, duplicate-modifier, modifier-count, CPT-code, diagnosis-pointer, or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 549 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor, creates a temporary same-day encounter with valid ICD10 diagnosis `K21.9`, a valid CPT4 `99214` service line carrying supported modifier text `25`, positive fee and units, and a temporary queued claim with `payerId` `0` and blank payer name, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `missing-payer` report metadata without CPT-code, diagnosis-code, diagnosis-pointer, modifier, fee, unit, future-date, or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 550 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary same-day encounter with valid ICD10 diagnosis `K21.9`, a valid CPT4 `99214` service line carrying supported modifier text `25`, zero CPT fee, positive units, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `invalid-fee` report metadata without payer, CPT-code, diagnosis-code, diagnosis-pointer, modifier, unit, future-date, or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 551 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary same-day encounter with valid ICD10 diagnosis `K21.9`, a valid CPT4 `99214` service line carrying supported modifier text `25`, positive fee, zero CPT units, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `invalid-units` report metadata without payer, fee, CPT-code, diagnosis-code, diagnosis-pointer, modifier, future-date, or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 552 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary same-day encounter with valid ICD10 diagnosis `K21.9`, intentionally creates no CPT4 service line, and queues a temporary Northstar HMO claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `missing-cpt-line` report metadata without payer, fee, unit, CPT-code, diagnosis-code, diagnosis-pointer, modifier, future-date, or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/single-diagnosis-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 553 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary same-day encounter with valid ICD10 diagnosis `K21.9`, a valid CPT4 `99214` service line carrying supported modifier text `25`, positive fee and units, but a blank CPT diagnosis pointer, and a temporary queued claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `missing-diagnosis-pointer` report metadata without payer, CPT-line, fee, unit, CPT-code, diagnosis-code, invalid-pointer, modifier, future-date, or missing-code misclassification, verifies process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 554 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary same-day encounter with valid ICD10 diagnosis `K21.9`, creates a valid CPT4 `99214` service line carrying supported modifier text `25`, positive fee and units, marks that CPT row billed/inactive before claim scrubbing, and queues a temporary Northstar HMO claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `missing-cpt-line` report metadata from the active-line projection without payer, fee, unit, CPT-code, diagnosis-code, diagnosis-pointer, modifier, future-date, or missing-code misclassification, verifies inactive-row status evidence, process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 555 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor and seeded Northstar HMO payer, creates a temporary same-day encounter with ICD10 diagnosis `K21.9`, marks that diagnosis row billed/inactive before claim scrubbing, creates a valid active CPT4 `99214` service line carrying supported modifier text `25`, positive fee and units, and diagnosis pointer `K21.9`, queues a temporary Northstar HMO claim, runs the modernized Fees `Scrub` action or equivalent legacy claim update, verifies deterministic `SCRUB-FAIL` / `missing-diagnosis-code` report metadata from the active-line projection without payer, CPT-line, fee, unit, CPT-code, diagnosis-pointer, modifier, future-date, or invalid-pointer misclassification, verifies inactive-row status evidence, process-file naming, modernized claim rendering, encounter/claim/billing-line count movement, and cleanup, then deletes all temporary rows so both targets return to their starting counts.

Slice 556 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052`, active seeded billing lines, and seeded Northstar HMO payer, creates a temporary queued claim, drives the modernized server-side claim scrub operation through the Fees `Scrub` action or applies the equivalent legacy claim update, verifies deterministic `SCRUB-PASS` / `issues=none` report metadata, process-file naming, modernized claim rendering, claim-count movement, and cleanup, then deletes the temporary claim so both targets return to their starting counts.

Slice 557 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary queued claim, drives the modernized server-side claim generation operation through the Fees `Generate` action or applies the equivalent legacy claim update, verifies deterministic 837P-style process-file naming, X12 target metadata, submitted-claim payload content, modernized claim rendering, claim-count movement, and cleanup, then deletes the temporary claim so both targets return to their starting counts.

Slice 558 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary denied claim, drives the modernized server-side claim resubmission operation through the Fees `Resubmit` action or applies the equivalent legacy claim update, verifies deterministic X12 resubmission process-file naming, queued status, submitted-claim payload content, modernized claim rendering, claim-count movement, and cleanup, then deletes the temporary claim so both targets return to their starting counts.

Slice 559 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary queued claim with a deterministic denial response file seed, drives the modernized server-side claim denial operation through the Fees `Deny` action or applies the equivalent legacy claim update, verifies deterministic X12 denial metadata, denied status, submitted-claim fallback content, modernized claim rendering, claim-count movement, and cleanup, then deletes the temporary claim so both targets return to their starting counts.

Slice 560 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary denied claim with deterministic X12 response metadata, drives the modernized server-side claim clearing operation through the Fees `Clear` action or applies the equivalent legacy claim update, verifies HCFA cleared-status metadata, blank process-file state, submitted-claim fallback content, modernized claim rendering, claim-count movement, and cleanup, then deletes the temporary claim so both targets return to their starting counts.

Slice 561 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, creates a temporary generated claim, drives the modernized server-side claim adjudication operation through the Fees `Adjudicate` action or applies the equivalent legacy payment-plus-claim update, verifies linked EOB payment and contractual adjustment rows, payer-claim number, X12 835 process-file metadata, account balance and ledger movement, modernized claim/payment rendering, and cleanup, then deletes the temporary payment and claim so both targets return to their starting counts.

Slice 562 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, drives the modernized server-side EOB batch import operation through the Fees `Import EOB` action or creates the equivalent legacy payment postings, verifies the deterministic two-line remittance batch, payer claim numbers, reason codes, account balance and ledger movement, modernized payment rendering, and cleanup, then deletes the temporary payment rows so both targets return to their starting payment-session and payment-activity counts.

Slice 563 adds no permanent seed records. It uses the existing `MOD-PAT-0001` fee-sheet billing anchor and latest seeded encounter, drives the modernized server-side charge-template lookup through the Fees `Office visit` and `Complex follow-up` template buttons or creates equivalent legacy CPT rows, verifies the template CPT, modifier, fee, units, and justification payloads, then deletes the temporary billing lines so both targets return to their starting billing-line counts.

Slice 564 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052`, drives the modernized server-side patient-refund posting operation through the Fees `Patient refund` mode or creates the equivalent legacy refund posting, verifies the negative `patient_refund` payment, patient payer identity, zero adjustment, account balance and ledger movement, modernized refund rendering, and cleanup, then deletes the temporary refund row so both targets return to their starting payment-session and payment-activity counts.

Slice 565 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, drives the modernized server-side insurance-reversal posting operation through the Fees `Insurance reversal` mode or creates the equivalent legacy insurer-backed reversal posting, verifies the negative `insurance_reversal` payment, payer identity, zero adjustment, payer claim number, account balance and ledger movement, modernized reversal rendering, and cleanup, then deletes the temporary reversal row so both targets return to their starting payment-session and payment-activity counts.

Slice 566 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, drives the modernized server-side adjustment-reversal posting operation through the Fees `Adjustment reversal` mode or creates the equivalent legacy insurer-backed adjustment reversal posting, verifies zero payment amount, negative `adjustment_reversal` adjustment, payer identity, payer claim number, account balance and ledger movement, modernized reversal rendering, and cleanup, then deletes the temporary reversal row so both targets return to their starting payment-session and payment-activity counts.

Slice 567 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052` and seeded Northstar HMO payer, drives the modernized server-side insurance-payment posting operation through the Fees `Insurance` payment source or creates the equivalent legacy insurer-backed payment posting, verifies positive `insurance_payment` payment amount, non-negative contractual adjustment, reason/account-code handling, payer identity, payer claim number, account balance and ledger movement, void rollback, modernized payment rendering, and cleanup, then deletes the temporary payment row so both targets return to their starting payment-session and payment-activity counts.

Slice 568 adds no permanent seed records. It uses the existing `MOD-PAT-0005` billing anchor encounter `1000052`, drives the modernized server-side patient-payment posting operation through the Fees `Patient` payment source or creates the equivalent legacy patient-responsibility payment posting, verifies positive `patient_payment` payment amount, patient payer identity, zero adjustment, blank reason/account codes, blank payer claim number, account balance and ledger movement, void rollback, modernized patient-payment rendering, and cleanup, then deletes the temporary payment row so both targets return to their starting payment-session and payment-activity counts.

Slice 569 adds no permanent seed records. It reuses the existing billing mutation authorization-policy anchors and temporary payment/claim rows, updates modernized payment creation helpers to call focused payment endpoints, and verifies the retired broad payment-create route without inserting a payment row. Cleanup behavior remains owned by the existing billing mutation authorization-policy suite.

Slice 570 adds no permanent seed records. It reuses the existing ranked statement batch candidates, statement summaries, patient email values, and mailing-address fields to prepare deterministic delivery handoff metadata for the top five statement candidates. The plan verifies queued status, delivery method, destination, file name, balance, due date, and aggregate email-ready/print-ready counts without sending email, printing, mailing, or mutating delivery state.

Slice 571 adds no permanent seed records. It reuses the Slice 570 delivery manifest projection and derives deterministic local dispatch handoff rows for the same top five statement candidates. The plan verifies dispatch ID, dispatch timestamp, audit IDs, email/print queue names, dispatch statuses, local external references, destinations, and file names without sending email, printing, mailing, or inserting durable delivery-audit rows.

Slice 572 adds an empty modernized operational audit table, `statement_delivery_audit_events`, to the generated PostgreSQL seed and no permanent dispatch events. It reuses the Slice 571 handoff projection, persists the top-five deterministic local dispatch rows during the test run, verifies the saved audit history through API and UI, and relies on run-level reset to return the table to its empty seeded state.

Slice 573 adds no permanent seed records. It reuses the existing top-five ranked statement batch candidates and the login-ready portal statement recipient `MOD-PAT-0200`; the earlier top portal-enabled candidates `MOD-PAT-0197` through `MOD-PAT-0199` remain pending password setup and are skipped by the delivery rule. The modernized test publishes an idempotent `portal-statement-{statementNumber}` PDF document into the existing `Invoices` patient-document category and relies on run-level reset to return the generated portal document to its unseeded state.

Slice 574 adds no permanent seed records. It reuses the existing top-five ranked statement batch candidates, their deterministic patient email addresses, statement summaries, and statement PDF file names to create idempotent local `statement_email_outbox` rows during the modernized test run. The outbox message IDs follow `EMAIL-STMT-EMAIL-{YYYYMMDD}-TOP{count}-{sequence}`, and run-level reset plus idempotent upsert behavior keep the local outbox evidence repeatable without sending SMTP mail or storing production delivery credentials.

Slice 575 adds no permanent seed records. It uses existing scheduling anchors `MOD-PAT-0003` and `MOD-PAT-0004`, creates a temporary blocker appointment on `2026-12-29` at 10:00 in room `Availability`, validates that a second draft appointment reports provider and room conflicts, validates that a Saturday 07:00 draft is outside the shared provider/facility bookable window, and deletes the temporary blocker so both targets return to their starting appointment state.

Slice 576 adds no permanent seed records. It uses the existing portal-enabled `MOD-PAT-0004` account (`mod-pat-0004@example.test` / `PortalPass207!`), creates a temporary portal appointment request on `2026-09-23` at 09:30 with a unique reason, verifies the resulting waiting appointment (`status = '^'`) and linked provider reminder, promotes the same appointment to OpenEMR pending status (`~`), and deletes both the temporary appointment and reminder so both targets return to their starting portal appointment state.

Slice 577 adds no permanent seed records. It uses the existing reminder-readiness anchor `MOD-PAT-0191` and appointment `APPT-MOD-PAT-0191-3` on `2026-06-25`, derives the due reminder from the shared `2026-06-18` business date, dispatches an idempotent local SMS/email reminder on the modernized target, persists the audit row in `appointment_reminder_dispatch_audit`, and relies on run-level reset plus deterministic upsert behavior to return the local dispatch table to its empty seeded state.

Slice 578 adds no permanent seed records. It uses the existing clinical-list anchor `MOD-PAT-0006`, creates two temporary active medication-list rows dated `2026-08-03` and `2026-08-04` with the same normalized medication title `DUPLICATE SAFETY MEDICATION 578`, verifies duplicate count, source IDs, first/latest dates, diagnosis evidence, and modernized Lists rendering, then deletes both temporary rows so both targets return to their starting medication-list state.

Slice 579 adds no permanent seed records. It uses the existing prescription mutation anchor `MOD-PAT-0008`, creates a temporary active prescription on `2026-08-01` with one refill, authorizes one additional refill on `2026-08-20`, verifies the prescription remains active with no end date, two refills, updated modified-date, refill note evidence, and modernized Lists rendering, then deletes the temporary prescription so both targets return to their starting prescription state.

Slice 580 adds no permanent seed records. It uses the existing portal-enabled account `MOD-PAT-0004` (`mod-pat-0004@example.test` / `PortalPass207!`), creates a temporary active prescription on `2026-08-01`, submits a patient-originated refill request dated `2026-08-20`, verifies the secure-message mailbox title/body/sender/recipient/status and `portal:prescription-refill-request` relation metadata, then deletes both the temporary prescription and refill-request mailbox rows so both targets return to their starting prescription and portal-message state.

Slice 581 adds no permanent seed records. It uses the existing portal-enabled account `MOD-PAT-0004` (`mod-pat-0004@example.test` / `PortalPass207!`), creates a temporary active prescription on `2026-08-01`, submits a portal refill request dated `2026-08-20`, verifies that staff can see the request in the clinical-list refill queue, approves one additional refill with deterministic note evidence, confirms the mailbox request is no longer pending, then deletes the temporary prescription and mailbox rows so both targets return to their starting state.

Slice 582 adds a small permanent modernized PostgreSQL pharmacy catalog (`Northstar Community Pharmacy`, `Summit Mail Order Pharmacy`, and `Lakeside 24 Hour Pharmacy`) plus prescription routing/transmit columns so reseeded modernized databases can support pharmacy routing. The parity plan uses deterministic Northstar pharmacy ID `9001`; legacy creates and deletes the matching temporary pharmacy fixture because the local legacy seed has no pharmacy rows, while both targets create a temporary active prescription on `MOD-PAT-0008`, route it on `2026-08-21 09:15:00`, verify pharmacy ID/name/NCPDP, note, sent timestamp, and local eRx payload evidence, and delete the temporary prescription so both targets return to their starting prescription state.

Slice 583 adds no permanent seed records. It reuses `MOD-PAT-0008` and deterministic Northstar pharmacy ID `9001`, creates a temporary active Oxycodone prescription, verifies ordinary pharmacy routing is blocked before pharmacy/eRx fields are stamped, checks the modernized Lists EPCS-review warning, and deletes the temporary prescription plus legacy-only pharmacy fixture so both targets return to their starting state.

Slice 584 adds the modernized `prescription_audit_events` table but no permanent seed records. It reuses `MOD-PAT-0008` and deterministic Northstar pharmacy ID `9001`, creates a temporary active prescription, refills it once, routes it to Northstar pharmacy evidence, verifies create/refill/route audit history, checks the modernized Lists `History` rendering, and deletes the temporary prescription plus legacy-only pharmacy fixture so both targets return to their starting prescription state.

Slice 585 adds structured prescription dose fields to the modernized PostgreSQL seed schema but no permanent seed records. It reuses `MOD-PAT-0008`, creates a temporary active prescription with dose amount `10`, dose unit `mg`, frequency `twice daily`, and duration `14` days, verifies those fields through the shared workflow adapter, checks the modernized Lists card rendering, and deletes the temporary prescription so both targets return to their starting prescription state.

Slice 586 adds a focused deterministic `medication_vocabulary` seed table for the modernized PostgreSQL target and mirrors the same catalog in the legacy parity adapter. It reuses `MOD-PAT-0008`, searches for Metformin, verifies RxNorm `860975`, strength, dose, frequency, duration, and controlled-substance schedule evidence, checks the modernized Lists prescription-form search/select behavior, and creates no permanent patient-specific records.

Slice 587 adds no permanent seed records. It reuses `MOD-PAT-0008`, creates a temporary active problem with diagnosis `ICD10:Z58.6`, creates one temporary prescription with that same diagnosis, creates one temporary prescription with unmatched diagnosis `ICD10:Z76.89`, verifies matched and unmatched prescription diagnosis interaction evidence through legacy and modernized source facts plus modernized API/UI rendering, and deletes all temporary rows.

Slice 588 adds no permanent seed records. It reuses `MOD-PAT-0008`, creates two temporary medication-list rows and two temporary prescriptions to prove matched, medication-list-only, and prescription-only reconciliation states, verifies normalized active medication-list and prescription source facts through legacy and modernized databases plus modernized API/UI rendering, and deletes all temporary rows.

Slice 589 adds no permanent seed records. It reuses `MOD-PAT-0003` and `MOD-PAT-0004`, creates one temporary blocker appointment with a shared provider and room, verifies provider/room conflict source facts on both targets, verifies strict modernized appointment creation rejects the overlapping request without inserting a row, and deletes the blocker appointment during cleanup.

Slice 590 adds no permanent seed records. It reuses `MOD-PAT-0001`, creates one temporary scanned PDF document with `OCR pending` metadata and a deterministic capture source, verifies scanned pending-OCR source facts on both targets, verifies the modernized protected OCR queue API and Documents OCR Queue rendering, and deletes the temporary document during cleanup.

Slice 591 adds no permanent seed records. It reuses `MOD-PAT-0001`, creates one temporary scanned PDF document with `OCR pending` metadata, verifies pending-OCR source facts on both targets, completes OCR on the modernized target, verifies the document moves to `OCR complete` and leaves the OCR queue, and deletes the temporary document during cleanup.

Slice 592 adds no permanent seed records. It reuses `MOD-PAT-0001`, creates one temporary database-backed PDF document, verifies PDF preview/source facts on both targets, verifies the modernized document API generates a deterministic SVG thumbnail data URI and the Documents card renders it, then archives and hard-deletes the temporary document during cleanup.

Slice 207 adds deterministic synthetic portal credentials to the existing 200 provisioned portal accounts without changing counts. The shared credential password is `PortalPass207!`; legacy OpenEMR receives a real bcrypt hash in `patient_access_onsite.portal_pwd`, the generated legacy SQL upserts `portal_onsite_two_enable = 1` and `portal_onsite_two_address = http://localhost:8080/portal` in `globals`, and the modernized seed maps the same synthetic password through `patient_portal_accounts.password_salt` / `password_hash`. The `MOD-PAT-0004` anchor uses `mod-pat-0004@example.test` for authentication parity, while the test temporarily toggles disabled access and pending reset state before restoring the account. Slice 208 adds no permanent seed records; its modernized side inserts and ends transient `patient_portal_sessions` rows while the legacy side verifies browser-cookie portal logout behavior. Slice 209 adds no permanent seed records; both targets read existing messages and future appointments through an authenticated portal-home view.

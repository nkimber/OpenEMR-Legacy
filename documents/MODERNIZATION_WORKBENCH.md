# Modernization Workbench

Created: 2026-06-18
First implemented: 2026-06-18

## Purpose

The Modernization Workbench is the third application in this project. It is an oversight and orchestration website for managing the modernization of OpenEMR from the legacy baseline into the modernized target solution.

The workbench should make the modernization effort observable. A user should be able to open it and understand what exists, what is running, what has been tested, which workflows have been modernized, how the two systems compare, and what evidence supports the current state.

## Current Implementation

The first version is implemented in `modernization-workbench/`.

Technology stack:

- React.
- TypeScript.
- Vite.
- Node.js.
- Express.
- Docker Compose command orchestration.

Run it from the repository root:

```powershell
.\scripts\Start-ModernizationWorkbench.ps1
```

Workbench URLs:

- UI: `http://127.0.0.1:5173`
- API: `http://127.0.0.1:5174`

The Workbench currently manages the legacy OpenEMR baseline and the modernized OpenEMR target as it grows slice by slice. It now uses a left-side application shell with hash-routed pages. It can show status, check health, start, stop, restart, start all configured Docker Compose app stacks, surface Docker Desktop and compose readiness guidance on the Dashboard and Applications pages, run smoke tests, run OpenEMR-native PHPUnit and Jest tests for the legacy target, run parity test suites and plans for implemented targets, run custom parity runs with selected reset strategy, render recent side-by-side comparison artifacts with expandable drill-ins, safe artifact links, direct run/report links, screenshot thumbnails, normalized probe detail views, safe text-like probe attachment previews, path-backed database contract plus Slice 1 through Slice 142 plus Slices 144, 145, 147, 148, 149, 151, 153, 154, 156, 157, 158, 159, 160, 161, 162, 163, 164, 165, 166, 167, 168, 169, 170, 171, 172, 173, 174, 175, 176, 177, 178, 179, 180, 181, 182, 183, 184, 185, 186, 187, 188, 189, 190, 191, 192, 193, 194, 195, 196, 197, 198, 199, 200, 201, 202, 203, 204, 205, 206, 207, 208, 209, 210, 211, 212, 213, 214, 215, 216, 217, 218, 219, 220, 221, 222, 223, 224, 225, 226, 227, 228, and 229 workflow probe payloads, accepted-difference status, and reliability trend summaries, run gold seed actions, run the starter seed action for legacy, display latest smoke-test, native-test, parity-test, and seed results, show Docker Compose logs, display database profiles, list action history, render the project changelog as a build timeline with timing and code-change metrics, and show architecture/progress views. The Progress page now renders both delivery milestone rows and a curated functionality progress ledger that separates completed, outstanding, and deferred project scope by domain area, with scope-adjusted completion estimates for each area. The Architecture page now presents a tabbed model with a versioned stack matrix, project topology map, architecture decisions, source inventory statistics, and per-system detail views for the legacy baseline, Workbench, and modernized target.

The Workbench frontend uses route-scoped hydration so the shell can show managed application status quickly without waiting for every historical and evidence-heavy dataset. Initial load fetches the operational app, event, and seed-data snapshot; the Dashboard then hydrates progress and timeline summaries independently; the Project Timeline, Progress, Architecture, and Test Runs pages fetch their heavier datasets when those pages are active. The app snapshot API is also split into lightweight summaries and rich details: `/api/apps` returns the runtime, health, source, and container summary without latest run artifacts, database profiles, or large test/seed definitions, while `/api/apps/:appId` hydrates those details for active pages. The recurring 15-second refresh polls only app snapshots and lifecycle events, and the client preserves already-loaded detail fields when summary refreshes arrive. The header refresh button reloads the current page plus the operational snapshot.

The Project Timeline uses paged changelog hydration so the growing project history does not block the rest of the Workbench. `/api/changelog/summary` serves the Dashboard with a compact parsed-only count and latest-entry summary, while `/api/changelog` returns the newest 100 rich timeline entries by default, includes `hasMore` and `nextOffset` pagination metadata, accepts explicit `offset`, `limit`, and `order` query parameters, and caches the parsed changelog plus one shared bounded Git-enrichment build in memory until `documents/PROJECT_CHANGELOG.md` changes. The Timeline page appends additional 100-entry pages only when the operator asks to load more.

The Progress page also includes curated Capability Rollups from `modernization-workbench/config/capability-rollups.json`. These rollups sit above the granular functionality ledger and group many small slice plans into parent capability families such as Patient Chart and Portal, Scheduling and Appointments, Encounters, Documents, Revenue Cycle, Labs and Procedures, Administration/Security, Workbench Evidence, and Seed Data/Production Readiness. Each rollup classifies child work as capability, coverage, evidence, Workbench platform, seed-data, or hardening so operators can understand project progress without treating every micro-slice as a separate product milestone.

Modern UI Claude is also registered as a managed frontend-only Docker Compose application. The Applications page shows the `modern-ui-claude` service status, checks `http://localhost:3100/`, exposes Start/Stop/Restart/Open/Logs actions, and keeps its database profile separate by pointing operators back to the modernized OpenEMR backend app for database state. Its compose stack is configured for Vite live reload by bind-mounting the app folder into the container; source edits should not require a rebuild unless dependencies or container configuration change.

Current pages:

- Dashboard.
- Applications.
- Project Timeline.
- Progress.
- Architecture.
- Test Runs.
- Seed Data.

The navigation model supports nested child items so the Workbench can grow into two-level navigation later without reworking the shell.

The Project Timeline page reads `documents/PROJECT_CHANGELOG.md` as the source of truth for project history. It displays explicit start time, finish time, and duration when entries include `Started:` and `Finished:` metadata. The primary date label for each timeline entry is derived from `Finished:` first, then Git completion time, then `Started:`, with the changelog section date used only as a fallback for older untimed entries. It also displays code-change metrics for files changed, lines added, lines deleted, net lines, and total churn. New changelog entries should record those values in a `Code changes:` section and replace `Commit: pending` with the real Git changeset ID once the commit exists. For older entries, the Workbench backfills code-change metrics and the visible changeset chip from Git when the entry contains a resolvable commit hash, and it uses conservative commit-subject matching for older entries whose commit field says `this commit` or `current slice commit`. If no safe match exists, the chip shows an unresolved changeset instead of repeating the placeholder as though it were a valid commit ID.

The Timeline page initially renders the most recent 100 entries, shows the total available and currently loaded counts, and exposes a load-more action when additional history is available. Load-more pages reuse the same bounded enrichment cache instead of increasing Git history scans as the operator pages deeper.

Slice 143 adds the functionality progress ledger to the Progress page. Slice 146 adds explicit scope-adjusted completion estimates to that ledger. Slice 152 adds weighted overall progress, Git-backed progress history, and rough remaining-time forecasting. Slice 155 adds direct comparison-side links to run JSON, Playwright JSON, JUnit XML, and HTML reports. Slice 256 adds screenshot thumbnail metadata and previews for comparison-side visual artifacts. Slice 257 adds normalized Playwright probe detail extraction and drill-in rendering for comparison-side runs. Slice 258 adds curated accepted-difference tracking from `modernization-workbench/config/accepted-differences.json`, with accepted/unreviewed counts on comparison cards and drill-ins. Slice 259 adds reliability trend summaries from stored parity run and comparison artifacts, including rolling pass rates, average durations, recent pass/fail strips, and selection-level comparison summaries. Slice 260 adds safe text-like probe attachment previews in comparison drill-ins while leaving binary artifacts as links/metadata. Slice 261 makes the database gold-seed contract suite emit path-backed JSON probe attachments for count, temporal coverage, anchor-patient, and related-record checks so those previews have durable database payloads. Slice 262 extends the same payload pattern into the Slice 1 patient search/chart summary workflow suite, attaching anchor demographics, workflow counts, and UI-steering patient lookup facts. Slice 263 extends the payload pattern into the Slice 2 scheduling workflow suite, attaching the future appointment anchor and UI-steering appointment facts. Slice 264 extends the payload pattern into the Slice 3 encounter clinical detail workflow suite, attaching the anchor patient, latest encounter, SOAP/vitals facts, and UI-steering precondition. Slice 265 extends the payload pattern into the Slice 4 clinical lists workflow suite, attaching the anchor patient, problem/allergy/medication/prescription facts, and UI-steering precondition. Slice 266 extends the payload pattern into the Slice 5 messaging workflow suite, attaching the anchor patient, portal access, seeded message facts, and UI-steering precondition. Slice 267 extends the payload pattern into the Slice 6 procedure results workflow suite, attaching the anchor patient, completed CBC order, completed report, final Hemoglobin result, and UI-steering precondition. Slice 268 extends the payload pattern into the Slice 7 fee-sheet billing workflow suite, attaching the anchor patient, latest encounter, seeded CPT fee-sheet charge lines, and UI-steering precondition. Slice 269 extends the payload pattern into the Slice 8 administration directory workflow suite, attaching seeded user counts, provider/calendar counts, selected provider and billing users, seeded facilities, and UI-steering precondition. Slice 270 extends the payload pattern into the Slice 9 operational reports workflow suite, attaching gold-data counts, selected provider activity, selected facility activity, selected clinical-condition summaries, and UI-steering precondition. Slice 271 extends the payload pattern into the Slice 10 patient contact mutation workflow suite, attaching the anchor patient, original contact state, proposed update, post-update state, and restored cleanup state. Slice 272 extends the payload pattern into the Slice 11 appointment mutation workflow suite, attaching the anchor patient, proposed future appointment, created appointment row, cancelled appointment row, and deleted cleanup state. Slice 273 extends the payload pattern into the Slice 12 encounter mutation workflow suite, attaching the anchor patient, proposed encounter/vitals/SOAP payloads, created encounter row, clinical-detail rows and count increments, updated encounter row, and deleted cleanup state. Slice 274 extends the payload pattern into the Slice 13 clinical-list allergy mutation workflow suite, attaching the anchor patient, proposed allergy payload, created allergy row and count increment, deactivated allergy row, and deleted cleanup state. Slice 275 extends the payload pattern into the Slice 14 patient-message mutation workflow suite, attaching the anchor patient, proposed message payload, created message row and count increment, closed message row, soft-deleted message row, and hard-delete cleanup state. Slice 276 extends the payload pattern into the Slice 15 prescription mutation workflow suite, attaching the anchor patient, proposed prescription payload, created prescription row and count increment, deactivated prescription row, and hard-delete cleanup state. Slice 277 extends the payload pattern into the Slice 16 billing mutation workflow suite, attaching the anchor patient, latest encounter, proposed CPT billing-line payload, created billing row and count increment, billed/inactive row state, and hard-delete cleanup state. Slice 278 extends the payload pattern into the Slice 17 procedure mutation workflow suite, attaching the anchor patient, proposed encounter/order/report/result payloads, created encounter and order count increments, completed order with final report/result rows, patient procedure summary projection, and hard-delete cleanup state. Slice 279 extends the payload pattern into the Slice 18 administration facility mutation workflow suite, attaching the proposed active facility payload, temporary facility count baseline/increment, created facility row, deactivated facility row, and hard-delete cleanup state. Slice 280 extends the payload pattern into the Slice 19 administration user mutation workflow suite, attaching the proposed active user payload, temporary user count baseline/increment, created user row, deactivated user row, and hard-delete cleanup state. Slice 281 extends the payload pattern into the Slice 20 administration access-control read-model suite, attaching ACL group, permission, group-permission assignment, user-membership counts and anchors plus UI-steering access-control matrix facts. Slice 282 extends the payload pattern into the Slice 21 administration access-permission mutation suite, attaching the focused Front Office demographics ACL assignment precondition, revoked state, restored state, and final cleanup restoration. Slice 283 extends the payload pattern into the Slice 22 administration user group membership mutation suite, attaching the proposed temporary user, created user before membership, granted Front Office membership, revoked membership, and final cleanup/deleted-user state. Slice 284 extends the payload pattern into the Slice 23 pending scheduled procedure-order suite, attaching the anchor patient, future scheduled CBC order, reportless order projection, and UI-steering precondition. Slice 285 extends the payload pattern into the Slice 24 operational reports CSV export suite, attaching the normalized export row set, selected CSV anchors, and UI/API export precondition. Slice 286 extends the payload pattern into the Slice 25 patient documents suite, attaching the anchor patient, filed document metadata, categories, storage mode, text previews, and UI-steering precondition. Slice 287 extends the payload pattern into the Slice 26 patient document mutation suite, attaching the anchor patient, proposed text document, created document row and count increment, archived row, and hard-delete cleanup state. Slice 288 extends the payload pattern into the Slice 27 patient document content suite, attaching the anchor patient, selected intake-packet metadata, full stored text content, UI-steering precondition, and modernized content/download API response facts. Slice 289 extends the payload pattern into the Slice 28 patient insurance coverage suite, attaching the `MOD-PAT-0005` anchor patient, normalized primary/secondary coverage facts, and UI-steering precondition. Slice 290 extends the payload pattern into the Slice 29 patient immunization history suite, attaching the `MOD-PAT-0007` pediatric anchor patient, normalized vaccine-history rows, 2026 influenza and Hep A anchors, and UI-steering precondition. Slice 291 extends the payload pattern into the Slice 30 patient immunization mutation suite, attaching the `MOD-PAT-0007` anchor patient, proposed temporary influenza immunization payload, created row and count increment, entered-in-error row, and hard-delete cleanup state. Slice 292 extends the payload pattern into the Slice 31 patient problem-list mutation suite, attaching the `MOD-PAT-0006` anchor patient, proposed temporary problem payload, created row and count increment, deactivated row, and hard-delete cleanup state. Slice 293 extends the payload pattern into the Slice 32 patient medication-list mutation suite, attaching the `MOD-PAT-0006` anchor patient, proposed temporary medication payload, created row and count increment, deactivated row, and hard-delete cleanup state. Slice 294 extends the payload pattern into the Slice 33 binary patient-document mutation suite, attaching the `MOD-PAT-0001` anchor patient, proposed temporary PDF binary document payload, created row and document-count increment, archived row, and hard-delete cleanup state. Slice 295 extends the payload pattern into the Slice 34 patient insurance mutation suite, attaching the `MOD-PAT-0005` anchor patient, baseline primary/secondary coverage, proposed temporary tertiary coverage payload, created row and coverage-count increment, updated row, and hard-delete cleanup state. Slice 296 extends the payload pattern into the Slice 35 encounter metadata mutation suite, attaching the `MOD-PAT-0002` anchor patient, proposed temporary encounter metadata payload, created row and encounter-count increment, updated metadata row, and hard-delete cleanup state. Slice 297 extends the payload pattern into the Slice 36 patient demographics mutation suite, attaching the `MOD-PAT-0010` anchor patient, original demographics row, proposed identity/DOB/address/marital-status/occupation update, updated row state, and restored cleanup state. Slice 298 extends the payload pattern into the Slice 37 patient registration lifecycle suite, attaching the proposed `TMP-PAT-REG-*` registration payload, baseline patient count, created demographics/contact rows and count increment, and hard-delete cleanup state. Slice 299 extends the payload pattern into the Slice 38 patient document sign-off suite, attaching the `MOD-PAT-0001` anchor patient, proposed reviewed text document payload, created pending row and document-count increment, approved sign-off row, archived row, and hard-delete cleanup state. Slice 300 extends the payload pattern into the Slice 39 patient document external-link suite, attaching the `MOD-PAT-0001` anchor patient, proposed URL-backed document payload, created row and document-count increment, archived row, and hard-delete cleanup state. Slice 301 extends the payload pattern into the Slice 40 patient document denial suite, attaching the `MOD-PAT-0001` anchor patient, proposed reviewed text document payload, created pending row and document-count increment, denied review row, archived row, and hard-delete cleanup state. Slice 302 extends the payload pattern into the Slice 41 patient document metadata suite, attaching the `MOD-PAT-0001` anchor patient, proposed original text document payload, created row and count increment, refiled metadata row, archived row, and hard-delete cleanup state. Slice 303 extends the payload pattern into the Slice 42 patient document archive/restore suite, attaching the `MOD-PAT-0001` anchor patient, proposed restorable document payload, created row and count increment, archived row with hidden-content projection, restored row, and hard-delete cleanup state. Slice 304 extends the payload pattern into the Slice 43 patient document content replacement suite, attaching the `MOD-PAT-0001` anchor patient, proposed original text document and replacement payload, created row and original content, replaced row and stored content projection, archived row, and hard-delete cleanup state. Slice 305 extends the payload pattern into the Slice 44 billing diagnosis suite, attaching the `MOD-PAT-0001` anchor patient, latest encounter, proposed ICD10 diagnosis billing payload, created row and encounter fee-sheet projection, inactive row, and hard-delete cleanup state. Slice 306 extends the payload pattern into the Slice 45 billing correction suite, attaching the `MOD-PAT-0001` anchor patient, latest encounter, proposed original CPT billing payload, created row, corrected fee/units/justification row and encounter fee-sheet projection, inactive row, and hard-delete cleanup state. Slice 307 extends the payload pattern into the Slice 46 billing modifier suite, attaching the `MOD-PAT-0001` anchor patient, latest encounter, proposed original CPT billing payload, created row with blank modifier, modified `25` row and encounter fee-sheet projection, inactive row, and hard-delete cleanup state. Slice 308 extends the payload pattern into the Slice 47 claim status suite, attaching the `MOD-PAT-0005` anchor patient, encounter `1000052`, seeded queued/generated/cleared Northstar HMO claim rows, and Fees rendering precondition. Slice 309 extends the payload pattern into the Slice 48 payment posting suite, attaching the `MOD-PAT-0005` anchor patient, encounter `1000052`, seeded `EOB-NSTAR-1000052` insurance payment and contractual adjustment rows, and Fees rendering precondition. Slice 310 extends the payload pattern into the Slice 49 account balance suite, attaching the `MOD-PAT-0005` anchor patient, encounter `1000052`, patient-level charge/payment/adjustment/balance totals, encounter-level rollups, and Fees rendering precondition. Slice 311 extends the payload pattern into the Slice 50 account aging suite, attaching the `MOD-PAT-0005` anchor patient, as-of date `2026-06-18`, deterministic Current/31-60/Over 90 encounter rows, bucket totals, total balance, and Fees rendering precondition. Slice 312 extends the payload pattern into the Slice 51 account ledger suite, attaching the `MOD-PAT-0005` anchor patient, chronological charge/payment/adjustment entries, entry-type counts, first/last ledger entries, charge/payment/adjustment totals, final running balance, reference examples, and Fees rendering precondition. Slice 313 extends the payload pattern into the Slice 52 account statement suite, attaching the `MOD-PAT-0005` anchor patient, statement-ready recipient/address/contact facts, period/due-date/status facts, open encounter and ledger counts, current/past/balance due amounts, source balance/aging/ledger context, and Fees rendering precondition. Slice 314 extends the payload pattern into the Slice 53 document preview suite, attaching the `MOD-PAT-0001` anchor patient, deterministic text document preview rows, document keys/categories/storage metadata, preview kind/status, thumbnail labels/text, inline/download flags, and Documents rendering precondition. Slice 315 extends the payload pattern into the Slice 54 document revision suite, attaching the `MOD-PAT-0001` anchor patient, current-version document rows, revision timestamps, version labels/status/counts, prior-version flags, revision hashes, content detail revision facts, and Documents rendering precondition. Slice 316 extends the payload pattern into the Slice 55 document replacement revision suite, attaching the `MOD-PAT-0001` anchor patient, proposed temporary document and replacement payload, created current-version facts, replaced revision timestamp/hash movement, archived state, and hard-delete cleanup. Slice 317 extends the payload pattern into the Slice 56 payment posting mutation suite, attaching the `MOD-PAT-0005` billing anchor, encounter `1000052`, proposed temporary insurance payment posting, payment/session/activity counts, payment and adjustment balance movement, ledger entries, void rollback, active-row hiding, and hard-delete cleanup. Slice 318 extends the payload pattern into the Slice 57 claim status mutation suite, attaching the `MOD-PAT-0005` billing anchor, encounter `1000052`, proposed queued claim, queued/generated/cleared claim state transitions, process file/target/x12 fields, claim-count increments, and hard-delete cleanup. Slice 319 extends the payload pattern into the Slice 58 patient payment capture suite, attaching the `MOD-PAT-0005` billing anchor, encounter `1000052`, proposed patient payment posting, payment/session/activity counts, balance and ledger movement, void rollback, and hard-delete cleanup. Slice 320 extends the payload pattern into the Slice 59 patient statement generation suite, attaching the `MOD-PAT-0005` billing anchor, statement summary, ledger source rows, deterministic statement number, generated text, line totals, and ending balance. Slice 321 extends the payload pattern into the Slice 60 patient statement PDF export suite, attaching the `MOD-PAT-0005` billing anchor, statement summary, ledger source rows, deterministic PDF filename/content type/header, text anchors, line totals, and ending balance. Slice 322 extends the payload pattern into the Slice 61 statement batch candidate suite, attaching ranked top-five candidates, all-candidate totals, statement identifiers, balances, aging fields, open encounter counts, ledger counts, delivery methods, and UI/API anchors. Slice 323 extends the payload pattern into the Slice 62 statement batch package export suite, attaching package source candidates, package manifest facts, summary CSV anchors, package ZIP metadata, PDF filenames, and first-PDF text anchors. Slice 324 extends the payload pattern into the Slice 63 collections work queue suite, attaching ranked past-due accounts, high-priority counts, balance and aging totals, collection tiers, recommended actions, contact methods, and UI/API anchors. Slice 325 extends the payload pattern into the Slice 64 collections follow-up task suite, attaching the selected queue account, proposed pnotes-compatible task, created/closed rows, modernized UI-created row, soft-delete/archive state, and final hard-delete cleanup counts. Slice 326 extends the payload pattern into the Slice 65 patient-message assignment suite, attaching the `MOD-PAT-0004` anchor patient, proposed temporary message, created admin-assigned row, billing-reassigned row, soft-delete/archive state, and final hard-delete cleanup counts. Slice 327 extends the payload pattern into the Slice 66 patient-message content suite, attaching the `MOD-PAT-0004` anchor patient, proposed temporary message content, created row, edited title/body row, soft-delete/archive state, and final hard-delete cleanup counts. Slice 328 extends the payload pattern into the Slice 67 encounter document attachment suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, seeded attached document rows, legacy category-rendering facts, modernized encounter-detail API rows, and Encounters workspace UI anchors. Slice 329 extends the payload pattern into the Slice 68 encounter billing linkage suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, linked CPT fee-sheet billing rows, total-fee/justification expectations, legacy Fee Sheet rendering facts, modernized encounter-detail API rows, and Encounters workspace billing-linkage UI anchors. Slice 330 extends the payload pattern into the Slice 69 encounter claim linkage suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, cleared HCFA claim row, Acme Health payer/status facts, legacy normalized claim reachability, modernized encounter-detail API rows, and Encounters workspace claim-linkage UI anchors. Slice 331 extends the payload pattern into the Slice 70 encounter procedure order linkage suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000011`, Hemoglobin A1c order `5000001`, reviewed report `6000001`, final result anchors, legacy Procedure Results rendering facts, modernized encounter-detail API rows, and Encounters workspace procedure-order UI anchors. Slice 332 extends the payload pattern into the Slice 71 encounter diagnosis coding suite, attaching the `MOD-PAT-0001` anchor patient, billing encounter `1000013`, procedure encounter `1000011`, `E78.5` fee-sheet/encounter diagnosis facts, `E11.9` procedure-order diagnosis facts, legacy encounter/Fee Sheet/Procedure Results rendering facts, modernized encounter-detail API rows, and Encounters workspace diagnosis-coding UI anchors. Slice 333 extends the payload pattern into the Slice 72 encounter billing linkage mutation suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, temporary `99499` CPT row, baseline/create/inactive/cleanup count movement, legacy Fee Sheet rendering facts, modernized encounter-detail API rows, and Encounters workspace billing/diagnosis linkage UI anchors. Slice 334 extends the payload pattern into the Slice 73 encounter diagnosis coding mutation suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, temporary `R73.03` ICD10 diagnosis row, baseline/create/inactive/cleanup count movement, legacy Fee Sheet rendering facts, modernized encounter-detail API rows, and Encounters workspace diagnosis-coding UI anchors. Slice 335 extends the payload pattern into the Slice 74 encounter fee-sheet entry suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, temporary `99499` CPT row, temporary `R73.03` ICD10 diagnosis row, baseline/create/inactive/cleanup count movement, legacy Fee Sheet rendering facts, modernized encounter-detail API rows, and Encounters workspace fee-sheet/billing/diagnosis UI anchors. Slice 336 extends the payload pattern into the Slice 75 encounter procedure-order entry suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, temporary `80053` pending laboratory order, baseline/create/cleanup count movement, legacy Procedure Orders and Reports rendering facts, modernized encounter-detail API procedure-order rows, and Encounters workspace procedure-order entry/linkage UI anchors. Slice 337 extends the payload pattern into the Slice 76 encounter procedure-result entry suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, temporary `80053` laboratory order, reviewed final report, `2345-7` Glucose result, baseline/create/cleanup count movement, legacy Procedure Results rendering facts, modernized encounter-detail API report/result rows, and Encounters workspace procedure result-entry/linkage UI anchors. Slice 338 extends the payload pattern into the Slice 77 encounter sign-off suite, attaching the `MOD-PAT-0002` anchor patient, temporary encounter, attestation row, signature facts, modernized sign-off rendering, and final cleanup. Slice 339 extends the payload pattern into the Slice 78 encounter document upload suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary text attachment, normalized document projection, legacy Documents category rendering facts, modernized encounter-detail API document rows, Encounters attached-document UI anchors, and final cleanup. Slice 340 extends the payload pattern into the Slice 79 encounter binary document upload suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary PDF attachment, normalized binary document projection, PDF base64/size facts, modernized download response, legacy Documents category rendering facts, Encounters attached-document UI anchors, and final cleanup. Slice 341 extends the payload pattern into the Slice 80 encounter document sign-off suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary reviewed text attachment, created pending row, approved sign-off/review facts, legacy Documents category rendering facts, modernized encounter-detail API review rows, Encounters attached-document sign-off UI anchors, and final cleanup. Slice 342 extends the payload pattern into the Slice 81 encounter document denial suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary reviewed text attachment, created pending row, denied review facts, legacy Documents category rendering facts, modernized encounter-detail API review rows, Encounters attached-document denial UI anchors, and final cleanup. Slice 343 extends the payload pattern into the Slice 82 encounter document metadata suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary text attachment, original Medical Record metadata, refiled Advance Directive metadata, legacy Documents category rendering facts, modernized encounter-detail API metadata rows, Encounters attached-document metadata UI anchors, and final cleanup. Slice 344 extends the payload pattern into the Slice 83 encounter document move suite, attaching the `MOD-PAT-0001` anchor patient, source encounter `1000013`, target encounter `1000011`, baseline source/target encounter document lists, temporary text attachment, source-to-target movement facts, legacy Documents category rendering facts, modernized source/target encounter-detail API rows, Encounters attached-document move UI anchors, and final cleanup. Slice 345 extends the payload pattern into the Slice 84 encounter document content replacement suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary original text attachment, replacement text payload, original/replaced content and revision facts, legacy Documents category rendering facts, modernized encounter-detail API replacement rows, Encounters attached-document replacement UI anchors, and final cleanup. Slice 346 extends the payload pattern into the Slice 85 encounter document archive/restore suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary text attachment, archived hidden-content state, restored active document facts, legacy Documents category rendering facts, modernized active/archived encounter-detail API rows, Encounters attached-document Archive/Restore UI anchors, and final cleanup. Slice 347 extends the payload pattern into the Slice 86 encounter document lifecycle timeline suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary text attachment, filed/current-version/review-pending/active create facts, review-approved sign-off facts, archived lifecycle facts, restored active lifecycle facts, legacy Documents category rendering facts, modernized encounter-detail lifecycle API rows, Encounters attached-document lifecycle UI anchors, and final cleanup. Slice 348 extends the payload pattern into the Slice 87 encounter external-link document suite, attaching the `MOD-PAT-0001` anchor patient, encounter `1000013`, baseline encounter document list, temporary URL-backed `text/uri-list` document payload, `web_url` storage facts, legacy Documents category rendering facts, modernized encounter-detail API external-link rows, Encounters attached-document Open Link UI anchors, archive state, and final cleanup. Slice 349 extends the payload pattern into the Slice 88 patient image document preview suite, attaching the `MOD-PAT-0001` anchor patient, baseline document counts, temporary `image/svg+xml` binary payload, normalized image preview metadata, legacy Documents category rendering facts, modernized Documents card/viewer inline image UI anchors, byte-preserving download facts, archive state, and final cleanup. Slice 350 extends the payload pattern into the Slice 89 patient image document thumbnail suite, attaching the `MOD-PAT-0001` anchor patient, baseline document counts, temporary `image/svg+xml` binary payload, normalized thumbnail metadata, legacy Documents category rendering facts, modernized Documents card thumbnail image anchors, archive state, and final cleanup. Slice 351 extends the payload pattern into the Slice 90 patient PDF document inline-preview suite, attaching the `MOD-PAT-0001` anchor patient, baseline document counts, temporary `application/pdf` binary payload, normalized inline PDF preview metadata, legacy Documents category rendering facts, modernized Documents card/viewer iframe UI anchors, byte-preserving download facts, archive state, and final cleanup. Slice 352 extends the payload pattern into the Slice 91 patient document lifecycle timeline suite, attaching the `MOD-PAT-0001` anchor patient, baseline document counts, temporary text document payload, filed/current-version/review-pending/active create facts, review-approved sign-off facts, archived lifecycle facts, restored active lifecycle facts, legacy Documents category rendering facts, modernized Documents lifecycle API/card/viewer anchors, and final cleanup. Slice 353 extends the payload pattern into the Slice 92 patient scanned attachment suite, attaching the `MOD-PAT-0001` anchor patient, baseline document counts, temporary scanned `application/pdf` binary payload, normalized PDF preview metadata, scan-readiness facts, legacy Documents category rendering facts, modernized Documents card/viewer scan metadata anchors, archive state, and final cleanup. Slice 354 extends the payload pattern into the Slice 93 appointment reschedule suite, attaching the `MOD-PAT-0003` anchor patient, baseline appointment counts, temporary future appointment create payload, rescheduled title/date/time/duration/status/room facts, legacy appointment edit rendering facts, modernized Calendar list/detail rendering anchors, and final cleanup. Slice 355 extends the payload pattern into the Slice 94 appointment arrival suite, attaching the `MOD-PAT-0003` anchor patient, baseline appointment counts, temporary scheduled appointment create payload, arrived status/title facts, legacy appointment edit rendering facts, modernized Calendar arrival action/rendering anchors, and final cleanup. Slice 356 extends the payload pattern into the Slice 95 appointment check-out suite, attaching the `MOD-PAT-0003` anchor patient, baseline appointment counts, temporary scheduled appointment create payload, arrived `@` state, checked-out `>` state, legacy appointment edit rendering facts, modernized Calendar arrival/check-out action anchors, and final cleanup. Slice 357 extends the payload pattern into the Slice 96 appointment no-show suite, attaching the `MOD-PAT-0003` anchor patient, baseline appointment counts, temporary scheduled appointment create payload, no-show `?` status/title facts, legacy appointment edit rendering facts, modernized Calendar no-show action/rendering anchors, and final cleanup. Slice 358 extends the payload pattern into the Slice 97 appointment category suite, attaching the same anchor patient, baseline appointment counts, temporary Preventive Care Services appointment create payload, New Patient category update facts, legacy appointment edit rendering facts, modernized Calendar category selector/rendering anchors, and final cleanup. Slice 359 extends the payload pattern into the Slice 98 appointment pending-status suite, attaching the same anchor patient, baseline appointment counts, temporary scheduled appointment create payload, pending `~` status/title facts, legacy appointment edit rendering facts, modernized Calendar pending-status selector/rendering anchors, and final cleanup. Slice 360 extends the payload pattern into the Slice 99 appointment provider reassignment suite, attaching the same anchor patient, baseline appointment counts, temporary scheduled appointment create payload, original/alternate provider IDs, reassigned title/provider facts, legacy appointment edit rendering facts, modernized Calendar provider selector/rendering anchors, and final cleanup. Slice 361 extends the payload pattern into the Slice 100 appointment facility reassignment suite, attaching the same anchor patient, baseline appointment counts, temporary scheduled appointment create payload, original/reassigned service and billing facility IDs, reassigned title/facility facts, legacy appointment edit rendering facts, modernized Calendar facility selector/rendering anchors, and final cleanup. Slice 362 extends the payload pattern into the Slice 101 appointment billing-location reassignment suite, attaching the same anchor patient, baseline appointment counts, temporary scheduled appointment create payload, preserved service facility ID, original/reassigned billing facility IDs, reassigned title/billing-location facts, legacy appointment edit rendering facts, modernized Calendar billing facility selector/rendering anchors, and final cleanup. Slice 363 extends the payload pattern into the Slice 102 appointment comments suite, attaching the same anchor patient, baseline appointment counts, temporary scheduled appointment create payload, initial/updated comments, updated title/comment facts, legacy appointment edit rendering facts, modernized Calendar comments control/rendering anchors, and final cleanup. Slice 364 extends the payload pattern into the Slice 103 appointment recurrence metadata suite, attaching the same anchor patient, baseline appointment counts, temporary recurring appointment create payload, weekly cadence/end-date facts, updated two-week cadence/end-date facts, legacy appointment repeat-control rendering facts, modernized Calendar repeat-control/rendering anchors, and final cleanup. Slice 365 extends the payload pattern into the Slice 104 appointment recurring-series suite, attaching the same anchor patient, seeded recurrence search window, expected generated occurrence dates/numbers, normalized recurring-series workflow rows, modernized Calendar generated-occurrence rendering anchors, and legacy workflow projection facts. Slice 366 extends the payload pattern into the Slice 105 appointment recurrence-exceptions suite, attaching the `MOD-PAT-0013` anchor patient, seeded skipped occurrence date, expected generated occurrence dates/numbers after exception filtering, normalized recurrence-exdate workflow rows, modernized Calendar skipped-date rendering anchors, and legacy workflow projection facts. Slice 367 extends the payload pattern into the Slice 106 appointment occurrence-cancel suite, attaching the same anchor patient, seeded recurring-series state, generated occurrence selected for cancellation, recurrence-exdate mutation, hidden cancelled occurrence expansion, modernized Calendar skip-occurrence rendering anchors, and cleanup restoration. Slice 368 extends the payload pattern into the Slice 107 appointment occurrence-restore suite, attaching the same anchor patient, generated occurrence selected for restore, temporary skipped state, recurrence-exdate removal, restored occurrence expansion, modernized Calendar restore-occurrence rendering anchors, and cleanup restoration. Slice 369 extends the payload pattern into the Slice 108 appointment occurrence-reschedule suite, attaching the same anchor patient, generated occurrence selected for movement, recurring-root exception mutation, standalone moved appointment, modernized Calendar reschedule rendering anchors, and cleanup restoration. Slice 370 extends the payload pattern into the Slice 109 appointment recurrence exception-list edit suite, attaching the same anchor patient, recurring root, seeded skipped date, added skipped date, edited occurrence expansion, modernized Calendar skipped-date edit rendering anchors, and cleanup restoration. Slice 371 extends the payload pattern into the Slice 110 appointment series root update suite, attaching the same anchor patient, recurring root title/time update, generated occurrence propagation, seeded exception preservation, modernized Calendar root edit rendering anchors, and cleanup restoration. Slice 372 extends the payload pattern into the Slice 111 appointment series root metadata suite, attaching the same anchor patient, recurring root provider/facility/billing/category/status/room/comment metadata update, generated occurrence propagation, seeded exception preservation, modernized Calendar generated-occurrence rendering anchors, and cleanup restoration. Slice 373 extends the payload pattern into the Slice 112 appointment monthly recurrence suite, attaching the anchor patient, temporary monthly recurring root creation, every-two-month update, generated occurrence expansion, legacy repeat-control rendering facts, modernized Calendar monthly recurrence rendering anchors, and cleanup deletion. Slice 374 extends the payload pattern into the Slice 113 appointment recurrence unit matrix suite, attaching the anchor patient, temporary daily/workday/yearly recurring root creation, generated occurrence expansion, legacy repeat-control rendering facts, modernized Calendar recurrence-unit rendering anchors, and cleanup deletion. Slice 375 extends the payload pattern into the Slice 114 appointment days-of-week recurrence suite, attaching the anchor patient, temporary Monday/Wednesday/Friday recurring root creation, weekday selection metadata, generated occurrence expansion, legacy `REPEAT_DAYS` rendering facts, modernized Calendar weekday rendering anchors, and cleanup deletion. Slice 376 extends the payload pattern into the Slice 115 appointment monthly repeat-on recurrence suite, attaching the anchor patient, temporary second-Tuesday and last-Friday recurring root creation, repeat-on ordinal/weekday metadata, generated occurrence expansion, legacy `REPEAT_ON` rendering facts, modernized Calendar repeat-on rendering anchors, and cleanup deletion. Slice 377 extends the payload pattern into the Slice 116 appointment series recurrence update suite, attaching the seeded recurring root, original cadence/end-date state, temporary every-three-weeks update, skipped-date preservation, generated occurrence expansion, legacy repeat-control rendering facts, modernized Calendar recurrence update anchors, and cleanup restoration. Slice 378 extends the payload pattern into the Slice 117 appointment provider overlap suite, attaching the two seeded patient anchors, shared provider/date/time precondition, temporary overlapping appointment rows, overlap query results, legacy appointment rendering facts, modernized provider-overlap rendering anchors, count deltas, and cleanup deletion. Slice 379 extends the payload pattern into the Slice 118 appointment patient overlap suite, attaching the seeded patient anchor, two-provider/date/time precondition, temporary overlapping appointment rows, overlap query results, legacy appointment rendering facts, modernized patient-overlap rendering anchors, count deltas, and cleanup deletion. Slice 380 extends the payload pattern into the Slice 119 appointment room overlap suite, attaching the two seeded patient anchors, two-provider shared-room/date/time precondition, temporary overlapping appointment rows, overlap query results, legacy appointment rendering facts, modernized room-overlap rendering anchors, count deltas, and cleanup deletion. Slice 381 extends the payload pattern into the Slice 120 appointment reminders suite, attaching the `MOD-PAT-0191` seeded patient, future preventive-care appointment, contact-consent/channel derivation, legacy appointment rendering facts, and modernized reminder-detail rendering anchors. Slice 382 extends the payload pattern into the Slice 121 encounter co-signature suite, attaching the `MOD-PAT-0002` seeded patient, temporary encounter, ordered admin/provider signatures, hash/lock/amendment facts, UI rendering anchors, signature deletion, and encounter cleanup. Slice 383 extends the payload pattern into the Slice 122 encounter document revision suite, attaching the `MOD-PAT-0001` seeded patient, encounter `1000013`, two current-version encounter documents, revision timestamps, hash parity, API document projection, and modernized attached-document card rendering anchors. Slice 384 extends the payload pattern into the Slice 123 encounter document replacement revision suite, attaching the `MOD-PAT-0001` seeded patient, encounter `1000013`, temporary original document creation, replacement content/hash movement, legacy Documents rendering facts, modernized replacement form/API/card rendering anchors, and cleanup deletion. Slice 385 extends the payload pattern into the Slice 126 encounter scanned attachment suite, attaching the `MOD-PAT-0001` seeded patient, encounter `1000013`, temporary scanned `application/pdf` payload, normalized PDF preview metadata, scan/OCR-readiness facts, legacy Documents rendering facts, modernized encounter detail API/card rendering anchors, and cleanup deletion. Slice 386 extends the payload pattern into the Slice 127 encounter binary document content replacement suite, attaching the `MOD-PAT-0001` seeded patient, encounter `1000013`, temporary original PDF attachment, replacement PDF bytes, preview/download metadata, revision hash movement, legacy Documents rendering facts, modernized binary replacement form/API/card/download anchors, and cleanup deletion. Slice 387 extends the payload pattern into the Slice 128 patient binary document content replacement suite, attaching the `MOD-PAT-0001` seeded patient, temporary original PDF document, replacement PDF bytes, preview/download metadata, revision hash movement, legacy Documents rendering facts, modernized Documents binary replacement form/API/card/download anchors, and cleanup deletion. Slice 388 extends the payload pattern into the Slice 129 procedure result correction suite, attaching the `MOD-PAT-0009` seeded patient, temporary encounter/order/report/result creation, corrected result value/range/abnormal/status movement, legacy Procedure Results rendering facts, modernized Procedures correction UI facts, patient procedure projection, and cleanup deletion. Slice 389 extends the payload pattern into the Slice 130 procedure specimen suite, attaching the `MOD-PAT-0009` seeded patient, temporary encounter/order/report/result creation, collected date and specimen number facts, legacy Procedure Results rendering facts, modernized Procedures specimen UI facts, patient procedure projection, and cleanup deletion. Slice 390 extends the payload pattern into the Slice 131 procedure specimen detail suite, attaching the `MOD-PAT-0009` seeded patient, temporary encounter/order/specimen-detail creation, specimen identifier/accession/type/collection/location/volume/condition facts, legacy database projection facts, modernized Procedures specimen-detail UI facts, patient procedure projection, and cleanup deletion. Slice 391 extends the payload pattern into the Slice 132 procedure order correction suite, attaching the `MOD-PAT-0009` seeded patient, temporary encounter/order creation, initial and corrected order metadata, follow-up report/result projection, legacy Procedure Results rendering facts, modernized Procedures correction UI facts, and cleanup deletion. Slice 392 extends the payload pattern into the Slice 133 procedure report correction suite, attaching the MOD-PAT-0009 seeded patient, temporary encounter/order/report/result creation, corrected collected-date/report-date/specimen/status/review/notes facts, preserved linked result projection, legacy Procedure Results rendering facts, modernized Procedures report-correction UI facts, and cleanup deletion. Slice 393 extends the payload pattern into the Slice 134 procedure report sign-off suite, attaching the MOD-PAT-0009 seeded patient, temporary encounter/order/report/result creation, pending-review report state, reviewed-by/reviewed-at sign-off facts, preserved linked result projection, legacy Procedure Results rendering facts, modernized Procedures sign-off UI facts, and cleanup deletion. Slice 394 extends the payload pattern into the Slice 135 procedure report review queue suite, attaching the MOD-PAT-0009 seeded patient, temporary encounter/order/report creation, unreviewed queue membership, reviewed queue movement after sign-off, legacy review queue rendering facts, modernized Reports queue rendering facts, and cleanup removal from both queues. Slice 395 extends the payload pattern into the Slice 136 procedure report review queue filter suite, attaching the MOD-PAT-0009 seeded patient, temporary encounter/order/report creation, patient/date filter inclusion, outside-date exclusion, reviewed filter movement after sign-off, legacy filtered queue rendering facts, modernized Reports filter rendering facts, and cleanup removal from filtered queues. Slice 396 extends the payload pattern into the Slice 137 procedure report review queue provider filter suite, attaching the MOD-PAT-0009 seeded patient, temporary encounter/order/report creation, ordering-provider filter inclusion, alternate-provider exclusion, reviewed provider-filter movement after sign-off, legacy provider-filtered queue rendering facts, modernized Reports provider filter rendering facts, and cleanup removal from provider-filtered queues. Slice 397 extends the payload pattern into the Slice 138 procedure report review queue lab filter suite, attaching the MOD-PAT-0009 seeded patient, temporary lab-provider/alternate-lab-provider creation, temporary encounter/order/report creation, processing-lab filter inclusion, alternate-lab exclusion, reviewed lab-filter movement after sign-off, legacy lab-filtered queue rendering facts, modernized Reports lab filter rendering facts, and cleanup removal from lab-filtered queues plus temporary lab providers. Slice 398 extends the payload pattern into the Slice 139 procedure lab provider catalog suite, attaching the MOD-PAT-0009 seeded patient, permanent reviewed report ownership by Pacific Women's Health Laboratory, outside-lab exclusion, legacy reviewed queue rendering facts, and modernized Reports lab catalog rendering facts. Slice 399 extends the payload pattern into the Slice 140 procedure lab provider directory suite, attaching the permanent five-provider gold-data directory, active/inactive provider counts, per-provider order/report/future-order counts, legacy Procedure Providers rendering facts, and modernized Reports directory rendering facts. Slice 400 extends the payload pattern into the Slice 141 procedure lab provider lifecycle suite, attaching temporary provider create inputs, active provider row/rendering, deactivated include-inactive row/rendering, and cleanup deletion facts. Slice 401 extends the payload pattern into the Slice 142 procedure lab provider configuration suite, attaching initial provider configuration, updated SFTP transport settings, browser/API rendering facts, and cleanup deletion facts. Slice 402 extends the payload pattern into the Slice 144 procedure lab provider address-book suite, attaching temporary order-service organizations, provider name derivation from linked organizations, relinked provider state, browser/API rendering facts, and cleanup deletion facts. Slice 403 extends the payload pattern into the Slice 145 procedure order catalog suite, attaching the permanent gold-data catalog root, provider groups, Pacific Women's Health Laboratory anchor panels, modernized API counts, legacy AJAX rendering facts, and modernized Reports catalog rendering facts. Slice 404 extends the payload pattern into the Slice 147 procedure order catalog lifecycle suite, attaching temporary catalog item create inputs, created and updated rows, rendered create/update facts, deactivation facts, and cleanup deletion facts. Slice 405 extends the payload pattern into the Slice 148 procedure vendor compendium import suite, attaching temporary import group inputs, first and second PathGroup CSV payloads, created/updated/deactivated/reactivated order and result rows, rendering facts, and cleanup deletion facts. Slice 406 extends the payload pattern into the Slice 149 procedure order queue suite, attaching temporary encounter/order/report inputs, ready-to-send queue membership, ready rendering facts, reported queue transition, reported rendering facts, and cleanup count restoration. Slice 407 extends the payload pattern into the Slice 151 procedure order transmit suite, attaching temporary encounter/order inputs, ready-to-send queue precondition, transmit action state, sent-awaiting-results queue transition, browser/API rendering facts, and cleanup count restoration. Slice 408 extends the payload pattern into the Slice 153 procedure report bulk sign-off suite, attaching temporary encounter/order/report inputs, unreviewed queue precondition, bulk sign-off action state, reviewed queue transition, browser/API rendering facts, and cleanup count restoration. Slice 409 extends the payload pattern into the Slice 154 procedure report reopen-review suite, attaching temporary encounter/order/report inputs, reviewed queue precondition, reopen action state, received/unreviewed queue transition, browser/API rendering facts, and cleanup count restoration. Slice 410 extends the payload pattern into the Slice 156 patient-message reply suite, attaching temporary pnotes-compatible message creation, reply append state, browser/API rendering facts, soft-delete state, and cleanup count restoration. Slice 411 extends the payload pattern into the Slice 157 patient-message portal metadata suite, attaching seeded pnotes portal relation metadata, plaintext encryption flags, legacy Patient Notes rendering facts, and modernized Messages card rendering facts. Slice 412 extends the payload pattern into the Slice 158 patient-message update metadata suite, attaching temporary pnotes-compatible message creation, edited title/body state, OpenEMR-compatible updated-by and updated-at metadata, browser/API rendering facts, soft-delete state, and cleanup count restoration. Slice 413 extends the payload pattern into the Slice 159 admin login readiness suite, attaching redacted configured-credential preconditions, legacy HTTP success and invalid-password rejection markers, modernized API success and rejection responses, and modernized Admin-page login rendering facts. Slice 414 extends the payload pattern into the Slice 160 admin login audit readiness suite, attaching audit-table boundary preconditions, legacy login log rows, modernized auth audit event rows, success/failure row summaries, and modernized Admin-page login audit rendering facts. Slice 415 extends the payload pattern into the Slice 161 admin session readiness suite, attaching redacted session preconditions, legacy session cookie/logout markers, modernized login/current-session/logout API facts, auth session persistence rows, and modernized Admin-page session rendering facts. Slice 416 extends the payload pattern into the Slice 162 admin audit protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API blocking facts, authenticated legacy/API visibility facts, ended-session rejection facts, and modernized Admin-page audit rendering facts. Slice 417 extends the payload pattern into the Slice 163 admin directory protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API directory blocking facts, unauthenticated modernized mutation rejection facts, authenticated legacy/API directory visibility facts, and modernized Admin-page directory rendering facts. Slice 418 extends the payload pattern into the Slice 164 operational reports protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API report blocking facts, unauthenticated modernized CSV export rejection facts, authenticated legacy/API report visibility facts, authenticated CSV export facts, and modernized Reports-page rendering facts. Slice 419 extends the payload pattern into the Slice 165 patient chart protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API patient chart blocking facts, authenticated legacy/API patient search and chart visibility facts, and modernized Patient/Client-page rendering facts. Slice 420 extends the payload pattern into the Slice 166 clinical-list protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API clinical-list blocking facts, unauthenticated modernized mutation rejection facts, authenticated legacy/API list visibility facts, and modernized Lists-page rendering facts. Slice 421 extends the payload pattern into the Slice 167 appointment protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API appointment blocking facts, unauthenticated modernized create rejection facts, authenticated legacy/API appointment search/detail visibility facts, and modernized Calendar-page rendering facts. Slice 422 extends the payload pattern into the Slice 168 encounter protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API encounter blocking facts, unauthenticated modernized create rejection facts, authenticated legacy/API encounter search/detail visibility facts, and modernized Encounters-page rendering facts. Slice 423 extends the payload pattern into the Slice 169 document protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API document list/content/create blocking facts, authenticated legacy/API document visibility facts, and modernized Documents-page rendering facts. Slice 424 extends the payload pattern into the Slice 170 message protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API message list/create blocking facts, authenticated legacy/API message visibility facts, and modernized Messages-page rendering facts. Slice 425 extends the payload pattern into the Slice 171 billing protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API billing summary/batch/create blocking facts, authenticated legacy/API fee-sheet visibility facts, and modernized Fees-page rendering facts. Slice 426 extends the payload pattern into the Slice 172 procedure protection readiness suite, attaching redacted protection preconditions, unauthenticated legacy/API procedure result/catalog/create blocking facts, authenticated legacy/API procedure result visibility facts, and modernized Procedures-page rendering facts. Slice 427 extends the payload pattern into the Slice 173 administration authorization-policy readiness suite, attaching redacted ACL preconditions, legacy ACL matrix rendering facts, modernized front-desk forbidden directory/mutation facts, admin directory allow facts, and modernized Admin-page retry rendering facts. Slice 428 extends the payload pattern into the Slice 174 operational reports authorization-policy readiness suite, attaching redacted ACL preconditions, legacy operational report rendering facts, modernized front-desk forbidden report/export facts, admin report/export allow facts, and modernized Reports-page retry rendering facts. Slice 429 extends the payload pattern into the Slice 175 clinical-list authorization-policy readiness suite, attaching redacted ACL preconditions, legacy patient-summary clinical-list rendering facts, modernized front-desk forbidden clinical-list retrieval and allergy-mutation facts, admin clinical-list allow facts, and modernized Lists-page retry rendering facts. Slice 430 extends the payload pattern into the Slice 176 appointment authorization-policy readiness suite, attaching redacted ACL preconditions, legacy appointment edit rendering facts, modernized front-desk login, appointment search, and appointment detail allow facts, and modernized Calendar-page rendering facts. Slice 431 extends the payload pattern into the Slice 177 encounter authorization-policy readiness suite, attaching redacted ACL preconditions, legacy encounter rendering facts, modernized front-desk forbidden encounter search/mutation facts, admin encounter search/detail allow facts, and modernized Encounters-page retry rendering facts. Slice 432 extends the payload pattern into the Slice 178 patient chart authorization-policy readiness suite, attaching redacted Demographics ACL preconditions, legacy patient-summary rendering facts, modernized unauthenticated patient-search rejection facts, front-desk patient search/chart allow facts, admin chart allow facts, and modernized Patient/Client-page rendering facts. Slice 433 extends the payload pattern into the Slice 179 patient document authorization-policy readiness suite, attaching redacted Documents ACL preconditions, legacy document-list rendering facts, modernized front-desk forbidden document list/content/create facts, admin document list/content allow facts, and modernized Documents-page retry rendering facts. Slice 434 extends the payload pattern into the Slice 180 patient message authorization-policy readiness suite, attaching redacted Patient Notes ACL preconditions, legacy patient-notes rendering facts, modernized front-desk forbidden message list/create facts, admin message list allow facts, and modernized Messages-page retry rendering facts. Slice 435 extends the payload pattern into the Slice 181 billing authorization-policy readiness suite, attaching redacted Billing ACL preconditions, legacy fee-sheet rendering facts, modernized front-desk forbidden billing summary/batch/create facts, admin billing summary allow facts, and modernized Fees-page retry rendering facts. Slice 436 extends the payload pattern into the Slice 182 procedure authorization-policy readiness suite, attaching redacted Lab Results ACL preconditions, legacy procedure-result rendering facts, modernized front-desk forbidden procedure result/catalog/create facts, admin procedure-result allow facts, and modernized Procedures-page retry rendering facts. Slice 437 extends the payload pattern into the Slice 183 procedure mutation authorization-policy readiness suite, attaching redacted Lab Results/Sign ACL preconditions, legacy procedure-result rendering facts, clinician add-only order/report create facts, clinician write/sign/delete denial facts, admin status/sign/delete cleanup facts, and final cleanup facts. Slice 438 extends the payload pattern into the Slice 184 patient mutation authorization-policy readiness suite, attaching redacted Demographics ACL preconditions, legacy Demographics edit rendering facts, clinician add-only registration facts, clinician Demographics/contact/delete denial facts, admin delete cleanup facts, and final cleanup facts. Slice 439 extends the payload pattern into the Slice 185 encounter mutation authorization-policy readiness suite, attaching redacted Authorize Any Encounter ACL preconditions, legacy encounter rendering facts, temporary clinician view-grant facts, clinician read allow facts, clinician create/update/sign/delete denial facts, admin detail allow facts, and final grant cleanup facts. Slice 440 extends the payload pattern into the Slice 186 document mutation authorization-policy readiness suite, attaching redacted Documents/Documents Delete ACL preconditions, legacy Documents rendering facts, admin and clinician login facts, clinician read/add-only create facts, clinician metadata/content/sign/archive/delete denial facts, admin hard-delete cleanup facts, and final document-count cleanup facts. Slice 441 extends the payload pattern into the Slice 187 patient message mutation authorization-policy readiness suite, attaching redacted Patient Notes ACL preconditions, legacy Patient Notes rendering facts, admin and clinician login facts, clinician read/add-only create facts, clinician status/content/assignment/reply/archive/delete denial facts, admin hard-delete cleanup facts, and final message-count cleanup facts. Slice 442 extends the payload pattern into the Slice 188 billing mutation authorization-policy readiness suite, attaching redacted Billing ACL preconditions, legacy Fee Sheet rendering facts, admin login facts, temporary clinician Billing view-grant facts, clinician login and read facts, clinician billing-line, collections follow-up, claim, and payment denial facts, grant cleanup facts, and final revenue-cycle count cleanup facts. Slice 443 extends the payload pattern into the Slice 189 appointment mutation authorization-policy readiness suite, attaching redacted Appointment ACL preconditions, legacy appointment edit rendering facts, admin login facts, temporary clinician Appointment view-downgrade facts, clinician login and read facts, clinician create/update/status/recurrence-restore/occurrence-reschedule/delete denial facts, write-grant restoration facts, and final appointment-count cleanup facts. Slice 444 extends the payload pattern into the Slice 190 encounter amendment history readiness suite, attaching anchor-patient preconditions, temporary encounter creation, signature and amendment-history rows, legacy surface facts, modernized API/UI rendering facts, signature cleanup, and final encounter/signature cleanup facts. Slice 445 extends the payload pattern into the Slice 191 patient duplicate detection readiness suite, attaching anchor-patient/contact preconditions, temporary duplicate registration creation, database duplicate-candidate rows, legacy database-surface facts, modernized API/chart/UI duplicate panel facts, and final temporary-patient cleanup facts. Slice 446 extends the payload pattern into the Slice 192 patient registration validation readiness suite, attaching legacy validator/source-rule facts, invalid modernized registration draft inputs, structured API validation problem facts, UI validation-panel rendering facts, and no-row database guard facts. Slice 447 extends the payload pattern into the Slice 193 patient deceased-status readiness suite, attaching anchor/original deceased-status preconditions, temporary deceased date/reason update facts, legacy demographics edit rendering facts, modernized patient chart API/UI deceased-status facts, and restore cleanup facts. Slice 448 extends the payload pattern into the Slice 194 patient guardian-contact readiness suite, attaching anchor/original mother and guardian contact preconditions, temporary contact update facts, legacy demographics edit rendering facts, modernized patient chart API/UI Guardian Contact facts, and restore cleanup facts. Slice 449 extends the payload pattern into the Slice 195 patient guardian demographic/address readiness suite, attaching anchor/original guardian detail preconditions, temporary sex/address/country/work-phone update facts, legacy demographics edit rendering facts, modernized patient chart API/UI Guardian Contact facts, and restore cleanup facts. Slice 450 extends the payload pattern into the Slice 196 patient social-detail readiness suite, attaching anchor/original demographics preconditions, temporary race/ethnicity/interpreter/family-size/income/homeless/review-date update facts, legacy demographics edit rendering facts, modernized patient chart API/UI demographics facts, and restore cleanup facts. Slice 451 extends the payload pattern into the Slice 197 patient employer core readiness suite, attaching anchor/original employer preconditions, temporary employer identity/address update facts, legacy demographics/employer rendering facts, modernized patient chart API/UI Employer facts, and restore cleanup facts. Slice 452 extends the payload pattern into the Slice 198 patient provider assignment readiness suite, attaching anchor/original primary-provider preconditions, temporary provider ID/name update facts, legacy demographics provider rendering facts, modernized patient chart API/UI Primary Provider facts, and restore cleanup facts. Slice 453 extends the payload pattern into the Slice 199 patient care-team readiness suite, attaching anchor/original care-team preconditions, temporary lead-member provider/facility/role update facts, legacy patient-summary care-team rendering facts, modernized patient chart API/UI Care Team facts, and restore cleanup facts. Slice 454 extends the payload pattern into the Slice 200 patient multi-member care-team readiness suite, attaching anchor/original care-team member preconditions, temporary two-member provider/facility/role update facts, legacy patient-summary care-team rendering facts, modernized patient chart API/UI Care Team facts, and restore cleanup facts. Slice 455 extends the payload pattern into the Slice 201 patient contact-backed care-team readiness suite, attaching anchor/original provider/contact care-team preconditions, temporary provider plus patient-contact update facts, legacy patient-summary care-team rendering facts, modernized patient chart API/UI Care Team facts, and restore cleanup facts. Slice 456 extends the payload pattern into the Slice 202 patient history/lifestyle readiness suite, attaching anchor/history/lifestyle preconditions, legacy History and Lifestyle rendering facts, modernized patient chart API facts, and modernized Patient/Client history panel facts. Slice 457 extends the payload pattern into the Slice 203 patient insurance subscriber readiness suite, attaching seeded secondary subscriber preconditions, temporary tertiary subscriber create/update facts, legacy insurance browse rendering facts, modernized Patient/Client insurance panel facts, and cleanup facts. Slice 458 extends the payload pattern into the Slice 204 patient portal account readiness suite, attaching seeded CMS login and portal-account preconditions, legacy demographics edit rendering facts, modernized patient chart API facts, and modernized Patient/Client Portal Account panel facts. Slice 459 extends the payload pattern into the Slice 205 patient portal reset readiness suite, attaching original portal reset preconditions, temporary one-time reset issue/clear facts, legacy demographics edit rendering facts, modernized patient chart API facts, modernized Portal Account panel facts, and restore cleanup facts. Slice 466 extends the payload pattern into the Slice 212 patient portal secure-message reply readiness suite, attaching anchor-patient preconditions, seeded original-message state, cleanup-backed threaded reply result, post-cleanup mailbox state, legacy portal Sent-folder rendering, and modernized Portal reply/Sent-folder rendering. Slice 465 remains available for Slice 211 patient portal secure-message compose payloads, Slice 464 remains available for Slice 210 patient portal secure-message inbox payloads, Slice 463 remains available for Slice 209 patient portal home payloads, Slice 462 remains available for Slice 208 patient portal session payloads, Slice 461 remains available for Slice 207 patient portal authentication payloads, and Slice 460 remains available for Slice 206 patient portal access payloads. Slice 255 adds managed patient portal secure-message notification plan actions; Slice 254 patient portal profile review revert actions, Slice 253 patient portal profile review accept actions, and earlier managed patient portal, patient administration, protection, authorization-policy, scheduling, encounter, document, billing, procedure, reporting, and administration actions remain available.

The Progress page reads milestone rows and the functionality progress ledger from `/api/progress`. The milestone rows remain an operational summary for baseline, Workbench, test-management, and modernized target delivery state. The functionality progress ledger is a curated JSON artifact at `modernization-workbench/config/functionality-progress.json`; it is intentionally edited by the project, not inferred from raw test names. Each area records:

- `status` - a coarse status such as `in-progress`, `partial`, `verified`, or `not-started`.
- `summary` - the current product-level interpretation for the domain area.
- `completionEstimatePercent` - an editable 0-100 estimate for how much of the currently understood scope is complete.
- `estimatedScopeWeight` - a relative size/complexity weight for the area; the current ledger totals 100 weight points.
- `scopeWeightRationale` - the short reason the area carries that weight.
- `estimateRationale` - the short reason for the current estimate, including major covered and remaining work.
- `completed` - user-facing functionality considered covered, with short detail and related evidence IDs such as slice readiness plans.
- `outstanding` - work still expected or likely needed before the modernization feels complete in that area.
- `deferred` - known longer-tail, integration, compliance, or production-hardening items that should stay visible without being confused with the immediate next slice.

Completion estimates are planning signals, not contractual pass/fail measurements. They may move up when new parity evidence lands, or down when legacy discovery reveals more scope inside an area. The Progress page now displays the weighted estimate as the main overall completion value and keeps the simple average visible as a comparison signal. The weighted formula is `sum(completionEstimatePercent * estimatedScopeWeight) / sum(estimatedScopeWeight)`.

The accepted-difference registry is stored in `modernization-workbench/config/accepted-differences.json`. Entries are curated project decisions, not generated from failed tests. Each accepted entry can be scoped by comparison selection kind, selection ID, difference path, and message text. The Workbench API applies only active accepted entries to recent comparison artifacts and reports accepted and unreviewed counts without mutating the original comparison JSON.

The Test Runs page also reads `/api/parity-reliability`. That endpoint is intentionally lighter than the comparison drill-in endpoint: it scans bounded `run.json` and `comparison.json` artifacts without loading screenshots or Playwright probe details. The panel summarizes recent run pass rate, comparison match rate, average run duration, unreviewed differences, recent pass/fail strips, and the noisiest selection-level comparison summaries.

Probe drill-ins now include safe attachment previews when Playwright records text-like artifacts such as Markdown, text, JSON, XML, CSV, or logs. Preview reads are capped and restricted to the existing safe artifact roots. Binary artifacts such as traces remain available only as artifact links and metadata. The database gold-seed contract suite now writes JSON payload attachments for count, temporal coverage, anchor-patient, and related-record probes on both legacy and modernized targets. The Slice 1 workflow suite writes JSON payload attachments for anchor patient demographics, activity counts, and UI steering preconditions. The Slice 2 scheduling suite writes JSON payload attachments for the future appointment anchor and UI steering appointment facts. The Slice 3 encounter suite writes JSON payload attachments for the anchor patient, latest encounter, SOAP/vitals facts, and UI steering precondition. The Slice 4 clinical-list suite writes JSON payload attachments for the anchor patient, problem/allergy/medication/prescription facts, and UI steering precondition. The Slice 5 messaging suite writes JSON payload attachments for the portal-enabled anchor patient, seeded care-team and portal-message facts, and UI steering precondition. The Slice 6 procedure suite writes JSON payload attachments for the anchor patient, completed CBC order, completed report, final Hemoglobin result, and UI steering precondition. The Slice 7 billing suite writes JSON payload attachments for the anchor patient, latest encounter, selected CPT fee-sheet lines, and UI steering precondition. The Slice 8 administration suite writes JSON payload attachments for seeded user counts, provider/calendar counts, selected users, facilities, and UI steering precondition. The Slice 9 reports suite writes JSON payload attachments for gold-data counts, selected provider/facility activity, selected clinical-condition summaries, and UI steering precondition. The Slice 10 contact mutation suite writes JSON payload attachments for the `MOD-PAT-0001` anchor patient, original contact state, proposed update, post-update state, and restored cleanup state. The Slice 11 appointment mutation suite writes JSON payload attachments for the `MOD-PAT-0003` anchor patient, proposed future appointment, created appointment row, cancelled appointment row, and deleted cleanup state. The Slice 12 encounter mutation suite writes JSON payload attachments for the `MOD-PAT-0002` anchor patient, proposed encounter/vitals/SOAP payloads, created encounter row, clinical-detail rows and count increments, updated encounter row, and deleted cleanup state. The Slice 13 clinical-list allergy mutation suite writes JSON payload attachments for the `MOD-PAT-0006` anchor patient, proposed allergy payload, created allergy row and count increment, deactivated allergy row, and deleted cleanup state. The Slice 14 patient-message mutation suite writes JSON payload attachments for the `MOD-PAT-0004` anchor patient, proposed message payload, created message row and count increment, closed message row, soft-deleted message row, and hard-delete cleanup state. The Slice 15 prescription mutation suite writes JSON payload attachments for the `MOD-PAT-0008` anchor patient, proposed prescription payload, created prescription row and count increment, deactivated prescription row, and hard-delete cleanup state. The Slice 16 billing mutation suite writes JSON payload attachments for the `MOD-PAT-0001` anchor patient, latest encounter, proposed CPT billing-line payload, created billing row and count increment, billed/inactive row state, and hard-delete cleanup state. The Slice 17 procedure mutation suite writes JSON payload attachments for the `MOD-PAT-0009` anchor patient, proposed encounter/order/report/result payloads, created encounter and order count increments, completed order with final report/result rows, patient procedure summary projection, and hard-delete cleanup state. The Slice 18 administration facility mutation suite writes JSON payload attachments for the proposed active facility payload, temporary facility count baseline/increment, created facility row, deactivated facility row, and hard-delete cleanup state. The Slice 19 administration user mutation suite writes JSON payload attachments for the proposed active user payload, temporary user count baseline/increment, created user row, deactivated user row, and hard-delete cleanup state. The Slice 20 administration access-control suite writes JSON payload attachments for ACL group, permission, group-permission assignment, user-membership counts and anchors plus UI steering matrix facts. The Slice 21 administration access-permission mutation suite writes JSON payload attachments for the focused Front Office demographics ACL assignment precondition, revoked state, restored state, and final cleanup restoration. The Slice 22 administration user group membership mutation suite writes JSON payload attachments for the proposed temporary user, created user before membership, granted Front Office membership, revoked membership, and final cleanup/deleted-user state. The Slice 23 pending scheduled procedure-order suite writes JSON payload attachments for the anchor patient, future scheduled CBC order, reportless order projection, and UI steering precondition. The Slice 24 operational reports CSV export suite writes JSON payload attachments for the normalized export row set, selected CSV anchors, and UI/API export precondition. The Slice 25 patient documents suite writes JSON payload attachments for the anchor patient, filed document metadata, categories, storage mode, text previews, and UI steering precondition. The Slice 26 patient document mutation suite writes JSON payload attachments for the anchor patient, proposed text document, created document row and count increment, archived row, and hard-delete cleanup state. The Slice 27 patient document content suite writes JSON payload attachments for the anchor patient, selected intake-packet metadata, full stored content, UI steering precondition, and modernized content/download API response facts. The Slice 28 patient insurance coverage suite writes JSON payload attachments for the anchor patient, primary and secondary policy facts, subscriber relationship facts, and UI steering precondition. The Slice 29 patient immunization history suite writes JSON payload attachments for the pediatric anchor patient, normalized vaccine-history rows, 2026 influenza and Hep A anchors, lot/manufacturer facts, and UI steering precondition. The Slice 30 patient immunization mutation suite writes JSON payload attachments for the anchor patient, proposed temporary influenza immunization, created row and count increment, entered-in-error row, and hard-delete cleanup state. The Slice 31 patient problem-list mutation suite writes JSON payload attachments for the anchor patient, proposed temporary problem, created row and count increment, deactivated row, and hard-delete cleanup state. The Slice 32 patient medication-list mutation suite writes JSON payload attachments for the anchor patient, proposed temporary medication, created row and count increment, deactivated row, and hard-delete cleanup state. The Slice 33 binary patient-document mutation suite writes JSON payload attachments for the anchor patient, proposed temporary PDF binary document, created document row and count increment, archived row, and hard-delete cleanup state. The Slice 34 patient insurance mutation suite writes JSON payload attachments for the anchor patient, baseline primary/secondary coverage, proposed temporary tertiary coverage, created row and coverage-count increment, updated row, and hard-delete cleanup state. The Slice 35 encounter metadata mutation suite writes JSON payload attachments for the anchor patient, proposed temporary encounter metadata, created encounter row and count increment, updated reason, billing note, sensitivity, referral source, external ID, POS row state, and hard-delete cleanup state. The Slice 36 patient demographics mutation suite writes JSON payload attachments for the anchor patient, original demographics row, proposed identity/DOB/address/marital-status/occupation update, updated row state, and restored cleanup state. The Slice 37 patient registration lifecycle suite writes JSON payload attachments for the proposed temporary registration payload, baseline patient count, created demographics/contact rows and count increment, and hard-delete cleanup state. The Slice 38 patient document sign-off suite writes JSON payload attachments for the anchor patient, proposed reviewed text document payload, created pending row and document-count increment, approved sign-off row, archived row, and hard-delete cleanup state. The Slice 39 patient document external-link suite writes JSON payload attachments for the anchor patient, proposed URL-backed document payload, created row and document-count increment, archived row, and hard-delete cleanup state. The Slice 40 patient document denial suite writes JSON payload attachments for the anchor patient, proposed reviewed text document payload, created pending row and document-count increment, denied review row, archived row, and hard-delete cleanup state. The Slice 41 patient document metadata suite writes JSON payload attachments for the anchor patient, proposed original text document payload, created row and count increment, refiled category/name/date/encounter/notes row, archived row, and hard-delete cleanup state. The Slice 42 patient document archive/restore suite writes JSON payload attachments for the anchor patient, proposed restorable document payload, created row and count increment, archived row with hidden-content projection, restored row, and hard-delete cleanup state. Broader workflow and mutation database-probe payload generation remains a parity-test responsibility.

The `/api/progress` response also reconstructs a committed progress history from Git by reading `modernization-workbench/config/functionality-progress.json` as it existed at each first-parent commit that changed the ledger after it was introduced. For the earlier modernization check-ins that predate explicit completion estimates, it reads the stored backfill artifact at `modernization-workbench/config/functionality-progress-backfill.json`. That artifact currently covers first-parent commits from `6586a4a0` (`Bootstrap modernized OpenEMR slice one`) through `bab45ab1` (`Implement procedure order catalog slice 145`), stores a cumulative `completionEstimatePercent`, estimate basis, summary, and rationale for each check-in, and is calibrated to the first explicit progress-ledger estimate at `40822fd8` (52.7%). Each history point exposes the Git commit completion timestamp as `snapshotCompletedAt`, and the Progress page plots the weighted completion line chart on that timestamp-based x-axis so longer gaps between completed snapshots remain visible. The API enriches history points with matching `PROJECT_CHANGELOG.md` entry titles, summaries, and key outcomes when their explicit `Commit:` or `Changeset:` metadata matches the progress commit, and falls back to stored historical-estimate text or Git commit-message body text when needed. The chart renders historical-estimate spans as dashed pre-ledger history, measured snapshots as the solid line, supports brush-to-zoom range selection, resettable zoom, rich hover details, and click-to-inspect details. Historical estimate details show commit identity, weighted percent, delta, elapsed time since the prior timeline point, the stored estimate basis/rationale, and the changelog narrative for what was accomplished; measured snapshot details keep the same metrics plus the unweighted comparison signal. Commits before scope weights existed fall back to equal weighting for their historical point.

The remaining-time forecast is intentionally rough. It converts the current weighted remaining percent into slice-equivalent work using the number of completed slice numbers in the changelog, then multiplies that by recent explicit slice durations from `Started:` and `Finished:` metadata. This produces an active-work estimate, not a promised calendar delivery date, and it may move up or down as the estimated scope changes.

When scope changes, update `functionality-progress.json` in the same work item as the related implementation or planning change. Keep the entries concise and product-facing: the ledger should answer what the modernization can do, what remains, and which evidence supports the completed claims. It should not replace `MODERNIZATION_PLAN.md`, `TEST_ARCHITECTURE.md`, or `PROJECT_CHANGELOG.md`; it summarizes them for Workbench operators.

The Architecture page now uses sub-tabs. The Overview tab shows a versioned technology-stack matrix for the three project systems: legacy OpenEMR, the Modernization Workbench, and modernized OpenEMR. The matrix groups technologies by UI, server-side runtime, data stores, local runtime/orchestration, and tests/evidence, with logo-style technology chips and explicit versions where the repository or running containers provide them. The Overview tab also includes architecture-size source inventory, a project topology diagram, and architecture-decision notes. Each system tab provides source/code/schema statistics, a focused architecture diagram, runtime summary, data ownership summary, business-logic narrative, responsibilities, and evidence notes.

The legacy app launch link opens `http://localhost:8080` because that is the browser-friendly local URL. The OpenEMR HTTPS endpoint remains available at `https://localhost:9443`, but it uses a self-signed local certificate and browsers will show a privacy warning unless the certificate is trusted or manually bypassed. The Workbench backend supports both `http` and `https` health URLs, still uses `https://localhost:9443/meta/health/readyz` for the legacy health check, and is configured to tolerate the self-signed certificate only for HTTPS internal checks.

The Managed Application panel also displays the local demo OpenEMR login read from `legacy-openemr/.env`. This is intentionally local-only and helps distinguish the actual baseline credential from any browser autofill suggestion on the OpenEMR login page.

The Workbench owns the shared seed-data contract under `modernization-workbench/seed-data/`. The current manifest defines `openemr-shared-synthetic-v1`, the generated 1,000-patient deterministic synthetic gold dataset, now including deterministic guardian contact/detail fields, 2,648 immunization rows for read-only vaccine-history parity, 80 regular appointment recurrence metadata anchors, 200 provisioned portal accounts, and 400 deterministic portal mailbox rows projected into legacy `onsite_mail` and modernized `portal_mailbox_messages` from the shared portal-message facts. The legacy MariaDB adapter is implemented through `legacy-openemr/scripts/Seed-LegacyGoldDataset.ps1`; the modernized PostgreSQL adapter is implemented through `modernized-openemr/scripts/Seed-ModernizedGoldDataset.ps1`.

Verified behavior:

- Production build passes with `npm run build`.
- The UI renders in desktop and mobile viewports.
- The UI includes a left navigation shell with separate pages for dashboard, applications, timeline, progress, architecture, tests, and seed data.
- The UI loads the operational app snapshot first, hydrates route-specific progress, timeline, architecture, and test evidence data independently, and keeps periodic polling scoped to app status and lifecycle events.
- The Architecture page renders a versioned visual stack matrix, source inventory statistics, project topology diagram, architecture-decision notes, and detail tabs for legacy OpenEMR, the Workbench, and modernized OpenEMR.
- The API can read legacy OpenEMR status.
- The API can classify Docker Compose runtime readiness for managed apps, including stopped stacks, partial stacks, unhealthy containers, unreachable health endpoints, and likely Docker Desktop engine failures.
- The Dashboard and Applications pages render Docker runtime readiness guidance for the legacy, modernized, and Modern UI Claude apps, including a Start all apps action that runs the configured Docker Compose start commands and then refreshes app snapshots.
- The API can load recent Docker Compose logs.
- The API can run the baseline smoke test.
- The API can run the containerized OpenEMR-native isolated PHPUnit stable suite.
- The API can run the OpenEMR-native JavaScript Jest suite.
- The API can run the legacy parity database, HTTP, UI, workflow mutation, named-plan, and full-suite test commands through allowlisted manifests.
- The API can run validated custom parity selections from the manifest with operator-selected reset mode, headed mode, and optional grep filter.
- The API can read recent side-by-side comparison artifacts from `parity-tests/artifacts/comparisons/`, enrich each comparison side with run-level visual artifacts when screenshots/images exist, extract normalized Playwright probe details from comparison-side JSON reports, apply curated accepted-difference rules, summarize rolling reliability from run/comparison artifacts, expose safe text-like probe attachment previews when present, and the Test Runs page renders matched/different status, run IDs, suites, metrics, accepted/unreviewed difference counts, reliability summaries, artifact paths, expandable drill-ins, screenshot thumbnails, probe detail lists, attachment previews including database contract plus Slice 1 through Slice 142 plus Slices 144, 145, 147, 148, 149, 151, 153, 154, 156, 157, 158, 159, 160, 161, 162, 163, 164, 165, 166, 167, 168, 169, 170, 171, 172, 173, 174, 175, 176, 177, 178, 179, 180, 181, 182, 183, 184, 185, 186, 187, 188, 189, 190, 191, 192, 193, 194, 195, 196, 197, 198, 199, 200, 201, 202, 203, 204, 205, 206, 207, 208, 209, 210, 211, 212, 213, 214, 215, 216, 217, 218, 219, 220, 221, 222, 223, 224, 225, 226, 227, 228, and 229 workflow payload JSON when present, safe links to run/comparison artifact files, and direct links to comparison-side run JSON, Playwright JSON, JUnit XML, and HTML reports.
- The API exposes `/api/artifacts/file` for read-only artifact access under `parity-tests/artifacts/`, `legacy-openemr/artifacts/`, `modernized-openemr/artifacts/`, and `modernization-workbench/artifacts/`; paths outside those roots are rejected.
- The API can run and validate the legacy gold seed action.
- The API can run and validate the modernized PostgreSQL gold seed action.
- The API can run the shared slice-1 readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-2 scheduling readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-3 encounters readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-4 clinical-lists readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-5 messaging readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-6 procedures readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-7 billing readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-8 admin readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-9 reports readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-10 contact mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-11 appointment mutation readiness, slice-93 appointment reschedule readiness, slice-94 appointment arrival readiness, slice-95 appointment check-out readiness, slice-96 appointment no-show readiness, slice-97 appointment category readiness, slice-98 appointment pending-status readiness, slice-99 appointment provider reassignment readiness, slice-100 appointment facility reassignment readiness, slice-101 appointment billing-location reassignment readiness, slice-102 appointment comments readiness, slice-103 appointment recurrence metadata readiness, slice-104 appointment recurring-series readiness, slice-105 appointment recurrence-exceptions readiness, slice-106 appointment occurrence-cancel readiness, slice-107 appointment occurrence-restore readiness, slice-108 appointment occurrence-reschedule readiness, slice-109 appointment recurrence exception-edit readiness, slice-110 appointment series root update readiness, slice-111 appointment series root metadata readiness, slice-112 appointment monthly recurrence readiness, slice-113 appointment recurrence unit matrix readiness, slice-114 appointment days-of-week recurrence readiness, slice-115 appointment monthly repeat-on recurrence readiness, slice-116 appointment series recurrence update readiness, slice-117 appointment provider overlap readiness, slice-118 appointment patient overlap readiness, slice-119 appointment room overlap readiness, slice-120 appointment reminders readiness, and slice-167 appointment protection readiness parity plans for both legacy and modernized targets.
- The API can run the shared slice-12 encounter mutation readiness, slice-168 encounter protection readiness, slice-190 encounter amendment history readiness, slice-169 document protection readiness, slice-170 message protection readiness, slice-171 billing protection readiness, slice-172 procedure protection readiness, slice-173 admin authorization-policy readiness, slice-174 reports authorization-policy readiness, slice-175 clinical-list authorization-policy readiness, slice-176 appointment authorization-policy readiness, slice-189 appointment mutation authorization-policy readiness, slice-177 encounter authorization-policy readiness, slice-185 encounter mutation authorization-policy readiness, slice-178 patient chart authorization-policy readiness, slice-191 patient duplicate detection readiness, slice-192 patient registration validation readiness, slice-193 patient deceased status readiness, slice-195 patient guardian details readiness, slice-196 patient social details readiness, slice-197 patient employer core readiness, slice-198 patient provider assignment readiness, slice-199 patient care-team readiness, slice-200 patient care-team members readiness, slice-201 patient care-team contact readiness, slice-202 patient history readiness, slice-203 insurance subscriber readiness, slice-204 patient portal account readiness, slice-205 patient portal reset readiness, slice-206 patient portal access readiness, slice-207 patient portal authentication readiness, slice-208 patient portal session readiness, slice-209 patient portal home readiness, slice-210 patient portal secure-message readiness, slice-211 patient portal secure-message compose readiness, slice-212 patient portal secure-message reply readiness, slice-213 patient portal secure-message thread view readiness, slice-214 patient portal secure-message archive readiness, slice-215 patient portal secure-message read-status readiness, slice-179 patient document authorization-policy readiness, slice-186 patient document mutation authorization-policy readiness, slice-180 patient message authorization-policy readiness, slice-187 patient message mutation authorization-policy readiness, slice-181 billing authorization-policy readiness, slice-188 billing mutation authorization-policy readiness, slice-182 procedure authorization-policy readiness, slice-183 procedure mutation authorization-policy readiness, and slice-184 patient mutation authorization-policy readiness parity plans for both legacy and modernized targets.
- The API can run the shared slice-255 patient portal secure-message notification readiness parity plan for both legacy and modernized targets; the Slice 254 patient portal profile review revert, Slice 253 patient portal profile review accept, Slice 252 patient portal profile review queue, Slice 251 patient portal profile change request, Slice 250 patient portal profile, and earlier patient portal plans remain available.
- The API can run the shared slice-13 clinical-list mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-14 message mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-15 prescription mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-16 billing mutation readiness and slice-171 billing protection readiness parity plans for both legacy and modernized targets.
- The API can run the shared slice-17 procedure mutation readiness and slice-172 procedure protection readiness parity plans for both legacy and modernized targets.
- The API can run the shared slice-18 admin facility mutation readiness parity plan, the shared slice-19 admin user mutation readiness parity plan, the shared slice-20 access-control readiness parity plan, the shared slice-21 access-permission mutation readiness parity plan, the shared slice-22 user group membership mutation readiness parity plan, and the shared slice-159 through slice-255 authentication, protection, authorization-policy, mutation authorization-policy, patient portal, generated report, and administration readiness parity plans for both legacy and modernized targets.
- The API can run the shared slice-23 pending procedure orders readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-24 reports export readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-25 patient documents readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-26 patient document mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-27 patient document content readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-28 patient insurance coverage readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-29 patient immunizations readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-30 patient immunization mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-31 patient problem-list mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-32 patient medication-list mutation, slice-33 binary patient-document mutation, slice-34 patient insurance mutation, and slice-35 encounter metadata mutation readiness parity plans for both legacy and modernized targets.
- The API can run the shared slice-36 patient demographics mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-37 patient registration readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-40 document denial readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-41 document metadata readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-43 document content replacement readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-44 billing diagnosis readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-46 billing modifier readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-47 claim status readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-48 payment posting readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-49 account balance readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-50 account aging readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-60 statement PDF export readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-61 statement batch candidate readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-62 statement batch package export readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-63 collections work queue readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-64 collections follow-up task readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-65 patient-message assignment readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-66 patient-message content readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-156 patient-message reply readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-157 patient-message portal metadata readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-158 patient-message update metadata readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-67 encounter documents readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-68 encounter billing readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-69 encounter claims readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-70 encounter procedure orders readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-71 encounter diagnosis coding readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-72 encounter billing linkage mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-73 encounter diagnosis coding mutation readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-74 encounter fee-sheet entry readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-75 encounter procedure-order entry readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-76 encounter procedure-result entry readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-77 encounter sign-off readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-121 encounter co-signature readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-122 encounter document revision readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-123 encounter document replacement revision readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-78 encounter document upload readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-79 encounter binary document upload readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-126 encounter scanned attachment readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-127 encounter binary document content replacement readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-128 patient binary document content replacement readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-129 procedure result correction readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-130 procedure specimen readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-131 procedure specimen detail readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-132 procedure order correction readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-133 procedure report correction readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-134 procedure report sign-off readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-135 procedure report review queue readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-136 procedure report review queue filters readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-137 procedure report review queue provider filters readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-138 procedure report review queue lab filters readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-139 procedure lab provider catalog, slice-140 procedure lab provider directory, slice-141 procedure lab provider lifecycle, slice-142 procedure lab provider configuration, slice-144 procedure lab provider address-book, slice-145 procedure order catalog readiness, slice-147 procedure order catalog lifecycle, slice-148 procedure vendor compendium import, slice-149 procedure order queue, slice-151 procedure order transmit, slice-153 procedure report bulk sign-off, and slice-154 procedure report reopen review parity plans for both legacy and modernized targets.
- The API can run the shared slice-80 encounter document sign-off readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-81 encounter document denial readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-82 encounter document metadata readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-83 encounter document move readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-84 encounter document content replacement readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-85 encounter document archive/restore readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-87 encounter external-link document readiness parity plan for both legacy and modernized targets.
- The API can run the shared slice-88 patient image document preview readiness, slice-89 patient image document thumbnail readiness, slice-90 patient PDF document inline-preview readiness, slice-91 patient document lifecycle timeline readiness, and slice-92 patient scanned attachment readiness parity plans for both legacy and modernized targets.
- The API can run the shared slice-93 appointment reschedule readiness, slice-94 appointment arrival readiness, slice-95 appointment check-out readiness, slice-96 appointment no-show readiness, slice-97 appointment category readiness, slice-98 appointment pending-status readiness, slice-99 appointment provider reassignment readiness, slice-100 appointment facility reassignment readiness, slice-101 appointment billing-location reassignment readiness, slice-102 appointment comments readiness, slice-103 appointment recurrence metadata readiness, slice-104 appointment recurring-series readiness, slice-105 appointment recurrence-exceptions readiness, slice-106 appointment occurrence-cancel readiness, slice-107 appointment occurrence-restore readiness, slice-108 appointment occurrence-reschedule readiness, slice-109 appointment recurrence exception-edit readiness, slice-110 appointment series root update readiness, slice-111 appointment series root metadata readiness, slice-112 appointment monthly recurrence readiness, slice-113 appointment recurrence unit matrix readiness, slice-114 appointment days-of-week recurrence readiness, slice-115 appointment monthly repeat-on recurrence readiness, slice-116 appointment series recurrence update readiness, slice-117 appointment provider overlap readiness, slice-118 appointment patient overlap readiness, slice-119 appointment room overlap readiness, and slice-120 appointment reminders readiness parity plans for both legacy and modernized targets.
- The API can parse `documents/PROJECT_CHANGELOG.md`, calculate section duration from explicit `Started:` and `Finished:` timestamps when present, enrich older resolvable commit-backed entries with completion clock time and elapsed time since the previous completed step, backfill code-change metrics from changelog `Code changes:` sections or Git commit stats, and expose the result as structured timeline data.
- The API can stop and restart the legacy OpenEMR Docker Compose stack.
- The API can start, stop, restart, health-check, seed, smoke test, and profile the modernized OpenEMR Docker Compose stack.
- After Workbench restart control, legacy OpenEMR returns to healthy state and the smoke test passes.

## Relationship To The Other Systems

The project has three major systems:

- **Legacy OpenEMR baseline** - the original OpenEMR application running locally in a reproducible Docker-based environment.
- **Modernization Workbench** - the oversight website that tracks status, progress, tests, comparisons, and technical differences.
- **Modernized OpenEMR target** - the new implementation built in vertical slices using a modern UI, API, business tier, and PostgreSQL.

The workbench was built after the baseline could run a minimal meaningful smoke test. It is now the primary visual control surface for the rest of the modernization effort.

The intended operator workflow is that a user runs one local script to start the Modernization Workbench, then uses the Workbench to inspect, start, stop, restart, and test the other project applications such as the legacy OpenEMR site.

## Core Responsibilities

The workbench should help answer:

- Is the legacy OpenEMR baseline running?
- Which OpenEMR version and seed-data version are being used?
- Which tests can be run against the baseline?
- What were the latest test results?
- Is the modernized target running?
- Which applications are stopped, starting, healthy, unhealthy, or stopped with errors?
- Can an operator start, stop, or restart a project application from one controlled interface?
- Which workflow slices have been discovered, implemented, and parity tested?
- How do the legacy and modernized systems compare for a given workflow?
- What are the technical architecture differences between the two systems?
- What evidence exists for the current modernization state?

## Initial Version

The first workbench version is intentionally small and useful.

Implemented capabilities:

- Show legacy OpenEMR environment status.
- Show the configured baseline browser URL, database status, and seed-data status when available.
- Show the current local demo login from `legacy-openemr/.env`.
- Start, stop, and restart the legacy OpenEMR Docker Compose environment through controlled local commands.
- Trigger the gold legacy seed through `legacy-openemr/scripts/Seed-LegacyGoldDataset.ps1`.
- Trigger the starter legacy seed through `legacy-openemr/scripts/Seed-LegacyExampleData.ps1`.
- Trigger baseline smoke tests through `legacy-openemr/scripts/Test-LegacyBaseline.ps1`.
- Trigger OpenEMR-native isolated PHPUnit tests through `legacy-openemr/scripts/Test-LegacyNative.ps1`.
- Trigger OpenEMR-native JavaScript Jest tests through `legacy-openemr/scripts/Test-LegacyNativeJs.ps1`.
- Trigger parity test suites through `scripts/Run-OpenEmrParityTests.ps1`.
- Trigger custom parity runs through a manifest-backed run builder that validates suite, plan, reset, headed, and grep options.
- Display latest baseline, modernized, smoke, and parity test results.
- Display recent lifecycle action results, including command status, duration, and logs.
- Display the project changelog as a designed build timeline sourced from `documents/PROJECT_CHANGELOG.md` on its own page, including explicit start time, finish time, and calculated duration when available, with Git-derived completion clock times as a fallback for older entries that reference a resolvable commit.
- Display architecture, progress, test runs, seed data, and managed applications on dedicated pages.
- Display links or paths to logs, screenshots, and reports.
- Show the modernized target as a managed application once the first slice exists.
- Show a project progress view with the three major systems and their current stage.

The first version originally did not require the modernized target to exist. As of the first modernization slice, the Workbench includes `modernized-openemr` as a managed application.

## Application Lifecycle Control

The workbench should be able to control the running state of project applications, but only through explicit, allowlisted operations.

Expected lifecycle actions:

- Check status.
- Start.
- Stop.
- Restart.
- Run health check.
- Open application URL.
- View recent logs.

Initial target:

- Legacy OpenEMR baseline in `legacy-openemr/`, controlled through Docker Compose.

Current additional target:

- Modernized OpenEMR in `modernized-openemr/`, controlled through Docker Compose.

Future targets:

- Databases and supporting services used by the modernized target.
- Test runners and comparison services.

Preferred implementation:

- A Workbench startup script starts the Workbench backend and frontend.
- The Workbench backend exposes local-only orchestration APIs.
- Each managed application has a manifest that defines its working directory, start command, stop command, status command, health endpoint, public URL, and log locations.
- The frontend calls the backend for lifecycle actions.
- The backend executes only predefined commands from manifests.

The Workbench should not provide a general-purpose shell. Lifecycle control is powerful enough that it must remain local, explicit, logged, and constrained to known project commands.

## Test Orchestration

The workbench may trigger tests, but tests should remain executable outside the workbench.

Preferred pattern:

- Test suites live in normal source-controlled test projects or scripts.
- The workbench calls a small orchestration API or command runner.
- Test output is written to structured result files.
- The workbench reads and displays those result files.
- The same commands can later run in CI.

The first available baseline test command is `legacy-openemr/scripts/Test-LegacyBaseline.ps1`, which writes `legacy-openemr/artifacts/latest-smoke-test.json`.

The native OpenEMR implementation-confidence command is `legacy-openemr/scripts/Test-LegacyNative.ps1`, which writes `legacy-openemr/artifacts/latest-native-test.json` and a companion log file. Its default stable mode runs OpenEMR's upstream isolated PHPUnit suite inside the pinned OpenEMR container while excluding the upstream `twig` and `large` groups because the complete suite currently has Windows bind-mount-sensitive CRLF fixture and built-in-server routing failures. The verified stable run covers 2,344 tests and 6,188 assertions.

The native JavaScript implementation-confidence command is `legacy-openemr/scripts/Test-LegacyNativeJs.ps1`, which writes `legacy-openemr/artifacts/latest-native-jest-test.json`, a full Jest JSON report, and a companion log file. It runs OpenEMR's upstream Jest suite with 12 verified suites and 105 tests covering CCDA service utilities and jsPDF compatibility.

The reusable parity test harness lives in `parity-tests/` and is launched by `scripts/Run-OpenEmrParityTests.ps1`. It currently provides database, HTTP, Playwright UI, workflow mutation, named run plans, a side-by-side slice-1 readiness plan through a side-by-side slice-255 patient portal secure-message notification readiness plan, and full-suite legacy runs.

The UI and workflow suites cover the implemented patient, scheduling, encounter, clinical-list, messaging, document, revenue-cycle, procedure/lab, administration, ACL, reporting, and Workbench evidence workflows. The procedure/lab family now includes procedure report review queue rendering and filters, procedure report bulk sign-off and reopen review, procedure order queue rendering and transmit readiness, permanent lab-provider catalog ownership, and provider-directory rendering. Latest suite and plan summaries are written under `parity-tests/artifacts/` and displayed on the Workbench Test Runs page.

Slice 221 adds patient portal appointment request-options evidence and Slice 475 path-backed probe payload attachments: the shared plan signs in as `MOD-PAT-0004`, verifies category, provider, facility, duration, date, and time defaults, records precondition, option projection, and target-specific request-form UI payloads, and compares the same option behavior on the modernized Portal workspace. Slice 220 remains available with patient portal appointment-request evidence and Slice 474 path-backed probe payload attachments: the shared plan signs in as `MOD-PAT-0004`, creates a cleanup-backed pending appointment plus provider reminder, records precondition, creation/persistence, cleanup, and target-specific requested-appointment UI payloads, and compares the same request behavior on the modernized Portal workspace. Slice 219 remains available with patient portal appointment evidence and Slice 473 path-backed probe payload attachments: the shared plan signs in as `MOD-PAT-0004`, verifies one upcoming and two past appointment cards from the legacy portal home, records precondition, future/past projection, and target-specific appointment UI payloads, and compares the same API/UI behavior on the modernized Portal workspace. Slice 218 remains available with patient portal document list/download evidence and Slice 472 path-backed probe payload attachments for preconditions, document/category projection, selected ZIP package metadata, and target-specific document download UI surfaces, Slice 217 remains available with patient portal secure-message All-folder evidence and Slice 471 path-backed probe payload attachments for preconditions, pre-All-folder mailbox state, cleanup-backed active/archived projection, cleanup state, and target-specific All-folder UI surfaces, Slice 216 remains available with patient portal secure-message batch-archive evidence and Slice 470 path-backed probe payload attachments for preconditions, pre-batch mailbox state, cleanup-backed archive result, cleanup state, and target-specific selected-message archive UI surfaces, Slice 215 remains available with patient portal secure-message read-status evidence and Slice 469 path-backed probe payload attachments for preconditions, pre-read mailbox/home-summary state, cleanup-backed read result, cleanup state, and target-specific read-status UI surfaces, Slice 214 remains available with secure-message archive evidence and Slice 468 path-backed probe payload attachments for preconditions, pre-archive mailbox state, cleanup-backed archive result, cleanup state, and target-specific Sent/Archive UI surfaces, Slice 213 remains available with thread view evidence and Slice 467 path-backed probe payload attachments for thread preconditions, seeded original-message/thread anchor state, cleanup-backed thread result, cleanup state, and target-specific thread UI surfaces, Slice 212 remains available with cleanup-backed reply evidence and Slice 466 path-backed probe payload attachments for original-message, reply result, cleanup, and target-specific Sent-folder rendering evidence, while Slice 211 remains available with cleanup-backed compose/sent evidence and Slice 465 path-backed probe payload attachments for compose result, cleanup, and target-specific Sent-folder rendering evidence, and Slice 210 remains available with seeded inbox evidence and Slice 464 path-backed probe payload attachments for inbox identity, message detail, and target-specific UI rendering evidence.

Slice 209 adds patient portal home evidence: the shared plan reuses the existing `MOD-PAT-0004` provisioned account, signs in with `mod-pat-0004@example.test` / `PortalPass207!`, verifies portal identity, two secure-message rows with one new message, and the next `2026-07-28` `Preventive Care` appointment, and compares the modernized first-class Portal workspace with legacy portal home entry. Slice 208 remains available with patient portal session evidence: the shared plan verifies active modernized session reads, ends the session through the modernized patient portal API and Patient/Client Portal Account end-session command, verifies inactive reuse rejection, and compares that with legacy portal login/logout browser behavior. Slice 207 remains available with patient portal authentication evidence: the shared plan verifies the deterministic credential, invalid-password rejection, disabled-access rejection, pending-reset rejection, modernized sign-in readiness rendering, and access/reset restoration. Slice 206 remains available with patient portal access evidence: the shared plan revokes portal access, verifies legacy `patient_data.allow_patient_portal`, verifies the modernized API/UI access projection through the Patient/Client Portal Account panel, grants access again, and restores the original enabled state while preserving CMS login and onsite account facts. Slice 205 remains available with patient portal reset evidence: the shared plan issues one-time reset state, verifies legacy `patient_access_onsite.portal_pwd_status` / `portal_onetime`, verifies the modernized API/UI reset projection through the Patient/Client Portal Account panel, clears the reset, and restores the original patient-managed state. Slice 204 remains available with patient portal account evidence: the shared plan reads the existing `MOD-PAT-0004` account anchor, verifies legacy OpenEMR `patient_data.cmsportal_login` and `patient_access_onsite` fields plus demographics-page rendering, and verifies the modernized API/UI projection through the Patient/Client Portal Account panel. Slice 203 remains available with insurance subscriber evidence: the shared plan reads the existing `MOD-PAT-0005` secondary subscriber anchor, verifies legacy OpenEMR `insurance_data` subscriber fields and insurance-detail rendering, verifies the modernized API/UI projection through the Patient/Client insurance panel, and creates/updates/deletes a temporary tertiary coverage row with subscriber details. Slice 202 remains available with patient history/lifestyle evidence, Slice 201 remains available with patient contact-backed care-team evidence, Slice 200 remains available with patient care-team members evidence, and earlier protection, mutation, report, queue, provider, catalog, administration, and authorization-policy evidence remains available through the existing Workbench cards and comparison drill-ins.

The legacy workflow mutation run covers deterministic demographics, guardian contact, guardian details, social details, patient registration, insurance coverage, appointment, appointment reschedule, appointment arrival, appointment check-out, appointment no-show, appointment category, appointment pending-status, appointment provider reassignment, appointment facility reassignment, appointment billing-location reassignment, encounter-detail, encounter metadata, clinical-list, problem-list, medication-list, patient-message, patient-message content, text patient-document, binary patient-document, patient binary document content replacement, image patient-document preview, image patient-document thumbnail, patient-document sign-off, patient-document denial, patient-document metadata, patient-document archive restore, patient-document content replacement, patient-document binary content replacement, patient-document replacement revision, patient scanned attachment, external-link patient-document, prescription, immunization, billing, billing diagnosis, billing correction, billing modifier, lab procedure, lab procedure result correction, lab procedure specimen metadata, lab procedure specimen detail, lab procedure order correction, administration facility, administration user, access-permission, and user group membership lifecycles. The shared patient contact, patient demographics, patient guardian contact, guardian details, patient social details, patient registration, patient insurance, appointment, appointment reschedule, appointment arrival, appointment check-out, appointment no-show, appointment category, appointment pending-status, appointment provider reassignment, appointment facility reassignment, appointment billing-location reassignment, encounter, encounter metadata, clinical-list, problem-list, medication-list, message, message content, text patient-document, binary patient-document, patient binary document content replacement, image patient-document preview, image patient-document thumbnail, patient-document sign-off, patient-document denial, patient-document metadata, patient-document archive restore, patient-document content replacement, patient-document binary content replacement, patient-document replacement revision, patient scanned attachment, external-link patient-document, prescription, immunization, billing, billing diagnosis, billing correction, billing modifier, procedure, procedure result correction, procedure specimen metadata, procedure specimen detail, procedure order correction, admin facility, admin user, access-permission, and user group membership mutation plans now run against both legacy and modernized targets, updating/rendering/restoring the same anchor patient contact record, updating/rendering/restoring the same anchor patient demographics record, updating/rendering/restoring the same anchor patient guardian contact record, updating/rendering/restoring the same anchor patient guardian detail record, updating/rendering/restoring the same anchor patient social detail record, creating/rendering/deleting a temporary registered patient, creating/rendering/updating/deleting the same anchor patient's insurance coverage row, creating/cancelling/deleting the same anchor patient's future appointment lifecycle, creating/rescheduling/deleting the same anchor patient's future appointment lifecycle, creating/marking-arrived/deleting the same anchor patient's future appointment lifecycle, creating/marking-arrived/checking-out/deleting the same anchor patient's future appointment lifecycle, creating/marking-no-show/deleting the same anchor patient's future appointment lifecycle, creating/updating-category/rendering/deleting the same anchor patient's future appointment lifecycle, creating/updating-provider/rendering/deleting the same anchor patient's future appointment lifecycle, creating/updating-facility/rendering/deleting the same anchor patient's future appointment lifecycle, creating/updating-billing-location/rendering/deleting the same anchor patient's future appointment lifecycle, creating/updating/rendering/deleting the same anchor patient's encounter with vitals and SOAP details, creating/rendering/updating/deleting the same anchor patient's encounter sensitivity/referral/external-ID/POS metadata, creating/rendering/deactivating/deleting the same anchor patient's allergy list entry, creating/rendering/deactivating/deleting the same anchor patient's medical-problem list entry, creating/rendering/deactivating/deleting the same anchor patient's medication-list entry, creating/rendering/closing/soft-deleting/hard-deleting the same anchor patient's message entry, editing/rendering the same anchor patient message title/body, creating/rendering/soft-deleting/hard-deleting the same anchor patient's database-backed text document entry, creating/rendering/downloading/soft-deleting/hard-deleting the same anchor patient's PDF-style binary document entry, creating/replacing/downloading/hard-deleting the same anchor patient's PDF-style binary document entry, creating/rendering/inline-previewing/downloading/archiving/hard-deleting the same anchor patient's image document entry, creating/rendering/thumbnailing/archiving/hard-deleting the same anchor patient's image document entry, creating/approving/rendering/archiving/hard-deleting the same anchor patient's reviewed text document entry, creating/denying/rendering/archiving/hard-deleting the same anchor patient's reviewed text document entry, creating/refiling/rendering/archiving/hard-deleting the same anchor patient's text document metadata entry, creating/archiving/restoring/rendering/hard-deleting the same anchor patient's text document entry, creating/replacing/verifying-revision/archiving/hard-deleting the same anchor patient's text document entry, creating/rendering/scanned-readiness/archiving/hard-deleting the same anchor patient's scanned PDF document entry, creating/rendering/archiving/hard-deleting the same anchor patient's external web-url document entry, creating/rendering/deactivating/deleting the same anchor patient's prescription entry, creating/rendering/marking-entered-in-error/deleting the same anchor patient's immunization entry, creating/rendering/marking billed/deactivating/deleting the same anchor patient's CPT fee-sheet line, creating/rendering/deactivating/deleting the same anchor patient's ICD10 fee-sheet diagnosis line, creating/correcting/rendering/deactivating/deleting the same anchor patient's CPT fee-sheet charge line, creating/completing/reporting/resulting/rendering/deleting the same anchor patient's lab procedure lifecycle, correcting/rendering/deleting the same anchor patient's temporary lab result lifecycle, creating/rendering/deleting the same anchor patient's temporary lab report specimen lifecycle, creating/rendering/updating to inactive/default-hiding/deleting a temporary administration facility, creating/rendering/updating to inactive/default-hiding/deleting a temporary administration user, revoking/rendering/restoring a focused Front Office ACL permission assignment, and assigning/rendering/revoking a focused temporary-user Front Office membership. The shared access-control plan runs read-only against both targets and verifies default ACL groups, permission objects, group-permission assignments, and default user memberships. The shared encounter documents plan runs read-only against both targets and verifies linked encounter document facts plus legacy and modernized UI rendering. The shared encounter billing plan runs read-only against both targets and verifies active encounter fee-sheet line facts plus legacy and modernized UI rendering. The shared encounter claims plan runs read-only against both targets and verifies linked encounter claim-status facts plus modernized API/UI rendering. The shared encounter procedures plan runs read-only against both targets and verifies linked encounter procedure-order/report/result facts plus legacy procedure-result rendering and modernized API/UI rendering. The shared pending procedure orders plan runs read-only against both targets and verifies future scheduled lab orders with no linked report rows. The shared reports export plan runs read-only against both targets and verifies normalized operational export rows plus visible CSV export affordances. The shared patient documents plan runs read-only against both targets and verifies seeded document metadata, category mapping, content previews, and visible document lists. The shared patient document content plan runs read-only against both targets and verifies the full stored `MOD-PAT-0001` document payload plus modernized API/viewer/download retrieval. The shared binary patient-document plan runs against both targets and verifies temporary PDF-style upload, visible filing, byte-preserving modernized download, archive, and cleanup. The shared patient binary document content replacement plan runs against both targets and verifies temporary PDF-style filing, in-place byte replacement, replacement download, UI rendering, and cleanup. The shared procedure result correction plan runs against both targets and verifies temporary lab order/report/result creation, in-place result correction, corrected UI/API/database facts, and cleanup. The shared procedure specimen plan runs against both targets and verifies temporary lab report collected date, specimen number, UI/API/database facts, and cleanup. The shared procedure specimen detail plan runs against both targets and verifies temporary order-level specimen facts, UI/API/database facts, and cleanup. The shared procedure order correction plan runs against both targets and verifies temporary lab order metadata correction, post-correction result visibility, UI/API/database facts, and cleanup. The shared patient image document preview plan runs against both targets and verifies temporary `image/svg+xml` filing, inline-preview metadata, browser-visible modernized image rendering, byte-preserving download, archive, and cleanup. The shared patient image document thumbnail plan runs against both targets and verifies temporary `image/svg+xml` filing, stored-byte thumbnail data URI facts, browser-visible modernized document-card thumbnail rendering, archive, and cleanup. The shared patient document sign-off plan runs against both targets and verifies temporary text document pending state, approval/sign-off, browser-visible rendering, archive, and cleanup. The shared patient document denial plan runs against both targets and verifies temporary text document pending state, denied review state, browser-visible rendering, archive, and cleanup. The shared patient document metadata plan runs against both targets and verifies temporary text document refiled title, category, date, encounter, notes, browser-visible rendering, archive, and cleanup. The shared patient document archive restore plan runs against both targets and verifies temporary text document archive hiding, inaccessible content while archived, restore, browser-visible rendering, and cleanup. The shared patient document replacement revision plan runs against both targets and verifies temporary text document replacement updates current revision timestamp/hash while preserving the single current-version contract, browser-visible rendering, archive, and cleanup. The shared patient document external-link plan runs against both targets and verifies temporary `web_url` filing, URL/storage metadata, browser-visible rendering, archive, and cleanup. The shared billing diagnosis plan runs against both targets and verifies temporary `ICD10` fee-sheet row create/render/deactivate/delete behavior. The shared billing correction plan runs against both targets and verifies temporary CPT fee-sheet row create/correct/render/deactivate/delete behavior. The shared billing modifier plan runs against both targets and verifies temporary CPT fee-sheet row create/modify/render/deactivate/delete behavior. The shared claim status plan runs read-only against both targets and verifies seeded OpenEMR claim rows plus modernized Fees rendering. The shared payment posting plan runs read-only against both targets and verifies seeded OpenEMR AR sessions/activities plus modernized Fees rendering. The shared account balance plan runs read-only against both targets and verifies charge, payment, adjustment, and balance rollups plus modernized Fees rendering. The shared account aging plan runs read-only against both targets and verifies deterministic Current, 31-60, 61-90, and Over 90 AR buckets plus modernized Fees rendering. The shared account ledger plan runs read-only against both targets and verifies chronological charge, payment, adjustment, and running-balance rows plus modernized Fees rendering. The shared account statement plan runs read-only against both targets and verifies statement-ready recipient, period, due-date, current-due, past-due, oldest-open, and balance-due facts plus modernized Fees rendering. The shared insurance plan runs read-only against both targets and verifies the primary and secondary `MOD-PAT-0005` coverage rows plus browser-visible chart rendering. The shared insurance mutation plan runs against both targets and verifies temporary tertiary coverage create/render/update/delete behavior. The shared encounter metadata mutation plan runs against both targets and verifies temporary encounter sensitivity/referral/external-ID/POS create/render/update/delete behavior. The shared patient demographics mutation plan runs against both targets and verifies `MOD-PAT-0010` identity, DOB, address, marital-status, and occupation update/render/restore behavior. The shared patient guardian-contact plan runs against both targets and verifies `MOD-PAT-0010` mother name, guardian name, relationship, phone, and email update/render/restore behavior. The shared patient guardian-details plan runs against both targets and verifies `MOD-PAT-0010` guardian sex, address, city, state, postal code, country, work phone, and contact block update/render/restore behavior. The shared patient social-details plan runs against both targets and verifies `MOD-PAT-0010` race, ethnicity, interpreter, family size, monthly income, homeless status, and financial review date update/render/restore behavior. The shared patient registration plan runs against both targets and verifies temporary `TMP-PAT-REG-*` demographics/contact creation, chart rendering, and deletion. The shared immunizations plan runs read-only against both targets and verifies the `MOD-PAT-0007` pediatric vaccine-history anchor plus browser-visible legacy Immunizations and modernized Lists rendering. The Workbench mutation commands use per-test reseeding for stronger isolation, while the tests also perform cleanup so they can safely run inside broader plans.

Slice 215 extends aggregate patient-administration coverage with portal secure-message read-status evidence from legacy `onsite_mail`, modernized `portal_mailbox_messages`, `PUT /api/patient-portal/messages/{messageId}/read`, Portal workspace `Mark read` rendering for the signed-in `MOD-PAT-0004` account after cleanup-backed temporary inbound message creation, and Slice 469 path-backed probe payload attachments for preconditions, pre-read mailbox/home-summary state, cleanup-backed read result, cleanup state, and target-specific read-status UI surfaces. Slice 214 remains available with portal secure-message archive evidence from legacy `onsite_mail`, modernized `portal_mailbox_messages`, `DELETE /api/patient-portal/messages/{messageId}`, Portal workspace `Archive message` rendering for the signed-in `MOD-PAT-0004` account after cleanup-backed temporary message creation, and Slice 468 path-backed probe payload attachments for preconditions, pre-archive mailbox state, cleanup-backed archive result, cleanup state, and target-specific Sent/Archive UI surfaces. Slice 213 remains available with portal secure-message thread view evidence from legacy `onsite_mail`, modernized `portal_mailbox_messages`, `GET /api/patient-portal/messages/{messageId}/thread`, Portal workspace `View thread` rendering for the signed-in `MOD-PAT-0004` account after a cleanup-backed reply, and Slice 467 path-backed probe payload attachments for thread preconditions, seeded original-message/thread anchor state, cleanup-backed thread result, cleanup state, and target-specific thread UI surfaces. Slice 212 remains available with portal secure-message reply evidence from legacy `onsite_mail`, modernized `portal_mailbox_messages`, `POST /api/patient-portal/messages/{messageId}/reply`, Portal workspace inline reply/sent rendering for the signed-in `MOD-PAT-0004` account, and Slice 466 path-backed probe payload attachments for original-message, reply result, cleanup, and Sent-folder UI evidence. Slice 211 remains available with portal secure-message compose evidence from legacy `onsite_mail`, modernized `portal_mailbox_messages`, `POST /api/patient-portal/messages`, Portal workspace compose/sent rendering, and Slice 465 path-backed probe payload attachments for compose result, cleanup, and Sent-folder UI evidence. Slice 210 remains available with portal secure-message inbox evidence from legacy `onsite_mail`, modernized `/api/patient-portal/messages`, Portal workspace rendering, and Slice 464 path-backed probe payload attachments for mailbox identity, message-detail, and UI rendering evidence. Slice 209 remains available with portal home evidence from authenticated portal identity, secure-message counts, upcoming appointment facts, and Slice 463 path-backed probe payload attachments for home-summary and UI rendering evidence. Slice 208 remains available with portal session lifecycle evidence from modernized `patient_portal_sessions.ended_at`, `/api/patient-portal/session` logout, legacy portal cookie logout behavior, and Slice 462 path-backed probe payload attachments for active, ended, inactive, and UI logout states. Slice 207 remains available for portal authentication readiness evidence from legacy `patient_access_onsite.portal_pwd` / `portal_pwd_status` / `portal_onetime`, legacy `patient_data.allow_patient_portal`, modernized `patient_portal_accounts.password_hash` / `password_status` / `one_time_token`, modernized `patients.portal_enabled`, modernized `patient_portal_sessions`, and Slice 461 path-backed probe payload attachments for valid, invalid, disabled, and pending-reset authentication paths. Slice 206 remains available for portal access readiness evidence from legacy `patient_data.allow_patient_portal` and modernized `patients.portal_enabled`. Slice 205 remains available for portal reset readiness evidence from legacy `patient_access_onsite.portal_pwd_status` / `portal_onetime` and modernized `patient_portal_accounts.password_status` / `one_time_token`. Slice 204 remains available for portal account readiness evidence from legacy `patient_data.cmsportal_login` / `patient_access_onsite` and modernized `patients.cms_portal_login` / `patient_portal_accounts`. Slice 203 remains available for insurance subscriber detail evidence from legacy `insurance_data` and modernized `insurance_records`, including seeded secondary subscriber checks and temporary tertiary subscriber mutation cleanup. Slice 202 remains available for read-only history/lifestyle evidence from legacy `history_data` and modernized `patient_histories`. Slice 201 remains available for contact-backed patient care-team evidence: the shared patient care-team contact plan runs against both targets and verifies `MOD-PAT-0010` mixed provider/contact update/render/restore behavior from legacy `care_teams` / `care_team_member.contact_id` and modernized `patient_care_teams` / `patient_care_team_members` plus `patient_related_contacts`, while Slice 200 remains available for multi-member provider-backed care-team evidence. The shared patient care-team members plan runs against both targets and verifies `MOD-PAT-0010` two-member team/provider/facility update/render/restore behavior from legacy `care_teams` / `care_team_member` and modernized `patient_care_teams` / `patient_care_team_members`, while Slice 199 remains available for lead-member care-team behavior. Slice 198 remains available for provider ID/name update/render/restore behavior from legacy `patient_data.providerID` and modernized `patients.provider_id`.

The shared procedure report correction plan runs against both targets and verifies temporary lab report metadata correction, linked result preservation, UI/API/database facts, and cleanup.

The shared procedure report sign-off plan runs against both targets and verifies temporary lab report review/sign-off metadata, linked result preservation, UI/API/database facts, and cleanup.

The shared procedure report review queue plan runs against both targets and verifies temporary lab report unreviewed/reviewed queue membership, queue counts, report metadata, reviewer transition, UI/API/database facts, and cleanup.

The shared procedure report review queue filters plan runs against both targets and verifies temporary lab report patient/date-filtered queue inclusion, outside-date exclusion, reviewed queue transition, UI/API/database facts, and cleanup.

The shared procedure report review queue provider filters plan runs against both targets and verifies temporary lab report provider-filtered queue inclusion, outside-provider exclusion, reviewed queue transition, UI/API/database facts, and cleanup.

The shared procedure lab provider catalog plan runs against both targets and verifies permanent gold-data lab provider rows, seeded lab-order ownership, reviewed queue inclusion, outside-lab exclusion, and browser-visible reviewed queue rendering.

The shared procedure order catalog, order queue, bulk report review, and reopen-review plans run against both targets. Slice 145 verifies permanent gold-data `procedure_type` / `lab_order_catalog` rows, the `Gold Lab Order Catalog` tree, five provider groups, three orderable panels per provider, legacy `types.php`/`types_ajax.php` rendering, modernized `/api/procedures/order-catalog`, and the Reports procedure order catalog panel. Slice 147 verifies temporary catalog item create/update/deactivate/delete lifecycle behavior under provider group `9040`, legacy AJAX rendering, modernized Reports catalog controls, and cleanup. Slice 148 verifies temporary catalog group imports for PathGroup-style order/result CSV rows, re-import deactivate/reactivate semantics, legacy AJAX rendering, modernized `/api/procedures/order-catalog/import-compendium`, modernized Reports catalog rendering, and temporary subtree cleanup. Slice 149 verifies temporary reportless order visibility in the ready-to-send queue, transition to the reported queue after a report is attached, legacy `list_reports.php` queue rendering, modernized `/api/procedures/order-queue`, modernized Reports order queue rendering, and temporary order-tree cleanup. Slice 151 verifies temporary reportless order transmit marking, sent-awaiting-results queue visibility, legacy `list_reports.php` option `4` rendering, modernized `/api/procedures/orders/{orderId}/transmit`, modernized Reports order transmit controls, and temporary order cleanup. Slice 153 verifies two temporary unreviewed reports, bulk sign-off, reviewed queue membership, legacy `list_reports.php` reviewed rendering, modernized `/api/procedures/reports/bulk-sign`, modernized Reports queue controls, and temporary order cleanup. Slice 154 verifies one temporary signed report, reopen review into received/unreviewed state, reviewed queue exclusion, unreviewed queue membership, blank reviewer metadata, legacy `list_reports.php` option `3` rendering, modernized `/api/procedures/reports/{reportId}/reopen-review`, modernized Procedures/Reports rendering, and temporary order cleanup.

Slice 181 extends the Workbench-managed administration/security family with the shared billing authorization-policy readiness plan. Slice 180 remains available with the shared patient message authorization-policy readiness plan. Slice 179 remains available with the shared patient document authorization-policy readiness plan. Slice 178 remains available with the shared patient chart authorization-policy readiness plan. Slice 177 remains available with the shared encounter authorization-policy readiness plan. Slice 176 remains available with the shared appointment authorization-policy readiness plan. Slice 175 remains available with the shared clinical-list authorization-policy readiness plan. Slice 166 extends the Workbench-managed administration/security family with the shared clinical-list protection readiness plan. Slice 165 remains available with the shared patient chart protection readiness plan, Slice 164 remains available with the shared operational reports protection readiness plan, Slice 163 remains available with the shared admin directory protection readiness plan, Slice 162 remains available with the shared admin audit protection readiness plan, Slice 161 remains available with the shared admin session readiness plan, Slice 160 remains available with the shared admin login audit readiness plan, and Slice 159 remains available with the shared admin login readiness plan. Slice 154 extends the Workbench-managed procedure family with the shared procedure report reopen review plan. Slice 153 remains available with the shared procedure report bulk sign-off plan. Slice 151 remains available with the shared procedure order transmit plan. Slice 149 remains available with the shared procedure order queue plan. Slice 148 remains available with the shared procedure vendor compendium import plan. Slice 147 remains available with the shared procedure order catalog lifecycle plan. Slice 145 remains available with the shared procedure order catalog plan. Slice 144 remains available for temporary provider address-book linkage evidence. Slice 142 remains available with the shared procedure lab provider configuration plan for temporary provider protocol, usage, direction, sender/receiver IDs, remote host, login/password, order/result paths, notes, rendering, deletion, and cleanup through legacy OpenEMR and the modernized Reports workspace/API. Slice 141 remains available with the shared procedure lab provider lifecycle plan for temporary provider creation, active rendering, deactivation, include-inactive rendering, deletion, and cleanup. Slice 140 remains available with the shared procedure lab provider directory plan for the permanent five-provider directory, active filtering, NPI values, OpenEMR's default `DL` protocol value, 200 assigned orders per provider, 140 reviewed reports per provider, 60 future scheduled orders per provider, and legacy/modernized browser-visible rendering. Slice 139 remains available with the shared procedure lab provider catalog plan for seeded lab provider catalog rows, `MOD-PAT-0009` report `6000009` ownership by lab `504`, outside-lab exclusion, and UI/API/database facts against both legacy and modernized targets. Slice 138 remains available with the shared procedure report review queue lab filters plan for temporary lab-filtered queue inclusion before sign-off, outside-lab exclusion, reviewed queue membership after sign-off, UI/API/database facts, and cleanup. Slice 137 remains available with the shared procedure report review queue provider filters plan for provider-filtered queue inclusion before sign-off, outside-provider exclusion, reviewed queue membership after sign-off, UI/API/database facts, and cleanup. Slice 136 remains available with the shared procedure report review queue filters plan for patient/date-filtered queue inclusion before sign-off, outside-date exclusion, reviewed queue membership after sign-off, UI/API/database facts, and cleanup. Slice 135 remains available with the shared procedure report review queue plan for received/unreviewed queue membership before sign-off, reviewed queue membership after sign-off, UI/API/database facts, and cleanup. Slice 134 report sign-off remains available for signed reviewer/timestamp metadata, reviewed status, and linked result preservation. Slice 133 report correction remains available for corrected collected date, report date, specimen number, report status, review status, notes, and linked result preservation. Slice 132 order correction remains available for temporary lab order metadata facts, and Slice 131 specimen detail remains available for temporary order-level specimen facts.

Slice 102 and Slice 103 extend the shared appointment mutation family with comments update parity and regular recurrence metadata parity, both using temporary future appointments on `MOD-PAT-0003`, browser-visible legacy and modernized rendering, and hard-delete cleanup.

The shared document preview plan runs read-only against both targets and verifies preview kind, inline-readiness, thumbnail label, thumbnail text, and modernized Documents card rendering.

The shared document revision plan runs read-only against both targets and verifies current revision timestamp, version label, version-history count, revision hash, and modernized Documents card/viewer rendering.

The shared document replacement revision plan runs against both targets and verifies that content replacement advances the current revision timestamp, changes the current hash, preserves `Version 1`/`Current version`, and renders the replacement state in the modernized Documents workspace.

The shared patient document lifecycle timeline plan runs against both targets and verifies temporary patient document create/sign/archive/restore/delete behavior, derived filed/current-version/review/active/archive lifecycle events, and modernized Documents card and viewer timeline rendering.

The shared patient scanned attachment plan runs against both targets and verifies temporary scanned PDF document create/archive/delete behavior, derived scan status, capture source, scanned page count, OCR status, and modernized Documents card and viewer scan-readiness rendering. The shared appointment reschedule plan runs against both targets and verifies temporary future appointment create/update/delete behavior, changed date/time/duration/status/room facts, and modernized Calendar reschedule rendering. The shared appointment arrival plan runs against both targets and verifies temporary future appointment create/mark-arrived/delete behavior, arrived status facts, and modernized Calendar `Mark arrived` rendering. The shared appointment check-out plan runs against both targets and verifies temporary future appointment create/mark-arrived/check-out/delete behavior, checked-out status facts, and modernized Calendar `Mark checked out` rendering. The shared appointment no-show plan runs against both targets and verifies temporary future appointment create/mark-no-show/delete behavior, no-show status facts, and modernized Calendar `Mark no-show` rendering. The shared appointment category plan runs against both targets and verifies temporary future appointment category `13` create/render behavior, category update to `10`, legacy `form_category` rendering, modernized Calendar category selection/rendering, and cleanup. The shared appointment comments plan runs against both targets and verifies temporary future appointment comment create/update/render behavior, legacy `form_comments`, modernized Calendar comments controls, and cleanup. The shared appointment recurrence metadata plan runs against both targets and verifies temporary future appointment regular recurrence create/update/render behavior, legacy repeat controls, modernized Calendar repeat controls, recurrence label rendering, and cleanup. The shared appointment recurring-series plan runs against both targets and verifies seeded regular recurrence expansion from the `MOD-PAT-0003` preventive-care anchor, generated occurrence numbers/dates, modernized Calendar generated-occurrence labels, read-only generated occurrence actions, and unchanged seeded appointment rows. The shared appointment recurrence exception-edit plan runs against both targets and verifies editing the seeded `MOD-PAT-0013` skipped-date list, hiding the newly skipped generated occurrence, restoring the seeded exception list, and modernized Calendar `Skipped dates` rendering. The shared appointment series root update plan runs against both targets and verifies changing the seeded `MOD-PAT-0013` recurring root title/time, generated occurrence propagation, restored seeded root cleanup, and modernized recurring-root edit rendering. The shared appointment series root metadata plan runs against both targets and verifies changing the same seeded root provider, facility, billing location, category, status, room, and comments, generated occurrence metadata propagation, restored seeded root cleanup, and modernized recurring-root metadata edit rendering. The shared appointment monthly recurrence plan runs against both targets and verifies temporary monthly recurring appointment creation, update to every two months, generated occurrence expansion, legacy repeat controls, modernized Calendar repeat controls, generated occurrence rendering, and cleanup deletion. The shared appointment recurrence unit matrix plan runs against both targets and verifies temporary daily, workday, and yearly recurring appointment creation, generated occurrence expansion, legacy repeat controls, modernized Calendar repeat controls, generated occurrence rendering, and cleanup deletion. The shared appointment days-of-week recurrence plan runs against both targets and verifies temporary Monday/Wednesday/Friday recurrence creation, OpenEMR `REPEAT_DAYS` weekday controls, modernized weekday controls, generated occurrence rendering, and cleanup deletion. The shared appointment monthly repeat-on recurrence plan runs against both targets and verifies temporary second-Tuesday and last-Friday monthly recurrence creation, OpenEMR `REPEAT_ON` controls, modernized repeat-on controls, generated occurrence rendering, and cleanup deletion. The shared appointment series recurrence update plan runs against both targets and verifies seeded recurring root cadence/end-date editing, OpenEMR repeat controls, modernized Calendar recurrence controls, generated occurrence changes, skipped-date preservation, and cleanup restoration. The shared appointment provider overlap plan runs against both targets and verifies temporary same-provider, same-time appointments for two patients, distinct appointment rendering, modernized provider-overlap count/ID detail, and cleanup deletion. The shared appointment patient overlap plan runs against both targets and verifies temporary same-patient, same-time appointments with two providers, distinct appointment rendering, modernized patient-overlap count/ID detail, and cleanup deletion. The shared appointment room overlap plan runs against both targets and verifies temporary same-room, same-time appointments for two patients, distinct appointment rendering, modernized room-overlap count/ID detail, and cleanup deletion. The shared patient portal appointment request plan runs against both targets and verifies a signed-in `MOD-PAT-0004` patient can create a pending `^` portal appointment request, generate the provider-facing `Patient Reminders` note, render the requested appointment on the portal surface, and clean up the temporary appointment and reminder rows. The shared patient portal appointment request-options plan runs against both targets and verifies signed-in request category/provider/facility choices, duration defaults, patient-specific provider/facility defaults, and visible portal request form dropdown rendering without mutating seed data. The shared patient portal clinical-summary plan runs against both targets and verifies signed-in problem, allergy, medication-list, and active-prescription facts plus visible portal rendering without mutating seed data. The shared patient portal lab-results plan runs against both targets and verifies signed-in lab order, report, final result values, and visible portal rendering without mutating seed data. The shared patient portal medical-report plan runs against both targets and verifies signed-in customized medical-history report sections, issue choices, encounter/form choices, procedure-order choices, preview content, and visible report-builder rendering without mutating seed data. The shared patient portal generated medical-report PDF plan runs against both targets and verifies PDF export availability, legacy `portal_custom_report.php` PDF POST behavior, modernized `POST /api/patient-portal/medical-report/pdf`, and visible modernized `Download report PDF` readiness without mutating seed data. The shared patient portal generated medical-report issue-selection plan runs against both targets and verifies selected problem, allergy, and medication issue rows flow into the generated customized medical-history report through legacy issue checkboxes and modernized `issueIds`/checkboxes without mutating seed data. The shared patient portal generated medical-report encounter-form selection plan runs against both targets and verifies selected Vitals/SOAP encounter form rows flow into the generated customized medical-history report through legacy `formdir_formid` checkboxes and modernized `encounterFormIds`/checkboxes without mutating seed data. The shared patient portal generated medical-report printable template plan runs against both targets and verifies the printable facility block, patient header, generated-on label, and signature-line readiness through legacy printable report HTML and modernized generated-report `templateMetadata` without mutating seed data. The shared patient portal generated medical-report package plan runs against both targets and verifies deterministic package metadata, legacy printable/PDF source artifacts, modernized `POST /api/patient-portal/medical-report/package`, ZIP `manifest.json`/PDF/`summary.txt` entries, and Portal `Download report package` readiness without mutating seed data. The shared patient portal generated medical-report audit plan runs against both targets and verifies normalized generated-report audit events, modernized `patient_portal_report_audit_events` rows for generation/PDF/package lifecycle actions, `GET /api/patient-portal/medical-report/audit`, and Portal Report Audit rendering without adding permanent seed rows.

Slice 232 seed note: the patient portal secure-message audit plan reads the same stable `MOD-PAT-0004` portal account and mailbox facts as Slices 210 through 217. It adds only transient modernized `patient_portal_message_audit_events` rows during compose, reply, read, single-archive, and selected-archive actions and no permanent patient-domain seed rows.

Slice 233 seed note: the patient portal secure-message encrypted-body plan creates cleanup-backed temporary encrypted inbox rows for the `MOD-PAT-0004` portal account on both targets. It does not add permanent mailbox rows; the baseline remains 400 projected portal mailbox messages, and the modernized API/UI deliberately renders only the protected encrypted-body placeholder for encrypted rows.

Slice 231 seed note: the patient portal generated medical-report audit plan reads the same stable `MOD-PAT-0004` portal account, generated-report, printable-report, PDF-report, billing, and facility facts as Slices 226 through 230. It adds only transient modernized `patient_portal_report_audit_events` rows during generated-report/PDF/package actions and no permanent patient-domain seed rows.

The shared payment posting mutation plan runs against both targets and verifies temporary payment posting create/render/void/delete behavior, active-row hiding after void, account balance rollback, ledger effects, and cleanup.

The shared claim status mutation plan runs against both targets and verifies temporary claim create/render, generated-file status, cleared status, and cleanup.

The shared patient payment capture plan runs against both targets and verifies temporary patient-responsibility payment create/render/void/delete behavior, payer-type-zero semantics, active-row hiding after void, balance/ledger recalculation, cleanup, and modernized Fees workspace rendering.

The shared statement generation plan runs against both targets and verifies deterministic printable patient statement number, payment instructions, generated text, line items, totals, and modernized Fees workspace rendering.

The shared statement batch plan runs against both targets and verifies ranked positive-balance statement candidates, aggregate candidate totals, delivery metadata, and modernized Fees workspace work-queue rendering.

The shared statement batch package plan runs against both targets and verifies deterministic package manifest rows, summary CSV rows, included statement PDFs, and modernized Fees workspace package export rendering.

The shared collections work queue plan runs against both targets and verifies deterministic past-due account ranking, high-priority and over-90 rollups, recommended collection actions, contact metadata, and modernized Fees workspace collections queue rendering.

The shared collections follow-up task plan runs against both targets and verifies pnotes-compatible task creation from the collections queue, close/archive/delete cleanup, legacy pnotes rendering, modernized Messages rendering, and modernized Fees workspace Create Task behavior.

The shared patient-message assignment plan runs against both targets and verifies temporary message reassignment from `admin` to `billing`, unchanged message counts, legacy pnotes rendering, modernized Messages reassignment controls, and cleanup.

The shared patient-message content plan runs against both targets and verifies temporary message title/body editing, unchanged message counts, legacy pnotes rendering, modernized Messages Save Edit controls, and cleanup.

The shared patient-message reply plan runs against both targets and verifies temporary message reply appending, unchanged message counts, legacy pnotes rendering, modernized Messages Reply controls, and cleanup.

The shared patient-message portal metadata plan runs against both targets and verifies seeded `MOD-PAT-0004` portal relation and plaintext encryption flags, plus modernized Messages metadata chip rendering.

The shared patient-message update metadata plan runs against both targets and verifies temporary message create/edit behavior, blank create-time update metadata, pnotes-compatible update user/timestamp stamping after edit, unchanged message counts, legacy pnotes rendering, modernized Messages update metadata rendering, and cleanup.

The shared encounter documents plan runs read-only against both targets and verifies `MOD-PAT-0001` encounter `1000013`, two linked document records, legacy document-category rendering, modernized Encounter detail API document fields, and modernized Encounters workspace attached-document cards.

The shared encounter billing plan runs read-only against both targets and verifies `MOD-PAT-0001` encounter `1000013`, two active CPT4 fee-sheet rows, legacy fee-sheet rendering, modernized Encounter detail API billing fields, and modernized Encounters workspace Fee Sheet Linkage cards.

The shared encounter billing linkage mutation plan runs against both targets and verifies a temporary `CPT4 99499` fee-sheet row on `MOD-PAT-0001` encounter `1000013`, legacy Fee Sheet rendering, modernized Encounter detail API/UI rendering, billed/inactive hiding, and hard-delete cleanup.

The shared encounter procedure-result entry plan runs against both targets and verifies a temporary pending `80053` laboratory order plus reviewed final report/result on `MOD-PAT-0001` encounter `1000013`, legacy Procedure Results rendering, modernized Encounters workspace result-entry behavior, Procedure Orders panel report/result rendering, and hard-delete cleanup. The shared encounter sign-off plan runs against both targets and verifies a temporary `MOD-PAT-0002` encounter attestation, legacy `esign_signatures` facts, modernized `encounter_signatures` facts, modernized Encounter Sign-Off rendering, signature deletion, and encounter cleanup. The shared encounter co-signature plan runs against both targets and verifies temporary `admin` and locked `gold-provider-02` encounter signatures on a temporary `MOD-PAT-0002` encounter, database ordering/hash/amendment facts, modernized `2 signatures` rendering, signature deletion, and encounter cleanup. The shared encounter document revision plan runs against both targets and verifies seeded encounter-attached document current-version metadata, revision timestamps, revision-hash parity, modernized Encounter attachment-card rendering, and side-by-side comparison evidence. The shared encounter document replacement revision plan runs against both targets and verifies a temporary encounter-attached document, replacement payload mutation, current revision hash/timestamp facts, modernized Encounter attachment-card/API rendering, and cleanup. The shared encounter document upload plan runs against both targets and verifies a temporary text document attached to `MOD-PAT-0001` encounter `1000013`, legacy document-category rendering, modernized Encounter attached-document rendering, document deletion, and cleanup. The shared encounter binary document upload plan runs against both targets and verifies a temporary PDF document on the same encounter, legacy document-category rendering, modernized Encounter attached-document rendering, download content, document deletion, and cleanup. The shared encounter document sign-off plan runs against both targets and verifies a temporary text document on the same encounter, pending review state, `admin` approval/sign-off metadata, modernized Encounter approved-state rendering, document deletion, and cleanup. The shared encounter document denial plan runs against both targets and verifies a temporary text document on the same encounter, pending review state, `admin` denied metadata, modernized Encounter denied-state rendering with reviewed actions disabled, document deletion, and cleanup. The shared encounter document metadata plan runs against both targets and verifies a temporary text document on the same encounter, metadata refiling, preserved encounter link, modernized Encounter inline Edit rendering, document deletion, and cleanup. The shared encounter document move plan runs against both targets and verifies a temporary text document moving from `MOD-PAT-0001` encounter `1000013` to same-patient encounter `1000011`, source removal, target attachment, modernized Encounter inline Move rendering, document deletion, and cleanup. The shared encounter document content replacement plan runs against both targets and verifies a temporary text document on `MOD-PAT-0001` encounter `1000013`, in-place payload replacement, replacement preview/current-version facts, modernized Encounter inline Replace rendering, document deletion, and cleanup. The shared encounter document archive/restore plan runs against both targets and verifies a temporary text document on the same encounter, archive hiding from active detail, archived-detail visibility, modernized Encounter inline Restore rendering, document restoration, document deletion, and cleanup. The shared encounter document lifecycle timeline plan runs against both targets and verifies a temporary text document on the same encounter, filed/current-version/review/active/archive lifecycle state, modernized Encounter lifecycle timeline rendering, document restoration, document deletion, and cleanup. The shared encounter external-link document plan runs against both targets and verifies a temporary web URL document on the same encounter, URL-backed `web_url` storage facts, modernized Encounter external-link rendering, archive behavior, document deletion, and cleanup. The shared patient image document preview plan runs against both targets and verifies a temporary `image/svg+xml` document on `MOD-PAT-0001`, inline-preview metadata, modernized Documents viewer image rendering, byte-preserving download behavior, archive behavior, document deletion, and cleanup. The shared patient image document thumbnail plan runs against both targets and verifies a temporary `image/svg+xml` document on `MOD-PAT-0001`, stored-byte thumbnail data URI facts, modernized Documents card thumbnail rendering, archive behavior, document deletion, and cleanup. The prior encounter procedure-order entry plan remains available and verifies pending order create/render/delete behavior on the same anchor.

The shared encounter claims plan runs read-only against both targets and verifies `MOD-PAT-0001` encounter `1000013`, claim `CLAIM-1000013-1`, payer `Acme Health`, `HCFA` target, cleared claim status facts, modernized Encounter detail API claim fields, and modernized Encounters workspace Claim Linkage cards.

The Workbench now exposes curated plan actions for legacy readiness, slice-1 side-by-side readiness through slice-255 patient portal secure-message notification readiness, isolated mutations, and the full legacy parity contract. Plan evidence displays the selected suites so an operator can distinguish a plan run from an individual suite run.

The Workbench also exposes a custom parity run builder on the Test Runs page for each managed application. It reads the parity manifest through the Workbench API and lets an operator choose a suite or plan, reset mode, headed mode, and optional grep filter. The backend validates these values before constructing `scripts/Run-OpenEmrParityTests.ps1`, preserving the allowlisted-command model while making targeted runs and reset-strategy experiments available from the UI.

The Test Runs page now renders recent side-by-side comparison artifacts from `parity-tests/artifacts/comparisons/`. The Workbench API exposes `/api/parity-comparisons`, reads bounded `comparison.json` summaries from that fixed artifact root, and returns normalized comparison rows. The UI shows the selection id, matched/different status, left and right target run IDs, selected suites, check counts, durations, difference counts, difference previews, and the comparison artifact path. Slice 124 adds an icon-driven expand/collapse drill-in on each comparison card that shows the legacy and modernized run artifact paths, comparison JSON path, artifact directory, selected suites, complete difference list, and a matched-state confirmation when no differences are recorded. Slice 125 adds icon links that open the legacy run artifact, modernized run artifact, and comparison JSON through the safe `/api/artifacts/file` endpoint while rejecting non-artifact paths. Slice 155 enriches each comparison side from its run summary and renders direct links to the run JSON, Playwright JSON, JUnit XML, and HTML report when those files exist under the safe artifact roots. This keeps comparison review in the Workbench instead of requiring operators to inspect raw JSON folders after each `npm run compare` execution.

The modernized target test command is `modernized-openemr/scripts/Test-ModernizedBaseline.ps1`, which writes `modernized-openemr/artifacts/latest-modernized-smoke-test.json`. It checks API health, admin login/session/audit readiness, protected access for audit, administration, reports, patient search, clinical lists, appointments, encounters, documents, messages, billing, and procedures, deterministic anchor patient search for `MOD-PAT-0001`, the anchor chart summary response, anchor patient history projection for `MOD-PAT-0010`, anchor insurance subscriber projection for `MOD-PAT-0005`, anchor patient portal account/reset/access/authentication/session/home/home immunization health-snapshot/clinical-summary/lab-results/appointment request-options/secure-message projection for `MOD-PAT-0004`, cleanup-backed patient, patient portal reset, patient portal access, patient portal session logout, patient deceased-status, patient guardian-contact, patient guardian-details, patient social-details, patient insurance subscriber, appointment, encounter, document, message, revenue-cycle, and procedure/lab mutation lifecycles, patient duplicate detection readiness, patient registration validation row safety, encounter amendment history projection, appointment mutation authorization, and the existing administration, access-control, operational-report, and CSV export smoke checks. The reusable side-by-side commands include the `slice-1-readiness` through `slice-255-patient-portal-message-notifications-readiness` parity plans, which write latest summaries for both `legacy-openemr` and `modernized-openemr` under `parity-tests/artifacts/` and can be compared with the parity comparison runner.

This keeps the workbench honest: it reports real automation evidence instead of inventing its own private test flow.


## Seed Data Orchestration

Slice 483 extends Workbench-previewable probe payload evidence into the patient portal generated medical-report printable-template plan. The Slice 229 parity suite now attaches precondition, printable template metadata projection, legacy printable generated-report rendering, and modernized Portal template rendering JSON artifacts so comparison drill-ins can inspect the printable report header evidence behind portal generated medical-report parity. Slice 482 remains the patient portal generated medical-report encounter-form selection probe-payload slice.

The Workbench owns seed-data visibility and orchestration.

Current seed-data files:

- `modernization-workbench/seed-data/manifest.json`
- `modernization-workbench/seed-data/openemr-shared-synthetic-v1/README.md`
- `modernization-workbench/seed-data/openemr-shared-synthetic-v1/personas/golden-patients.json`
- `modernization-workbench/seed-data/openemr-shared-synthetic-v1/scripts/generate-gold-dataset.mjs`
- `modernization-workbench/seed-data/openemr-shared-synthetic-v1/generated/canonical/gold-dataset.json`
- `modernization-workbench/seed-data/openemr-shared-synthetic-v1/generated/summary.json`
- `modernization-workbench/seed-data/openemr-shared-synthetic-v1/generated/legacy-mariadb/seed-gold.sql`

The seed manifest is the shared contract. Application-specific seeders should consume that contract and apply it to their own database. The legacy app currently has a gold seed action that resets the relevant legacy data tables, applies the 1,000-patient dataset, and validates expected counts. The starter example seed remains available as a small fallback.

The modernized app now has a PostgreSQL seeder that consumes the same canonical dataset, generates `modernized-openemr/artifacts/postgres/seed-gold.sql`, resets the modernized read-model tables, applies the canonical gold dataset, and validates counts for patients, insurance records, appointments, encounters, vitals, clinical notes, prescriptions, billing, claims, payment sessions, payment activities, lab orders, lab reports, lab results, messages, portal accounts, problems, allergies, medications, and patient documents.

## Comparison Capabilities

Once the modernized target exists, the workbench should support side-by-side comparison.

Comparison views should include:

- Test pass/fail comparison by suite and workflow.
- Normalized API response differences.
- Normalized database or domain-state differences where appropriate.
- Playwright screenshots or traces for UI workflows.
- Rolling timing and reliability summaries by run and comparison artifact.
- Known accepted differences.
- Blocking differences that prevent parity signoff.

The comparison should focus on observable behavior and domain outcomes rather than identical internal implementation details.

## Technical Architecture Comparison

The workbench should also make the technical modernization visible.

Architecture comparison views should show:

- Legacy, Workbench, and modernized technology stack and runtime components.
- Version numbers for key technologies, images, frameworks, and runtime components when they are pinned or observable from the repo or running containers.
- Data stores used by each system.
- Physical source-line, non-blank-line, file-count, and schema-signal statistics by source component.
- API boundaries and integration points.
- Where business logic lives in each system.
- Authentication and authorization model differences.
- Test coverage by layer for each system.
- Migration status by workflow, table, API, or domain area.

This information currently comes from curated Workbench UI metadata, project documents, package manifests, Docker Compose configuration, verified running-container versions, and the source inventory snapshot. Over time, more of it may be generated from repository scans, build metadata, service health endpoints, or test manifests.

Slice 150 adds the Architecture source inventory. The inventory contract lives at `modernization-workbench/config/source-inventory.json`; the generated snapshot used by `/api/architecture` lives at `modernization-workbench/config/source-inventory.snapshot.json`. Refresh it from `modernization-workbench/` with:

```powershell
npm run generate:source-inventory
```

The inventory counts physical source lines and non-blank lines from configured roots. It intentionally excludes dependency folders, generated output, runtime artifacts, local environment files, static-analysis baselines, and other noisy directories defined by the config. The numbers should be treated as architecture scale indicators, not as feature-completeness or complexity scores. For database comparisons, the Workbench shows DDL/script line counts plus schema signals such as `CREATE TABLE`, `ALTER TABLE`, `CREATE INDEX`, and database routine patterns where applicable.

## Progress Model

Workflow progress should be tracked by modernization slice.

The dedicated Progress page also tracks functionality scope by domain area through `modernization-workbench/config/functionality-progress.json`. This ledger is the project-maintained view of completed, outstanding, deferred, estimated-complete, and estimated-size/complexity-weighted functionality. It may be revised as legacy behavior discovery expands or as the modernization target narrows; the completed/outstanding lists, percent-complete estimates, weights, weighted overall estimate, historical trend, and time-remaining forecast can all move as the known scope changes. Changes to this ledger should be treated as documentation-affecting project state and reflected in the changelog when they materially change scope.

Suggested states:

- Not started.
- Legacy behavior discovery.
- Legacy tests written.
- Modern API designed.
- Modern database mapping designed.
- Modern implementation in progress.
- Modern tests written.
- Side-by-side parity testing.
- Parity verified.
- Deferred or blocked.

Each workflow should link to the relevant documents, tests, logs, and known decisions.

## Design Principle

The workbench is a control surface and evidence viewer. It should not hide the underlying modernization process.

Every action it performs should map to a repeatable command, script, API call, test suite, or documented workflow that can be inspected and run without the workbench.
